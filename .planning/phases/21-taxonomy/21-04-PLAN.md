---
phase: 21-taxonomy
plan: "04"
type: execute
wave: 4
depends_on: [21-03]
files_modified:
  - src/lib/taxonomy/apply-taxonomy.ts
  - src/lib/taxonomy/reference-migration.ts
  - tests/taxonomy/taxonomy-structural.test.ts
autonomous: true
requirements: [TAX-02, TAX-03, TAX-04, TAX-05, TAX-06, EXP-05]
must_haves:
  truths:
    - "A split assigns every story/spec/claim/citation/evidence/media/reference row to an explicit child or blocks; it never copies an ambiguous mixed payload to all children."
    - "Ordinary color, plating, nib width and market SKU remain hierarchical variants; stable mechanism/size/positioning differences become explicit sibling models only when the manifest says so."
    - "Every FK, polymorphic text target and embedded JSON slug is migrated or proven intentionally retained; foreign_key_check alone is not accepted as proof."
    - "Each canonical pen has one direct made_by brand, forward/reverse links are paired, and scoped licence/producer relations never become a second brand."
    - "Media/source provenance is preserved, ambiguous media remains on the retired source, and reviews are never cloned."
  artifacts:
    - path: "src/lib/taxonomy/reference-migration.ts"
      provides: "Typed dependency assignment and safe polymorphic/JSON rewrite helpers"
      exports: ["resolveReferenceAssignments", "rewriteIdentityJsonReferences"]
    - path: "src/lib/taxonomy/apply-taxonomy.ts"
      provides: "Split, variant and complete dependency-closure application"
      exports: ["applyTaxonomyPlan"]
    - path: "tests/taxonomy/taxonomy-structural.test.ts"
      provides: "Distinct-child, evidence, media, relation and semantic-orphan contracts"
  key_links:
    - from: "src/lib/taxonomy/apply-taxonomy.ts"
      to: "src/lib/taxonomy/reference-migration.ts"
      via: "manifest-owned payload mapping before insert/repoint"
      pattern: "resolveReferenceAssignments"
    - from: "src/lib/taxonomy/reference-migration.ts"
      to: "citations.target_id|diagrams.hotspots_json|exhibit_sections.related_entity_slugs_json"
      via: "typed parse/validate/re-serialize, never raw text replacement"
      pattern: "rewriteIdentityJsonReferences"
---

<objective>
Complete the branching taxonomy migration: split mixed pages, build family/edition/variant hierarchy, and move the full dependency graph without dangling evidence, wrong media or brand relations.

Purpose: TAX-02/03/06 are not satisfied by changing a title; every owned fact and embedded reference must follow the correct canonical child.
Output: Reference-migration helpers, complete transaction behavior and adversarial structural tests.
</objective>

<execution_context>
@/Users/xz/.codex/gsd-core/workflows/execute-plan.md
@/Users/xz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/21-taxonomy/21-RESEARCH.md
@.planning/phases/21-taxonomy/21-PATTERNS.md
@.planning/phases/21-taxonomy/21-SPLIT-IDENTITY-LOCK.md
@.planning/phases/21-taxonomy/21-UI-SPEC.md
@.planning/phases/21-taxonomy/21-03-SUMMARY.md
@src/lib/taxonomy/identity-plan.ts
@src/lib/taxonomy/apply-taxonomy.ts
@migrations/011_library_schema.sql
@migrations/031_evidence_readiness_v2.sql

<interfaces>
Every split manifest row supplies explicit `payloadAssignments` from old child IDs/fields to final entity and final payload IDs. `shared` is legal only when the evidence scope supports every destination and each cloned claim/spec/citation/evidence row receives a new immutable ID.

For Waterman、Opus 88、Leonardo and Aurora, `21-SPLIT-IDENTITY-LOCK.md` overrides earlier fallback suggestions. Plan 21-04 executes the exact materialized IDs/slugs/actions from Plan 21-02; it may neither defer those cases nor generate replacement IDs at runtime.

Canonical brand direction remains `pen --made_by--> brand`. Other producer/licence/family links use their own allowlisted relation types and never satisfy brand cardinality.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Split mixed identities and build the model/edition/variant hierarchy</name>
  <files>src/lib/taxonomy/apply-taxonomy.ts, src/lib/taxonomy/reference-migration.ts, tests/taxonomy/taxonomy-structural.test.ts</files>
  <behavior>
    - Waterman -> existing `gwKClNnwt3V3` becomes draft `waterman-hemisphere`; deterministic `4dcEbeUCjxH-` creates draft `waterman-charleston`; each receives only assigned content/evidence.
    - Opus 88 -> `dTCUDu03vrI6` retires; `CqFpmT3l4Mtm` Demo and `0CNmbxM54-GA` Koloro are distinct drafts; old mixed route has no redirect row and always 404.
    - Leonardo -> `s0HAxT1gsHxh` retires; `ixul2gTcJ06B` Furore and `UE5otlwKUfp9` Momento Magico are distinct drafts; old mixed route has no redirect row and always 404.
    - Aurora -> generic `G9ptvLpfyzNQ` retires; `5CcEDOz9jiUg` 88 and `5waoVLPHU2Pt` Optima are drafts with no generic story/spec/tag/claim/source/media inheritance; old pen route may target only public `/brand/aurora`.
    - PGS -> one canonical Slim model, SHIKIORI edition group and child colors; editions/colors/nib widths create zero pen entities/count.
    - Split row with one unassigned or multiply assigned payload item -> preflight blocker and zero writes.
    - New child/payload IDs -> immutable, deterministic, draft and free of copied reviews.
  </behavior>
  <action>
Extend resolution to require exhaustive row/field assignments for stories, specs, variants, claims, citations, source/entity references, scopes, evidence, conflicts, media, tags, relations, timeline and embedded references before any split can apply. Execute the Plan 21-02 locked table verbatim: retain/rename `gwKClNnwt3V3`, create exactly the seven deterministic new entity IDs, retire the three exact donor/generic IDs, attach every output to its one locked existing brand, and keep every affected/new output draft. Never call a random ID generator for these entities.

Move a payload only when its manifest assignment belongs wholly to one child. Every old payload ID must resolve exactly once to one supported output, `retired_source`, or `pending_conflict`; zero/multiple assignment aborts before writes. Reconstruct genuinely shared background only as separately evidenced rows with deterministic new IDs. Leave ambiguous material on the retired source or in conflict, never duplicate a mixed story, and never copy a donor review/hash/publication snapshot. Record route ledgers exactly: Waterman conditional public-Hémisphère redirect; Opus/Leonardo no redirect record; Aurora conditional public-brand correction only.

Implement hierarchical variants for family/model/edition group/color/material/nib option/market SKU. Encode PGS/SHIKIORI under one model; keep Slim 21/full-size/Realo/Mini/KOP as siblings only when the manifest declares their stable differences. Apply the same EXP-05 rule to MR/Cocoon SKUs, Elabo bodies, Majohn A1 clip/colors/nibs and Asvine nib options. Do not add a public family route/entity type.
  </action>
  <verify><automated>pnpm exec tsx --test --test-name-pattern "mixed split|variant hierarchy|ambiguous payload" tests/taxonomy/taxonomy-structural.test.ts</automated></verify>
  <done>Every branching outcome is explicit, draft, evidence-scoped and free of duplicate mixed payload.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate the complete reference, evidence, relation and media closure</name>
  <files>src/lib/taxonomy/apply-taxonomy.ts, src/lib/taxonomy/reference-migration.ts, tests/taxonomy/taxonomy-structural.test.ts</files>
  <behavior>
    - FK surfaces -> attributes/tags/concepts/aliases/external IDs/stories/specs/variants/claims/media/timeline/community/references/scopes/evidence/conflicts all resolve to the declared owner.
    - Polymorphic citation target and inbound claim object -> final IDs or explicit retained retired owner; no silent null/wrong child.
    - Diagram hotspot and exhibit slug JSON -> parsed, validated and reserialized; malformed/ambiguous JSON blocks transaction.
    - Media -> target-exact licensed asset moves once; ambiguous split hero moves zero times and is never copied to both children.
    - Source items -> shared document IDs remain stable; entity ownership/evidence edges are repointed/deduped.
    - Relations -> no self/duplicate edge, every forward row has swapped reverse row, every public pen has exactly one public canonical made_by brand.
    - Review/publication -> donor reviews remain with donor/revoked; target/new child requires fresh hash and four fresh approvals.
  </behavior>
  <action>
Implement the full ordered topology from `21-PATTERNS.md`: migrate set-valued metadata; then stories/specs/variants/scopes; claims in both subject and object roles; citations and evidence/conflicts; sources/references/timeline/community/media/diagrams; embedded JSON; finally canonical forward links and their reverse pairs. Preserve source item IDs and provenance. Use typed target maps and bound SQL; use parse/validate/re-serialize for JSON arrays/objects and exact `target_type` handling for citations. Rebuild `made_by` last, enforcing one canonical brand; encode JunLai/Wing Sung only as a scoped `marketed_under_licensed_brand` relation while the 630 remains draft, never as a second maker or global brand merge. Finish inside the transaction with exact action parity, quick/foreign-key checks, semantic orphan scans, reverse-link parity and zero unexpected `public_entities` membership.

Seed unique old hero/source title/citation locator/tag/relation sentinels. Assert allowed items appear once at the correct child and ambiguous/forbidden items appear zero times. Never use `INSERT OR REPLACE`, disabled foreign keys, best-effort retry, primary-key updates or review cloning.
  </action>
  <verify><automated>pnpm exec tsx --test --test-name-pattern "dependency closure|polymorphic|JSON slug|media provenance|made_by|review inheritance" tests/taxonomy/taxonomy-structural.test.ts &amp;&amp; pnpm exec tsc --noEmit</automated></verify>
  <done>The entire entity dependency graph reaches an explicit valid owner with no semantic orphan, duplicate brand, wrong image or inherited approval.</done>
</task>

</tasks>

<prohibitions>
- Do not copy a mixed story/spec/media/evidence graph to every split child or guess an ambiguous destination.
- Do not duplicate/rename source documents, rewrite historical logos/captions, or promote gallery/generic media.
- Do not create family/edition/color/nib pages or count them as canonical models.
- Do not access the protected catalog, remote database or production in this plan.
</prohibitions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| manifest assignments -> dependency graph | One wrong mapping can attach facts, rights or brands to the wrong model. |
| polymorphic/JSON references -> route graph | These references have no FK protection and can silently dangle. |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-21-13 | Spoofing | split child identity | high | mitigate | Exhaustive exact payload assignments; ambiguity blocks the whole action. |
| T-21-14 | Tampering | citation/JSON references | high | mitigate | Typed target maps, parser validation and semantic orphan scans. |
| T-21-15 | Information Disclosure | wrong media/source ownership | high | mitigate | Evidence-scoped target assignment and zero-copy rule for ambiguous assets. |
| T-21-16 | Elevation of Privilege | made_by/licence confusion | high | mitigate | Exactly one canonical maker; separate scoped producer/licence relations. |
</threat_model>

<verification>
- `pnpm exec tsx --test tests/taxonomy/taxonomy-structural.test.ts`
- `pnpm exec tsx --test tests/taxonomy/taxonomy-canonical.test.ts`
- `pnpm exec tsc --noEmit`
- `pnpm biome check src/lib/taxonomy/apply-taxonomy.ts src/lib/taxonomy/reference-migration.ts tests/taxonomy/taxonomy-structural.test.ts`
</verification>

<success_criteria>
- All locked split/family/variant cases match `21-SPLIT-IDENTITY-LOCK.md` and the Plan 21-02 exact IDs/slugs/routes; no unassigned dependency is silently dropped or duplicated.
- FK, polymorphic, JSON, media, source and relation checks are all green.
- Every affected output remains non-public and requires fresh independent reviews.
</success_criteria>

<output>Create `.planning/phases/21-taxonomy/21-04-SUMMARY.md` when done.</output>
