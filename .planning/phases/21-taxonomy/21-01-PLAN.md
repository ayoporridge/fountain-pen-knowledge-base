---
phase: 21-taxonomy
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/032_taxonomy_identity.sql
  - src/lib/publication.ts
  - src/lib/db.ts
  - tests/taxonomy/taxonomy-substrate.test.ts
autonomous: true
requirements: [TAX-03, TAX-05, TAX-06, EXP-05]
must_haves:
  truths:
    - "Alias、lineage、redirect、taxonomy action 与 hierarchical variant 都有可约束、可审计的持久化结构。"
    - "Reviewed alias、canonical tag、用户可见 taxonomy relation 与 variant hierarchy 进入当前 publication payload。"
    - "Migration 032 将 publication contract 原子升级为 sha256:v3；所有受 publication gate 管理的既有 brand/pen v2 snapshot（不只 manifest 后续会改的实体）即使之后没有 taxonomy mutation，也会 revision++、撤销旧审批并失去公开资格。"
    - "Migration 031 的 v2 reviews 作为 revoked history 原样保留；032 之后只有 v3 hash/contract 能新增 approved/current review、写入 publication snapshot 或通过公开 gate。"
    - "任何受影响 identity/taxonomy payload 变化都会增加 content_revision、撤销旧审核，并使原 published snapshot 失效。"
    - "Migration 032 不改旧 migration checksum，不更新 immutable semantic ID，也不自动发布任何实体。"
  artifacts:
    - path: "migrations/032_taxonomy_identity.sql"
      provides: "Taxonomy substrate, constraints, immutability and invalidation triggers"
      contains: "taxonomy_batches"
    - path: "src/lib/publication.ts"
      provides: "Taxonomy-aware canonical publication payload"
      exports: ["readPublicationContentPayload", "computePublicationContentHash"]
    - path: "tests/taxonomy/taxonomy-substrate.test.ts"
      provides: "Fresh-database migration, hash and review-revocation contracts"
  key_links:
    - from: "migrations/032_taxonomy_identity.sql"
      to: "entity_publications.content_revision"
      via: "alias/tag/visible-link/variant insert-update-delete invalidation triggers"
      pattern: "content_revision"
    - from: "src/lib/publication.ts"
      to: "migrations/032_taxonomy_identity.sql"
      via: "stable serialization of reviewed aliases, taxonomy links and hierarchy fields"
      pattern: "entity_aliases|entity_tags|member_of_series|marketed_under_licensed_brand"
---

<objective>
Close the two irreversible Phase 21 blockers before any identity row is moved: create the taxonomy substrate and make every user-visible identity change participate in current-hash review invalidation.

Purpose: A correct merge or alias is unsafe if the old review remains current or the route cannot later be audited against a ledger.
Output: Migration 032, taxonomy-aware publication payload, and focused disposable-database contracts.
</objective>

<execution_context>
@/Users/xz/.codex/gsd-core/workflows/execute-plan.md
@/Users/xz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/21-taxonomy/21-RESEARCH.md
@.planning/phases/21-taxonomy/21-PATTERNS.md
@.planning/phases/21-taxonomy/21-UI-SPEC.md
@migrations/011_library_schema.sql
@migrations/031_evidence_readiness_v2.sql
@src/lib/publication.ts
@src/lib/db.ts

<interfaces>
`migrateDatabase()` in `src/lib/db.ts` owns migration ordering, checksum validation and each migration transaction. Do not add another migration table or transaction runner.

`readPublicationContentPayload(db, entityId)` and `computePublicationContentHash(db, entityId)` in `src/lib/publication.ts` own the single canonical review payload. Migration 032 upgrades that contract in place from `sha256:v2` to `sha256:v3`; do not add a parallel taxonomy hash or let v2 authorize a public entity.

Existing publication lifecycle rows use `content_revision`; current-hash reviews are revoked when revision changes, and published snapshots are immutable. Taxonomy execution must demote before later payload writes.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the constrained identity, lineage and variant substrate</name>
  <files>migrations/032_taxonomy_identity.sql, src/lib/db.ts, tests/taxonomy/taxonomy-substrate.test.ts</files>
  <behavior>
    - Fresh migrated database -> taxonomy tables/columns/constraints exist and the second canonical migration run is a no-op.
    - Alias kind/market/validity/source-item/review state -> valid scoped values persist; invalid enum/date/source combinations fail.
    - Taxonomy action -> stable batch/source-row/action/checksum keys are unique and replay-safe.
    - Split/merge/rename lineage and old-route ledger -> support one-to-many lineage and one old route without open-path targets.
    - Model variant -> edition group can parent color/material/nib/market-SKU children; cycles and cross-model parents fail.
    - Existing semantic IDs -> update attempts remain rejected.
    - Pre-migration fully published v2 brand/pen -> applying migration 032 alone increments `content_revision` exactly once, demotes `published` to `in_review`, clears/revokes its old review snapshot and removes it from `public_entities` without any later taxonomy write.
    - Post-migration v2 hash/review rows -> cannot authorize publication; only a fresh current v3 fact/language/media/publication review set may restore public membership.
    - SQLite schema-object scan -> no CHECK/view/trigger/readiness/publication guard authorizes `sha256:v2:` or contract `2`; the sole surviving v2 schema reference is the `entity_content_reviews` revoked-history compatibility clause.
  </behavior>
  <action>
Write RED contracts first using a test-local `mkdtemp` database and the canonical migration entry point; this is the only Wave 0 scaffold and must not import Phase 19 lifecycle code or touch the protected catalog. Add migration 032 without modifying migrations 001–031. Extend `entity_aliases` with `alias_kind`, `market`, validity dates, `source_item_id`, `review_status` and immutable IDs. Add `taxonomy_batches`, `taxonomy_actions`, `entity_lineage`, and `entity_redirects` with exact action enums, stable source-row/checksum uniqueness, canonical `/brand|pen/slug` path constraints, and explicit split fallback reason. Extend `model_variants` with `variant_kind`, `parent_variant_id`, `product_code`, and `market`; keep family as taxonomy data, never a new public entity type. Add only constraints/indexes/triggers needed by these contracts.

In that same migration transaction, upgrade the only accepted publication contract to `PUBLICATION_CONTRACT_VERSION = 3` / `sha256:v3`. Perform the migration-031 compatibility work explicitly in SQLite dependency order:

1. Drop the dependent publication trigger/view family before either table rename. Rebuild `entity_content_reviews` through a temporary v3 table with the same IDs, entity/review kind, reviewer, timestamps, notes and uniqueness. Copy every legacy v2 row with `status='revoked'`; its CHECK may accept a v2 hash only when status is revoked, while pending/approved/rejected/current rows must be well-formed `sha256:v3:`. Recreate its lookup index. No legacy row is deleted or rewritten to a fake v3 hash.
2. Rebuild `entity_publications` through a temporary v3 table whose `approved_content_hash` CHECK is `NULL` or well-formed `sha256:v3:` and whose `reviewed_contract_version` CHECK is `NULL` or exactly `3`. Before copying, clear every old reviewed hash/revision/contract/reviewer/publish timestamp snapshot; for every governed existing brand/pen—not merely later manifest members—increment `content_revision` exactly once, demote `published` to `in_review`, and preserve existing `draft|in_review|retired`. Recreate both publication indexes.
3. Drop/recreate all migration-031 qualification/readiness objects: `publication_v2_qualified_source_items`, `publication_v2_field_evidence`, `publication_v2_qualified_core_claims`, `publication_v2_source_groups`, `publication_v2_source_group_counts`, `publication_v2_required_spec_fields`, `publication_v2_missing_core_claim_evidence`, `publication_v2_unresolved_conflicts`, `publication_v2_current_reviews`, `publication_v2_qualified_primary_media`, `publication_base_blockers`, `publication_public_brands`, `publication_blockers`, `public_entity_readiness`, and `public_entities`. Compatibility names may remain, but current-review hash checks, emitted `contract_version`, blocker filters and the sole public authorization branch must all use v3/contract 3.
4. Drop/recreate `publication_publish_insert_guard` and `publication_publish_transition_guard` after the views/tables are coherent. Both reject v2 hashes, contract 2, stale revision or incomplete v3 reviews; no direct SQL path may bypass the v3 gate. Recreate every mutation invalidation trigger dropped for the table rebuild, plus the new taxonomy invalidation triggers, under its original stable name.

Extend `src/lib/db.ts` readiness checks to require migration 032, contract v3 and every taxonomy object before serving. Add a `sqlite_schema` test over tables/views/triggers: fail on any contract-2 authorization or v2 hash acceptance, allowing only the exact `entity_content_reviews` clause that couples v2 to `status='revoked'`; separately prove the legacy row IDs and audit fields survive and cannot transition back to approved/current. No existing alias is silently approved, no new entity/publication row is inserted, and no semantic primary key is rewritten.
  </action>
  <verify><automated>pnpm exec tsx --test --test-name-pattern "taxonomy substrate|migration replay|v2 compatibility|schema authorization scan|immutable identity" tests/taxonomy/taxonomy-substrate.test.ts</automated></verify>
  <done>Migration 032 is checksum-safe, idempotent through `migrateDatabase`, and rejects malformed identity/lineage/variant state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Put visible taxonomy in the publication hash and revoke stale approvals</name>
  <files>migrations/032_taxonomy_identity.sql, src/lib/publication.ts, tests/taxonomy/taxonomy-substrate.test.ts</files>
  <behavior>
    - Approved alias/tag/visible relation/variant hierarchy change -> canonical hash changes, content revision increments, all old fact/language/media/publication approvals become non-current.
    - Pending/rejected alias or internal lineage/action row -> no user-visible payload contribution.
    - `made_by`, `member_of_series`, and scoped `marketed_under_licensed_brand` -> stable sorted payload; unrelated graph links remain outside.
    - Demoted affected entity -> remains draft/in_review after mutation; no trigger or helper republishes it.
    - Reverting content to an older value -> old review does not become current again.
    - Fresh v3 review set -> publication can succeed through the existing API; substituting a matching v2 hash or old v2 approval still fails closed.
  </behavior>
  <action>
Extend `readPublicationContentPayload` in place so stable canonical JSON includes reviewed aliases with scope/provenance, canonical tag IDs, the allowlisted user-visible relation tuples, and every new variant hierarchy field. Preserve deterministic ordering and emit only the new `sha256:v3` prefix; do not weaken evidence, media, story or `made_by` checks. In migration 032 add owner-aware insert/update/delete invalidation for reviewed aliases, entity tags, the allowlisted relation types and hierarchy fields. Triggers must invalidate both directly affected entities and canonical brands whose visible reverse model/taxonomy set changes, while leaving unrelated entities unchanged.

Tests must first seed a fully reviewed/published v2 brand and pen under migrations 001–031, then apply 032 without any taxonomy mutation and prove revision `+1`, demotion, old-approval revocation and absence from `public_entities`. They must also prove a direct v2 review/snapshot cannot pass the new public gate, no review row is copied or synthesized, and lawful republish requires a complete fresh current-hash v3 review set through the existing publication APIs. Separate mutation tests still prove later visible taxonomy changes invalidate v3 approvals without double-counting the one-time migration revision.
  </action>
  <verify><automated>pnpm exec tsx --test --test-name-pattern "taxonomy hash|review revocation|no automatic republish" tests/taxonomy/taxonomy-substrate.test.ts &amp;&amp; pnpm exec tsc --noEmit</automated></verify>
  <done>Every visible identity mutation is hash-bound and fail-closed; no old review or publication state survives by omission.</done>
</task>

</tasks>

<prohibitions>
- Do not open/copy/migrate `data/fpkg.db` in this plan; use only the test-owned temporary database.
- Do not modify migrations 001–031, accept any v2 snapshot after migration 032, weaken `public_entities`, or install packages.
- Do not migrate content, create canonical rows, move reviews, or auto-republish.
- Do not expand Phase 19 adversarial/process lifecycle infrastructure.
</prohibitions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| migration SQL -> persistent identity contract | Schema and triggers can authorize or invalidate every later taxonomy action. |
| stored taxonomy rows -> publication hash | User-visible identity data becomes part of the reviewed public payload. |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-21-01 | Tampering | taxonomy/publication schema | high | mitigate | Dependency-ordered SQLite rebuild, revoked-only v2 history clause, v3-only publication checks/guards, schema authorization scan, strict taxonomy constraints and replay checksum. |
| T-21-02 | Elevation of Privilege | publication invalidation | high | mitigate | Hash all visible taxonomy fields and revoke current reviews on every owner-aware mutation. |
| T-21-03 | Information Disclosure | publication payload | medium | mitigate | Include only approved public alias/scope fields; exclude internal action, blocker and review state. |
| T-21-04 | Denial of Service | invalidation fan-out | low | accept | Bounded 305-row offline migration; exact owner queries and focused tests prevent global invalidation. |
</threat_model>

<verification>
- `pnpm exec tsx --test tests/taxonomy/taxonomy-substrate.test.ts`
- `pnpm exec tsc --noEmit`
- `pnpm biome check src/lib/publication.ts tests/taxonomy/taxonomy-substrate.test.ts`
- `git diff --check -- migrations/032_taxonomy_identity.sql src/lib/publication.ts src/lib/db.ts tests/taxonomy/taxonomy-substrate.test.ts`
</verification>

<success_criteria>
- Migration 032 creates only the reviewed identity substrate and its invalidation contract.
- Migration 032 alone revokes every affected v2 brand/pen publication snapshot; only fresh v3 review state can authorize public membership.
- Alias/tag/visible relation/hierarchical variant changes deterministically alter the v3 current hash and revoke old reviews.
- No existing review is moved, no entity is auto-published, and no protected catalog path is accessed.
</success_criteria>

<output>Create `.planning/phases/21-taxonomy/21-01-SUMMARY.md` when done.</output>
