---
phase: 21-taxonomy
plan: "02"
type: execute
wave: 2
depends_on: [21-01]
files_modified:
  - data/taxonomy/v1.2-phase21.json
  - src/lib/taxonomy/identity-plan.ts
  - scripts/lib/taxonomy-fixture.ts
  - tests/taxonomy/taxonomy-ledger.test.ts
autonomous: true
requirements: [TAX-01, TAX-02, TAX-03, TAX-04, TAX-05, EXP-01, EXP-05]
must_haves:
  truths:
    - "The seven geographic tables contribute exactly 109 unique matrix rows: P0/P1/P2/P3 = 13/54/40/2."
    - "Every row has one reviewed six-class primary action plus explicit applied/gated/deferred and variant dispositions; no action is inferred from fuzzy names."
    - "Official-source addendum actions are preserved separately and never alter the 109 denominator."
    - "Canonical brand/pen/page deltas are recomputed from stable before/after ID sets and reconcile with atomic actions."
    - "A Phase 21-owned fixture rejects remote/protected targets before allocating any temporary resource."
    - "The locked Waterman、Opus 88、Leonardo、Aurora rows are mandatory Phase 21 actions: exact IDs/slugs/routes/payload assignments are materialized in the manifest and cannot be gated or deferred."
  artifacts:
    - path: "data/taxonomy/v1.2-phase21.json"
      provides: "Reviewed 109-row identity manifest plus out-of-matrix addendum"
    - path: "src/lib/taxonomy/identity-plan.ts"
      provides: "Strict manifest decoder, checksum validation and set-based reconciliation"
      exports: ["TaxonomyDecision", "TaxonomyPlan", "loadTaxonomyPlan", "reconcileTaxonomyPlan"]
    - path: "scripts/lib/taxonomy-fixture.ts"
      provides: "Guarded owned local database fixture"
      exports: ["TaxonomyFixture", "assertTaxonomyFixtureEnvironment", "createTaxonomyFixture", "cleanupTaxonomyFixture"]
    - path: "tests/taxonomy/taxonomy-ledger.test.ts"
      provides: "Denominator, checksum, net-set and fixture-safety contracts"
  key_links:
    - from: "data/taxonomy/v1.2-phase21.json"
      to: ".planning/research/V1.2-MODEL-COVERAGE.md"
      via: "stable source_row_key for every geographic-table row"
      pattern: "sourceRowKey"
    - from: "src/lib/taxonomy/identity-plan.ts"
      to: "data/taxonomy/v1.2-phase21.json"
      via: "strict unknown-input decoder and canonical JSON checksum"
      pattern: "loadTaxonomyPlan"
---

<objective>
Freeze the complete decision universe before mutation: a guarded Phase 21 fixture and a checked-in, checksum-bound 109-row manifest whose actions and net effects are independently reproducible.

Purpose: EXP-01 fails if a matrix row, future row, alias or variant is silently counted as a new page.
Output: Disposable fixture, manifest, decoder/reconciler and deterministic contracts.
</objective>

<execution_context>
@/Users/xz/.codex/gsd-core/workflows/execute-plan.md
@/Users/xz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/research/V1.2-MODEL-COVERAGE.md
@.planning/research/V1.2-OFFICIAL-COVERAGE-GAPS-2026-07-18.md
@.planning/phases/21-taxonomy/21-IDENTITY-RESEARCH.md
@.planning/phases/21-taxonomy/21-RESEARCH.md
@.planning/phases/21-taxonomy/21-SPLIT-IDENTITY-LOCK.md
@.planning/phases/21-taxonomy/21-UI-SPEC.md
@.planning/phases/21-taxonomy/21-01-SUMMARY.md
@scripts/lib/renderer-fixture.ts

<interfaces>
Manifest rows use immutable `sourceRowKey`, explicit before/after entity tuples, one `primaryAction` in `create|merge|split|rename|alias|retire`, `executionState` in `apply|gated|defer`, optional `variantDisposition`, atomic actions, aliases, redirects, evidence references and publication disposition.

`variant`, `gated` and `defer` are not hidden seventh/eighth primary-action counters: they describe the canonical outcome/execution state. Each source row still records the reviewed structural primary action, while reports separate planned, applied and deferred counts.

`21-SPLIT-IDENTITY-LOCK.md` is authoritative for its four mixed/generic cases. Materialize these exact permanent entity IDs in the manifest (the seven new IDs are the first 12 base64url characters of `sha256("phase21:pen:<canonical-slug>")`; runtime must never call `nanoid`/`randomUUID`):

| Action | Entity ID | Canonical slug | Canonical maker |
|---|---|---|---|
| retain + rename | `gwKClNnwt3V3` | `waterman-hemisphere` | `zkAu9PePDdqJ` |
| create | `4dcEbeUCjxH-` | `waterman-charleston` | `zkAu9PePDdqJ` |
| create | `CqFpmT3l4Mtm` | `opus-88-demo` | `I6tjleAZx9RU` |
| create | `0CNmbxM54-GA` | `opus-88-koloro` | `I6tjleAZx9RU` |
| create | `ixul2gTcJ06B` | `leonardo-furore` | `g5r4udSOYhI5` |
| create | `UE5otlwKUfp9` | `leonardo-momento-magico` | `g5r4udSOYhI5` |
| create | `5CcEDOz9jiUg` | `aurora-88` | `CJXe8UpnkHLJ` |
| create | `5waoVLPHU2Pt` | `aurora-optima` | `CJXe8UpnkHLJ` |

Preflight must reject any collision or mismatch between the materialized ID and this derivation. Locked old routes are also data, not a later heuristic: Waterman old mixed path conditionally redirects to public Hémisphère; Opus 88 and Leonardo old mixed paths always 404 and have no redirect row; Aurora old pen path conditionally redirects only to public `/brand/aurora`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the bounded Phase 21 disposable fixture</name>
  <files>scripts/lib/taxonomy-fixture.ts, tests/taxonomy/taxonomy-ledger.test.ts</files>
  <behavior>
    - Explicit fixture flag plus owned local destination -> one temp root/database is migrated and registered for cleanup.
    - Missing flag, remote credentials, inherited external base URL, protected catalog path, symlink/hardlink alias or outside-root destination -> failure before root/database creation.
    - Cleanup -> closes clients and removes only the registered owned root; uncertain ownership fails closed.
  </behavior>
  <action>
Create the smallest Phase 21-owned fixture needed by the remaining plans. Require `TAXONOMY_FIXTURE=1`, reject Turso credentials, inherited external `E2E_BASE_URL`, the protected catalog and filesystem aliases before `mkdtemp`, bind one `file:` URL, and invoke canonical `migrateDatabase()`. Track the canonical root/database identity and delete only that owned root after clients close. Borrow guard and ownership patterns from the renderer fixture, but do not import its authority flag, copy Phase 19 process/signal/descriptor orchestration, start a server, or add broad negative-case infrastructure. Seed helpers may insert only taxonomy-prefixed synthetic rows requested by focused tests.
  </action>
  <verify><automated>pnpm exec tsx --test --test-name-pattern "taxonomy fixture safety" tests/taxonomy/taxonomy-ledger.test.ts</automated></verify>
  <done>The reusable fixture is local, owned, deterministic and incapable of falling through to real/remote state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Freeze all 109 decisions and reconcile net identity actions</name>
  <files>data/taxonomy/v1.2-phase21.json, src/lib/taxonomy/identity-plan.ts, tests/taxonomy/taxonomy-ledger.test.ts</files>
  <behavior>
    - Matrix -> 109 unique rows, exact priority totals 13/54/40/2 and status totals A1/BM42/BM-A1/G-M1/M55/S5/S-M4.
    - Primary actions -> exactly one of six per row and six counts sum to 109; auxiliary redirect/tag/evidence steps never increment the denominator.
    - Official coverage addendum -> isolated `outOfMatrixActions`; matrix checksum/count unchanged.
    - Stable ID sets -> create/split-output/merge-donor/split-source/retire effects reconcile exactly with after-before brand, pen and page sets.
    - Alias/rename/variant/gated/defer -> zero applied canonical delta unless an explicit atomic create/retire effect says otherwise.
    - Duplicate row, unknown ID/action, checksum drift, ambiguous split destination or missing evidence reference -> fail closed.
    - Locked four-case subset -> `executionState=apply`, exact eight output slugs/IDs and donor actions match the lock; any `gated|defer`, alternate slug/ID, fuzzy survivor or route substitution fails decoding.
    - Payload assignment -> every old story/spec/claim/citation/source/media/tag/relation/reference item ID occurs exactly once as one supported output ID, `retired_source`, or `pending_conflict`; zero unassigned and zero multiply assigned items.
    - Old routes -> Waterman conditional Hémisphère redirect, Opus/Leonardo no redirect and unconditional 404, Aurora conditional public-brand redirect; all alternatives fail the manifest contract.
  </behavior>
  <action>
Transcribe the seven geographic tables—not the repeated P0 summary—into `data/taxonomy/v1.2-phase21.json`. Preserve original title, region, priority and status; add reviewed exact IDs/slugs only when supported by the Phase 19 ledger/identity research, otherwise use explicit `gated|defer`, blocker and no redirect. Include all P0/P1/P2/P3 rows and place the eight-brand official delta in `outOfMatrixActions`. Encode the locked Pilot MR/Metropolitan/Cocoon, Elabo/Falcon, Moonman/Majohn A1, Wing Sung/JunLai, PGS, Asvine and SKB cases without inventing an unlocked slug.

For the four cases governed by `21-SPLIT-IDENTITY-LOCK.md`, defer/gate is forbidden and the manifest must carry the exact interface table IDs/slugs plus these actions: `gwKClNnwt3V3 split_retain_as waterman-hemisphere`; create `4dcEbeUCjxH-` as Charleston; retire `dTCUDu03vrI6` with `retire_mixed` and create the two Opus outputs; retire `s0HAxT1gsHxh` with `retire_mixed` and create the two Leonardo outputs; retire `G9ptvLpfyzNQ` with `retire_generic` and create the two Aurora outputs with `create_from_research`. Lock old-route policies exactly: `/pen/威迪文-waterman-查尔斯顿-hemisphere` conditionally redirects only to public `/pen/waterman-hemisphere`; `/pen/opus-88-demo-kolora` and `/pen/leonardo-furore-momento-magico` always 404 with no redirect record; `/pen/奥罗拉-aurora` conditionally redirects only to public `/brand/aurora` and never to a model.

Inventory every donor payload row ID by surface and encode a single destination: one evidence-supported output entity/payload ID, `retired_source`, or `pending_conflict`. A shared background item may be reconstructed for multiple outputs only as separately evidenced rows with deterministic new payload IDs; it may not reuse or copy the donor ID. The decoder rejects missing, duplicate or wholesale donor assignment and proves no story/spec/claim/citation/source/media/tag/relation/reference/review row is silently copied across children.

Implement an unknown-input decoder and canonical JSON checksum in `identity-plan.ts`; no name/slug fuzzy merge is permitted. Reconcile distinct stable entity sets, report row counts separately from atomic-action counts and actual/applied net deltas, and prove `net = |after|-|before|` for brand, pen and total. Make serialization/order byte-stable. Tests must compare all 109 stable keys to a parser-owned expected key list derived from the checked-in source headings so duplicate or missing transcription cannot pass on count alone.
  </action>
  <verify><automated>pnpm exec tsx --test --test-name-pattern "109-row manifest|net action reconciliation|checksum" tests/taxonomy/taxonomy-ledger.test.ts &amp;&amp; pnpm exec tsc --noEmit</automated></verify>
  <done>The immutable denominator, exact row identities, six-class actions, deferred/gated states and set-derived net deltas are deterministic and reviewable.</done>
</task>

</tasks>

<prohibitions>
- Do not use `entityIdentityKey`, text similarity, random ID generation or search results to auto-select a survivor/child/slug.
- Do not defer, gate or reinterpret any action/ID/slug/route in `21-SPLIT-IDENTITY-LOCK.md`; it overrides earlier research/UI suggestions for these four cases.
- Do not mutate any database outside the fixture, invoke a Phase 19 wrapper, or claim the manifest has migrated the real 305 rows.
- Do not treat 109, sum of atomic actions, or out-of-matrix rows as net new pages.
- Do not create public content, copy old reviews, or add a package.
</prohibitions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| research tables -> checked-in manifest | Human-reviewed source rows become executable identity decisions. |
| environment/path -> disposable fixture | Test authority must never select protected or remote data. |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-21-05 | Tampering | manifest/checksum | high | mitigate | Strict schema, stable key parity, canonical checksum and exact totals. |
| T-21-06 | Spoofing | fuzzy identity resolution | high | mitigate | Exact immutable IDs/slugs and explicit gated/deferred state; no heuristic matching. |
| T-21-07 | Tampering | fixture target | high | mitigate | Guard remote/protected/alias paths before resource allocation and cleanup only owned roots. |
| T-21-08 | Repudiation | net quantity report | medium | mitigate | Preserve before/after sets and independently reconcile every action category. |
</threat_model>

<verification>
- `pnpm exec tsx --test tests/taxonomy/taxonomy-ledger.test.ts`
- `pnpm exec tsc --noEmit`
- `pnpm biome check src/lib/taxonomy/identity-plan.ts scripts/lib/taxonomy-fixture.ts tests/taxonomy/taxonomy-ledger.test.ts`
- Confirm the JSON contains 109 matrix rows and separately keyed out-of-matrix actions through the tested decoder, not a line-count grep.
</verification>

<success_criteria>
- All 109 matrix items are present once with exact priority/status totals and explicit non-guessed decisions.
- Six primary-action counts sum to 109 while actual/applied/deferred and variant outcomes remain separately visible.
- Set-derived net canonical changes reconcile exactly and the fixture cannot access protected/remote state.
</success_criteria>

<output>Create `.planning/phases/21-taxonomy/21-02-SUMMARY.md` when done.</output>
