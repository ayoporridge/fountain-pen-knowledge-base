---
phase: 21-taxonomy
plan: "06"
type: execute
wave: 6
depends_on: [21-05]
files_modified:
  - scripts/check-taxonomy-ui.ts
  - src/lib/entity-redirects.ts
  - src/lib/entity-page.ts
  - src/components/library/EncyclopediaShell.tsx
  - src/components/library/ModelArchive.tsx
  - src/app/[type]/[slug]/page.tsx
  - next.config.ts
  - tests/taxonomy/taxonomy-ui-contract.test.ts
  - tests/e2e/taxonomy.spec.ts
  - .planning/phases/21-taxonomy/21-VERIFICATION-EVIDENCE.md
autonomous: true
requirements: [TAX-01, TAX-02, TAX-03, TAX-04, TAX-05, TAX-06, EXP-01, EXP-05]
must_haves:
  truths:
    - "The runtime composes the audited checked-in redirect artifact with existing legacy/article redirects and never queries a database/filesystem at Edge runtime."
    - "Canonical H1, metadata, JSON-LD, aliases, product hierarchy and brand/model relations agree in initial server HTML without new route types or empty states."
    - "Browser fixtures inject redirects only through owned temporary JSON and a unique owned build directory; they never write, copy or restore the checked-in generated redirect artifact."
    - "After the single focused browser attempt, no child/sentinel/build/database/temp residue remains, the generated artifact is byte-identical and its scoped git diff is clean."
    - "Phase 20-04's fixture media gap remains a non-blocking Phase 21 exception and a production-release blocker; this plan does not claim PAGE-08 or production closure."
  artifacts:
    - path: "src/lib/entity-redirects.ts"
      provides: "Synchronous checked-in production map plus fixture-only build injection seam"
    - path: "src/lib/entity-page.ts"
      provides: "Approved identity lines and hierarchy in the publication-gated page model"
    - path: "scripts/check-taxonomy-ui.ts"
      provides: "Owned temporary build/database/server/browser controller"
    - path: ".planning/phases/21-taxonomy/21-VERIFICATION-EVIDENCE.md"
      provides: "Focused commands, clean-fixture proof, audit hashes and release-debt boundary"
  key_links:
    - from: "src/lib/entity-redirects.ts"
      to: "src/generated/entity-redirects.ts"
      via: "read-only synchronous production import composed with existing article redirects"
      pattern: "GENERATED_ENTITY_REDIRECTS"
    - from: "src/lib/entity-page.ts"
      to: "src/components/library/EncyclopediaShell.tsx"
      via: "one current-public server view model containing approved aliases/hierarchy"
      pattern: "identity"
    - from: "scripts/check-taxonomy-ui.ts"
      to: "next.config.ts"
      via: "fixture-only validated redirect JSON and unique owned distDir environment"
      pattern: "TAXONOMY_FIXTURE_REDIRECTS_JSON|TAXONOMY_FIXTURE_DIST_DIR"
---

<objective>
Consume the audited redirect artifact through the existing encyclopedia route, render the approved canonical identity contract, and prove exact redirect/404/UI behavior in one isolated desktop/mobile browser run.

Purpose: Separate public rendering and synthetic browser evidence from the protected-source audit and permanent generated artifact write.
Output: Runtime redirect composition, identity DTO/UI, temporary fixture runner, focused E2E and durable verification evidence.
</objective>

<execution_context>
@/Users/xz/.codex/gsd-core/workflows/execute-plan.md
@/Users/xz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/21-taxonomy/21-SPLIT-IDENTITY-LOCK.md
@.planning/phases/21-taxonomy/21-UI-SPEC.md
@.planning/phases/21-taxonomy/21-05-SUMMARY.md
@.planning/phases/20-renderer/20-04-SUMMARY.md
@src/generated/entity-redirects.ts
@src/lib/entity-redirects.ts
@src/lib/entity-page.ts
@src/components/library/EncyclopediaShell.tsx
@src/components/library/ModelArchive.tsx
@src/app/[type]/[slug]/page.tsx

<interfaces>
`src/generated/entity-redirects.ts` is a read-only Plan 21-05 artifact. This plan records its bytes/hash before work and may import/compare it, but no command or `finally` branch may write, copy, regenerate or restore it.

`getCanonicalEntityPath(type, slug)` remains synchronous and executes before metadata/page rendering. Production composes the checked-in map with existing legacy/article redirects. Only when `TAXONOMY_FIXTURE=1` may a validated build-injected `TAXONOMY_FIXTURE_REDIRECTS_JSON` add synthetic routes in memory; runtime/Edge performs no filesystem or database lookup.

`PublishedPageData` remains the only brand/pen renderer input. Extend it with approved identity rows and hierarchy; components stay pure props renderers with no SQL/client identity fetch. `21-UI-SPEC.md` is approved, while `21-SPLIT-IDENTITY-LOCK.md` overrides its earlier Opus/Leonardo fallback suggestions.
</interfaces>
</context>

<execution_preflight>
Read `20-04-SUMMARY.md` and preserve `gaps_found`. Record the media fixture defect as `phase21Blocking=false` and `productionReleaseBlocking=true`; constrained primary-row rewrite, four renderer screenshots, boundary run and human hero approval remain required before final production acceptance. Do not edit or rerun Phase 20-04.
</execution_preflight>

<execution_timeboxes>
- Task 1 uses only focused DTO/renderer selectors; each individual selector/type/style command is bounded to 60 seconds and never opens the protected source.
- Task 2 gets exactly one browser acceptance attempt: one fixture build, one server, one worker and desktop/mobile viewports serially, hard cap 10 minutes. On timeout/failure, persist the exact stage, execute owned cleanup and stop—no retry, broad suite or Phase 20 expansion.
</execution_timeboxes>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Compose redirects and render the approved canonical identity contract</name>
  <files>src/lib/entity-redirects.ts, src/lib/entity-page.ts, src/components/library/EncyclopediaShell.tsx, src/components/library/ModelArchive.tsx, src/app/[type]/[slug]/page.tsx, tests/taxonomy/taxonomy-ui-contract.test.ts</files>
  <behavior>
    - Production redirect lookup -> read-only checked-in map plus existing legacy/article maps; no environment injection unless the explicit fixture flag is present.
    - Locked routes -> Waterman one hop only after Hémisphère public; Opus 88/Leonardo always 404; Aurora old pen path only to public `/brand/aurora`, never either model.
    - Canonical page -> H1/breadcrumb/title/canonical/OG/JSON-LD name agree; approved aliases appear only in identity rows and `alternateName`.
    - Model -> one canonical brand; brand -> all unique public canonical models/count; aliases/editions/variants/draft/retired do not inflate count.
    - Hierarchy -> family/model/edition/variant renders in existing Version Differences area with stable fragments; no new route/public family page.
    - Empty/unapproved identity row -> omitted entirely; no internal taxonomy/review vocabulary reaches HTML.
  </behavior>
  <action>
Compose the Plan 21-05 generated module with existing legacy/article maps in `entity-redirects.ts`, retaining redirect-before-metadata and synchronous Edge behavior. Add the fixture-only in-memory seam: when and only when `TAXONOMY_FIXTURE=1`, parse build-injected JSON and apply the same normalized-source, same-site destination, collision, loop and one-hop validation. Production rejects/ignores fixture variables; neither mode writes the generated module.

Extend the existing single-snapshot page loader with reviewed regional/historic aliases, qualified producer/licensed-name relations and hierarchy. Update `EntityHeader`, `ModelArchive`, metadata and JSON-LD exactly per the approved UI spec: canonical H1 only; optional `地区名称`/`亦称`/`曾用名`/`生产方`; one canonical brand; nonlinked hierarchy labels; aliases in `alternateName`; no new cards/routes/search/LLM. Preserve Phase 20 story/media/source behavior. Focused contracts cover exact locked cases and unique old hero/source/citation/tag/relation sentinels.
  </action>
  <verify><automated>pnpm exec tsx --test --test-name-pattern "runtime redirects|canonical identity DTO|taxonomy server HTML" tests/taxonomy/taxonomy-ui-contract.test.ts &amp;&amp; pnpm exec tsc --noEmit</automated></verify>
  <done>Every public identity surface and exact old-route decision comes from one current-public server snapshot and the immutable audited redirect artifact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Run one owned temporary desktop/mobile taxonomy acceptance</name>
  <files>scripts/check-taxonomy-ui.ts, next.config.ts, tests/e2e/taxonomy.spec.ts, .planning/phases/21-taxonomy/21-VERIFICATION-EVIDENCE.md</files>
  <behavior>
    - Pilot aliases, Elabo/Falcon, PGS/SHIKIORI and one lawful merge -> URL/H1/breadcrumb/canonical/JSON-LD/counts match on desktop/mobile.
    - Waterman/Opus 88/Leonardo/Aurora -> exact locked redirect/404 behavior and distinct outputs with no donor sentinel leakage.
    - JunLai/SKB unresolved -> detail, metadata, browse, sitemap, graph, recommendations and brand models absent/404.
    - Long multilingual alias/hierarchy -> initial server HTML, keyboard/focus/touch and `scrollWidth <= clientWidth` pass.
    - Fixture sentinel -> works only in the temporary build; after finally it is absent from surviving workspace files, no child remains, and generated redirect bytes/hash/diff are unchanged.
  </behavior>
  <action>
Create the dedicated `check-taxonomy-ui.ts` runner; do not add browser behavior to the Plan 21-05 audit checker. Before fixture allocation, read/hash `src/generated/entity-redirects.ts` and capture its scoped diff. Create one registered `mkdtemp` root for the v3 fixture DB and redirect JSON, lawfully fresh-review/publish only explicit positive synthetic targets, and generate a random per-run sentinel only in that temp JSON/environment. Redact it from durable evidence.

Pass `TAXONOMY_FIXTURE=1` and the JSON value to both build/server. Let `next.config.ts` accept `TAXONOMY_FIXTURE_DIST_DIR` only when it matches a unique runner-owned relative `.next-taxonomy-<nonce>` name; reject default `.next`, absolute paths and traversal. Build once, start one server, use one Playwright worker and execute desktop/mobile serially. Assert initial response HTML before browser assertions, four exact lock outcomes, identity text/forbidden sentinels, exact brand/model sets, fragments, accessibility and overflow.

In one `finally`, stop the entire process tree, close DB clients, delete only the registered temp root and owned dist directory, then assert no live child, sentinel or owned residue remains; checked-in generated bytes/hash equal the pre-run value; and `git diff --exit-code -- src/generated/entity-redirects.ts` matches the pre-run scoped diff. Never write/copy/regenerate/restore that file, even on failure. Record exact commands/results, Plan 21-05 artifact hashes/counts, cleanup proof, no-remote/no-production scope and Phase 20 debt in `21-VERIFICATION-EVIDENCE.md`.
  </action>
  <verify><automated>pnpm exec tsx scripts/check-taxonomy-ui.ts --fixture-browser --viewports=desktop,mobile &amp;&amp; git diff --exit-code -- src/generated/entity-redirects.ts &amp;&amp; pnpm exec tsc --noEmit &amp;&amp; pnpm biome check next.config.ts scripts/check-taxonomy-ui.ts src/lib/entity-redirects.ts src/lib/entity-page.ts src/components/library/EncyclopediaShell.tsx src/components/library/ModelArchive.tsx 'src/app/[type]/[slug]/page.tsx' tests/taxonomy/taxonomy-ui-contract.test.ts tests/e2e/taxonomy.spec.ts</automated></verify>
  <done>One isolated browser run proves the approved public contract without modifying the permanent redirect artifact or overstating production/media completion.</done>
</task>

</tasks>

<prohibitions>
- Do not open/copy the protected catalog, write Turso, deploy Vercel, push or claim fixture evidence as production.
- Do not edit `src/generated/entity-redirects.ts` in this plan for any reason; fixture mode may only write its registered temp root and owned dist directory.
- Do not add runtime DB/filesystem lookup, redirect chains/choice pages, public family/variant routes, search or LLM behavior.
- Do not repair/rerun Phase 20-04 or claim PAGE-08/real-305 media approval complete.
</prohibitions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| checked-in audited map -> runtime | Permanent redirects are read-only input; fixture injection is explicit, isolated and build-scoped. |
| publication rows -> server HTML/browser | Internal taxonomy state becomes visible only through current `public_entities` and reviewed fields. |
| runner-owned temp state -> workspace | Synthetic DB/map/build/server state must be owned, bounded and absent after finally. |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-21-19 | Spoofing | canonical metadata/UI | high | mitigate | One server snapshot drives H1, breadcrumb, metadata, JSON-LD, aliases and relations. |
| T-21-21 | Information Disclosure | unresolved identity | high | mitigate | 404/exclusion across every public surface; no blocker/status copy. |
| T-21-22 | Tampering | fixture redirect/build seam | high | mitigate | Separate runner, explicit flag, validated JSON, owned dist, pre/post artifact hash/diff and finally cleanup. |
</threat_model>

<source_audit>
| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Stable canonical identities without duplicate/misleading regional, derived, mixed or disputed pages | 21-01..21-06 | COVERED | Substrate, manifest, mutation, topology, audit and public proof |
| REQ | TAX-01 | Canonical aliases/regions | 21-02, 21-03, 21-06 | COVERED | Exact data and redirect/HTML proof |
| REQ | TAX-02 | Mixed-page splits and evidence migration | 21-02, 21-04, 21-05, 21-06 | COVERED | Assignment audit and exact old-route proof |
| REQ | TAX-03 | PGS family/edition/variant | 21-02, 21-04, 21-06 | COVERED | Hierarchy without duplicate pages |
| REQ | TAX-04 | Aurora placeholder replacement | 21-02, 21-04, 21-05, 21-06 | COVERED | Exact mandatory drafts and brand-only route correction |
| REQ | TAX-05 | Disputed identities fail closed | 21-03, 21-04, 21-06 | COVERED | No arbitrary maker/public surface |
| REQ | TAX-06 | URL/relation/tag/source/media/reference integrity | 21-01, 21-03, 21-04, 21-05, 21-06 | COVERED | Full closure audit and browser proof |
| REQ | EXP-01 | 109-row six-action/net reconciliation | 21-02, 21-05 | COVERED | Denominator/addendum/set delta separated |
| REQ | EXP-05 | Variant versus model boundary | 21-01, 21-02, 21-04, 21-06 | COVERED | Hierarchy and brand-count proof |
| LOCK | SPLIT-IDENTITY | Exact Waterman/Opus 88/Leonardo/Aurora identities/routes/payloads | 21-02, 21-04, 21-05, 21-06 | COVERED | No defer or fallback heuristic |
| UI | 21-UI-SPEC | Approved identity, hierarchy, metadata and no-new-surface contract | 21-06 | COVERED | `status: approved` |
| CONTEXT | — | No Phase 21 CONTEXT.md; parent execution constraints | all | COVERED | No deferred CONTEXT decisions |
</source_audit>

<verification>
- `pnpm exec tsx --test tests/taxonomy/*.test.ts`
- `pnpm exec tsx scripts/check-taxonomy-contract.ts --check-redirects`
- `pnpm exec tsx scripts/check-taxonomy-ui.ts --fixture-browser --viewports=desktop,mobile`
- `git diff --exit-code -- src/generated/entity-redirects.ts`
- `pnpm exec tsc --noEmit`
- Exact changed-file Biome command from Task 2.
</verification>

<success_criteria>
- TAX-01..06 and EXP-01/05 have full-set, data-contract and public-surface evidence across six serial plans.
- Production redirects consume the immutable Plan 21-05 artifact; synthetic redirects exist only inside the owned fixture build.
- Cleanup leaves no sentinel/process/temp/build residue and no generated-artifact mutation.
- Phase 20-04 remains a production-release prerequisite without blocking Phase 21 taxonomy completion.
</success_criteria>

<output>Create `.planning/phases/21-taxonomy/21-06-SUMMARY.md` when done.</output>
