---
phase: 21-taxonomy
plan: "05"
type: execute
wave: 5
depends_on: [21-04]
files_modified:
  - src/lib/audit/taxonomy-audit.ts
  - scripts/check-taxonomy-contract.ts
  - src/generated/entity-redirects.ts
  - src/lib/entity-redirects.ts
  - src/lib/entity-page.ts
  - src/components/library/EncyclopediaShell.tsx
  - src/components/library/ModelArchive.tsx
  - src/app/[type]/[slug]/page.tsx
  - next.config.ts
  - tests/taxonomy/taxonomy-contract.test.ts
  - tests/e2e/taxonomy.spec.ts
  - .planning/phases/21-taxonomy/artifacts/taxonomy-operations.ndjson
  - .planning/phases/21-taxonomy/artifacts/taxonomy-coverage.csv
  - .planning/phases/21-taxonomy/artifacts/taxonomy-summary.json
  - .planning/phases/21-taxonomy/21-VERIFICATION-EVIDENCE.md
autonomous: true
requirements: [TAX-01, TAX-02, TAX-03, TAX-04, TAX-05, TAX-06, EXP-01, EXP-05]
must_haves:
  truths:
    - "A checkpointed caller-owned copy—not the protected catalog—is migrated, receives the manifest twice, and yields deterministic full-set artifacts."
    - "All 109 rows, out-of-matrix actions, before/after canonical sets, six action counts, actual net deltas and publication dispositions reconcile exactly."
    - "Old routes are either one permanent hop to a currently public canonical target/fallback or 404; no draft target, loop, chain or alias page returns 200."
    - "Canonical H1, metadata, JSON-LD, aliases, product hierarchy and brand/model relations agree in initial server HTML without new route types or empty states."
    - "Browser fixtures inject redirects only through an owned temporary build seam; the checked-in redirect artifact is byte-identical afterward, no runtime sentinel remains, and the fixture-owned build/database/temp roots are removed in finally."
    - "Phase 20-04's fixture media gap remains an explicit non-blocking Phase 21 exception and an unresolved production-release blocker; Phase 21 does not claim PAGE-08 or production closure."
  artifacts:
    - path: "src/lib/audit/taxonomy-audit.ts"
      provides: "Full-set identity, reference, relation, lifecycle and net-action audit"
      exports: ["runTaxonomyAudit", "taxonomyAuditVerdict", "serializeTaxonomyOperations", "serializeTaxonomyCoverage", "serializeTaxonomySummary"]
    - path: "scripts/check-taxonomy-contract.ts"
      provides: "Owned-copy full gate and focused fixture/browser controller"
    - path: "src/generated/entity-redirects.ts"
      provides: "Deterministic public-target-only runtime redirect map"
    - path: "src/lib/entity-page.ts"
      provides: "Approved identity lines and hierarchy in the publication-gated page model"
    - path: ".planning/phases/21-taxonomy/artifacts/taxonomy-summary.json"
      provides: "Deterministic 109-row reconciliation and release-debt summary"
  key_links:
    - from: "scripts/check-taxonomy-contract.ts"
      to: "src/lib/audit/read-only-catalog.ts"
      via: "copyCheckpointedCatalogToDisposableCopy before any SQLite open/migration"
      pattern: "copyCheckpointedCatalogToDisposableCopy"
    - from: "src/lib/entity-redirects.ts"
      to: "src/generated/entity-redirects.ts"
      via: "synchronous Edge-compatible generated map composed with existing article redirects"
      pattern: "GENERATED_ENTITY_REDIRECTS"
    - from: "src/lib/entity-page.ts"
      to: "src/components/library/EncyclopediaShell.tsx"
      via: "one current-public server view model containing approved aliases/hierarchy"
      pattern: "identity"
    - from: "scripts/check-taxonomy-contract.ts"
      to: "next.config.ts"
      via: "fixture-only validated redirect JSON and unique owned distDir environment"
      pattern: "TAXONOMY_FIXTURE_REDIRECTS_JSON|TAXONOMY_FIXTURE_DIST_DIR"
    - from: "scripts/check-taxonomy-contract.ts"
      to: "src/generated/entity-redirects.ts"
      via: "pre/post byte hash plus scoped clean-diff assertion; fixture never writes source"
      pattern: "check-redirects|write-generated"
---

<objective>
Prove the complete taxonomy outcome on a protected-source-owned copy, generate only safe runtime redirects, and project the canonical identity contract through the existing encyclopedia page on desktop and mobile.

Purpose: Phase 21 is complete only when the whole matrix and every old identity surface are auditable—not when a few fixture rows migrate.
Output: Full audit/report artifacts, public-target redirect generator, server UI contract, focused E2E and durable evidence.
</objective>

<execution_context>
@/Users/xz/.codex/gsd-core/workflows/execute-plan.md
@/Users/xz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/21-taxonomy/21-RESEARCH.md
@.planning/phases/21-taxonomy/21-PATTERNS.md
@.planning/phases/21-taxonomy/21-SPLIT-IDENTITY-LOCK.md
@.planning/phases/21-taxonomy/21-UI-SPEC.md
@.planning/phases/21-taxonomy/21-04-SUMMARY.md
@.planning/phases/20-renderer/20-04-SUMMARY.md
@src/lib/audit/read-only-catalog.ts
@src/lib/audit/readiness-audit.ts
@src/lib/entity-redirects.ts
@src/lib/entity-page.ts
@src/components/library/EncyclopediaShell.tsx
@src/components/library/ModelArchive.tsx
@src/app/[type]/[slug]/page.tsx

<interfaces>
Use only `copyCheckpointedCatalogToDisposableCopy()` for the real 305 source boundary. Snapshot/fingerprint main/WAL/SHM before and after; never call `openReadOnlyCatalog()` or otherwise SQLite-open the protected source. Migration, taxonomy writes, audit and browser server all use the returned owned copy.

`getCanonicalEntityPath(type, slug)` is synchronous and consumed before metadata/page rendering. The checked-in generated map may contain only audited redirects whose destination is currently public. `21-SPLIT-IDENTITY-LOCK.md` fixes the exceptions: Waterman redirects to public Hémisphère; Opus 88 and Leonardo never redirect; Aurora redirects only to public `/brand/aurora`. No all-children brand fallback heuristic remains.

`PublishedPageData` remains the only brand/pen renderer input. Extend it with approved identity rows and hierarchy; components stay pure props renderers with no SQL/client identity fetch.

`21-UI-SPEC.md` is approved (`status: approved`, 2026-07-19) and is the exact UI acceptance contract; there is no pending UI checker decision.

Production redirect generation and fixture redirects use separate seams. Only an explicit audited `--write-generated` operation may atomically update checked-in `src/generated/entity-redirects.ts`. `--fixture-browser` instead emits a map under its caller-owned temp root and injects validated JSON at build time only when `TAXONOMY_FIXTURE=1`; runtime/Edge performs no filesystem or database lookup.
</interfaces>
</context>

<execution_preflight>
Read `20-04-SUMMARY.md` and preserve its `gaps_found` status. The broad path-rewrite defect is isolated to the Phase 20 synthetic model-media fixture and does not block this content/taxonomy track. Do not edit or rerun Phase 20-04 here. Record it as `phase21Blocking=false` and `productionReleaseBlocking=true`, with the exact recovery requirement: constrained primary-row rewrite, four renderer screenshots, boundary run and human hero approval must be completed before final production acceptance.
</execution_preflight>

<execution_timeboxes>
Tasks are serial and file ownership is strict:

| Stage | Owner and allowed files | Bounded execution |
|---|---|---|
| Task 1 | Owns `src/lib/audit/taxonomy-audit.ts`, `scripts/check-taxonomy-contract.ts`, the shared contract-test harness and artifact writers. Later tasks do not edit the checker/audit controller. | Run focused fixture tests while iterating, then at most one protected-source checkpoint-copy/audit invocation. If that invocation exceeds 8 minutes, fingerprints/guards fail, or ownership is uncertain: stop, clean, record the exact blocker; do not retry automatically. |
| Task 2 | Owns `src/generated/entity-redirects.ts`, `src/lib/entity-redirects.ts`, `src/lib/entity-page.ts`; it may only add/adjust the named redirect/DTO assertion blocks in the shared contract test. | No protected-source copy/full audit. Run only redirect/DTO selectors, explicit generation `--check`/`--write-generated`, typecheck; each command is bounded to 60 seconds. |
| Task 3 | Owns `next.config.ts`, `EncyclopediaShell.tsx`, `ModelArchive.tsx`, `[type]/[slug]/page.tsx`, the focused E2E spec and evidence file. | After focused checks, make exactly one browser acceptance attempt: one fixture build, one server, one worker, both viewports serially, hard cap 10 minutes. On failure persist the exact gap and clean in `finally`; do not retry, broaden suites or reopen Phase 20. |
</execution_timeboxes>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Audit the full manifest on a checkpointed owned copy and emit deterministic artifacts</name>
  <files>src/lib/audit/taxonomy-audit.ts, scripts/check-taxonomy-contract.ts, tests/taxonomy/taxonomy-contract.test.ts, .planning/phases/21-taxonomy/artifacts/*</files>
  <behavior>
    - Fixture/full owned copy -> exact manifest/action/before-after/publication/reference audit with deterministic sort and bytes.
    - Real source -> main/WAL/SHM fingerprints unchanged before/after; all SQLite work occurs only on one registered owned copy.
    - First apply -> exact declared changes; second same-checksum apply -> deterministic no-op; mismatch -> fail closed.
    - Audit -> exact 109 unique rows, six primary-action counts sum 109, out-of-matrix isolated, set-derived brand/pen/page deltas reconcile.
    - Integrity -> quick_check ok, foreign_key_check empty, semantic orphan/redirect/alias/reverse/made_by/publication violations zero.
    - Console `--limit` -> display-only; canonical NDJSON/CSV/summary bytes, hash, verdict and exit status unchanged.
    - Redirect output modes -> audited `--write-generated` is the sole checked-in writer; fixture mode accepts only an owned temp destination and never aliases or overwrites `src/generated/entity-redirects.ts`.
  </behavior>
  <action>
Build `taxonomy-audit.ts` over the full decoded manifest and raw inventory, not the locked Phase 19 305 count after identity changes. Emit every operation and row, before/after raw and active canonical sets, applied/deferred/gated partitions, six action counts, out-of-matrix counts, brand/pen/page set deltas, alias/redirect/lineage status, publication status and blockers. Audit every reference surface from Plan 21-04, one-maker/reverse-model exact sets, old review non-inheritance, and zero unexpected public outputs. Reuse Phase 19 stable NDJSON/CSV/formula-protection/atomic-write patterns, not its baseline assertions.

Use `check-taxonomy-contract.ts` as the single thin controller for an already-owned fixture DB, the protected-source boundary, redirect generation/checking, and the later `--fixture-browser` subprocess lifecycle. The checker snapshots the source filesystem, uses `copyCheckpointedCatalogToDisposableCopy`, runs canonical migrations and the Phase 21 apply library only on the copy, replays once, audits, atomically writes the three artifacts, closes/cleans owned resources, then proves source fingerprints unchanged. Its only checked-in redirect write path is explicit `--write-generated` after a green complete audit; `--check-redirects` is read-only. Fixture redirect emission requires an already registered caller-owned temp destination, rejects the checked-in artifact path/inode and returns validated JSON for build-time injection rather than editing source.

The browser controller may point at the focused spec path before that spec is created, but must allocate/track a unique temp root, fixture DB, redirect JSON, build directory, server and every child for deterministic cleanup now. It rejects nonempty WAL/journals, aliases and unsafe destinations before SQLite access. Do not add another package script/runner, invoke Phase 19 wrappers, broad E2E, remote services or deployment.
  </action>
  <verify><automated>pnpm exec tsx --test --test-name-pattern "full taxonomy audit|artifact determinism|protected source" tests/taxonomy/taxonomy-contract.test.ts &amp;&amp; pnpm exec tsx scripts/check-taxonomy-contract.ts --source=data/fpkg.db --owned-copy --artifacts=.planning/phases/21-taxonomy/artifacts</automated></verify>
  <done>The full 109-row taxonomy result is byte-stable, set-reconciled and proven only on an owned copy whose protected source is unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Generate public-only redirects and render canonical identity in the existing encyclopedia shell</name>
  <files>src/generated/entity-redirects.ts, src/lib/entity-redirects.ts, src/lib/entity-page.ts, tests/taxonomy/taxonomy-contract.test.ts</files>
  <behavior>
    - Published rename/merge/alias -> old path one 308 hop to canonical path; draft/unresolved target -> old and new paths both 404.
    - Edition/color old route -> canonical model plus stable fragment only after model public; no heuristic ambiguous-split fallback exists.
    - Locked routes -> Waterman old mixed path redirects one hop only after Hémisphère is public; Opus 88 and Leonardo old mixed paths are always 404 with no generated entry; Aurora old pen path redirects only to public `/brand/aurora`, never either model.
    - Canonical page -> H1/breadcrumb/title/canonical/OG/JSON-LD name agree; approved alias only in identity rows and `alternateName`.
    - Model -> one canonical brand; brand -> all unique public canonical models/count; aliases/editions/variants/draft/retired do not inflate count.
    - Hierarchy -> family/model/edition/variant renders in existing Version Differences area with stable fragments; no new route or public family page.
    - Empty/unapproved identity row -> entire row omitted; no internal taxonomy/review vocabulary reaches HTML.
  </behavior>
  <action>
Consume Task 1's deterministic redirect output and invoke its explicit `--write-generated` mode once to atomically emit checked-in `src/generated/entity-redirects.ts` from the audited ledger; all routine validation uses read-only `--check-redirects`. Validate normalized source paths, same-site `/brand|pen/` destinations/fragments, target existence/publication, and no loop/chain/collision. Candidate rows targeting draft/retired/unresolved entities stay in the DB/report but are omitted from runtime output. Enforce the split lock exactly: Waterman conditional Hémisphère entry; no Opus/Leonardo entry under any publication combination; Aurora conditional brand-only entry. Compose the checked-in map with existing legacy/article maps in `entity-redirects.ts`.

Add the fixture-only build seam in `entity-redirects.ts`: when and only when `TAXONOMY_FIXTURE=1`, parse a build-injected `TAXONOMY_FIXTURE_REDIRECTS_JSON`, validate the same path/public-target/loop/collision contract and compose it in memory. Production ignores/rejects fixture variables. Keep `getCanonicalEntityPath` synchronous and Edge-compatible—no runtime file/DB access—and preserve redirect-before-metadata through Task 3's route integration.

Extend the existing single-snapshot page loader with reviewed regional/historic aliases, qualified producer/licensed-name relations and hierarchy. Keep the DTO explicit and fail closed; rendering/metadata wiring is Task 3. Preserve the Phase 20 story/media/source behavior. Contract tests must cover every locked data case and unique old hero/source/citation/tag/relation sentinels.
  </action>
  <verify><automated>pnpm exec tsx --test --test-name-pattern "redirect generation|canonical identity DTO" tests/taxonomy/taxonomy-contract.test.ts &amp;&amp; pnpm exec tsx scripts/check-taxonomy-contract.ts --check-redirects &amp;&amp; pnpm exec tsc --noEmit</automated></verify>
  <done>Only public canonical destinations redirect, while every rendered identity surface agrees with the same reviewed server snapshot.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Run focused desktop/mobile redirect and taxonomy UI acceptance</name>
  <files>next.config.ts, src/components/library/EncyclopediaShell.tsx, src/components/library/ModelArchive.tsx, src/app/[type]/[slug]/page.tsx, tests/e2e/taxonomy.spec.ts, .planning/phases/21-taxonomy/21-VERIFICATION-EVIDENCE.md</files>
  <behavior>
    - Pilot aliases, Elabo/Falcon, PGS/SHIKIORI and one lawful merge -> final URL/H1/breadcrumb/canonical/JSON-LD and counts match on desktop/mobile.
    - Waterman/Opus 88/Leonardo/Aurora split fixtures -> declared redirect/404 behavior and distinct children with no old sentinels.
    - JunLai/SKB unresolved -> detail, metadata, browse, sitemap, graph, recommendations and brand models all absent/404.
    - Long multilingual alias/hierarchy -> initial server HTML present, keyboard/focus/touch usable and page `scrollWidth <= clientWidth`.
    - Fixture build seam -> a unique runtime sentinel redirect works in the fixture build, never appears in checked-in/generated source, and is absent after cleanup; checked-in redirect file hash/content and scoped git diff are unchanged.
    - Evidence -> commands, artifact hashes, exact-set counts, protected-source fingerprints and Phase 20 debt boundary recorded without production claim.
  </behavior>
  <action>
Update `EntityHeader`, `ModelArchive`, metadata and JSON-LD exactly per the approved `21-UI-SPEC.md`: canonical H1 only; optional `地区名称`/`亦称`/`曾用名`/`生产方`; one canonical brand; nonlinked hierarchy labels; approved aliases in `alternateName`; no new cards/routes/search/LLM. Add only a focused taxonomy Playwright spec; execute desktop and mobile viewports serially inside this spec so `playwright.config.ts` stays unchanged.

Give the Phase 21 `--fixture-browser` controller an owned build seam. Before fixture generation, read and hash `src/generated/entity-redirects.ts`. Allocate one unique registered temp root; place the fixture DB and redirect JSON there; generate a random per-run sentinel route/value only in that temp JSON/environment (redact it from durable evidence); set `TAXONOMY_FIXTURE=1` plus `TAXONOMY_FIXTURE_REDIRECTS_JSON` for both build and server; and set `TAXONOMY_FIXTURE_DIST_DIR` to a unique relative directory accepted by `next.config.ts` only under fixture mode. The config must reject absolute/traversal/default `.next` targets. Build once, start one server, use one worker, and run both viewports serially. Never write/copy/restore the checked-in generated artifact as part of this fixture.

In one `finally`, stop the server/process tree, close DB clients, remove only the registered temp root and owned dist directory, then assert: no live child remains and the runtime sentinel is absent from every surviving workspace file; checked-in artifact bytes/hash equal the pre-run value; and `git diff --exit-code -- src/generated/entity-redirects.ts` succeeds. A failure records the exact stage and exits after cleanup with no retry. Do not borrow the broken Phase 20 media rewrite, run broad suites or expand Phase 19 infrastructure. Assert server response HTML before browser assertions, the four exact locked old-route outcomes, exact identity text and forbidden donor sentinels, brand/model exact sets, stable fragments, accessibility and overflow.

Write `21-VERIFICATION-EVIDENCE.md` with exact focused commands/results, all artifact hashes/counts, source before/after fingerprints, no-remote/no-production scope, and the Phase 20-04 exception. State explicitly that Phase 21 may pass with that external debt open, but final production acceptance must remain blocked until Phase 20-04 recovery produces four screenshots, boundary evidence and human hero approval. Do not mark PAGE-08 or the real 305 content/media inventory complete.
  </action>
  <verify><automated>pnpm exec tsx scripts/check-taxonomy-contract.ts --fixture-browser --viewports=desktop,mobile &amp;&amp; git diff --exit-code -- src/generated/entity-redirects.ts &amp;&amp; pnpm exec tsc --noEmit &amp;&amp; pnpm biome check next.config.ts src/lib/audit/taxonomy-audit.ts scripts/check-taxonomy-contract.ts src/generated/entity-redirects.ts src/lib/entity-redirects.ts src/lib/entity-page.ts src/components/library/EncyclopediaShell.tsx src/components/library/ModelArchive.tsx 'src/app/[type]/[slug]/page.tsx' tests/taxonomy/taxonomy-contract.test.ts tests/e2e/taxonomy.spec.ts</automated></verify>
  <done>Focused real-browser and owned-copy evidence proves Phase 21 without overstating renderer, content, production or remote completion.</done>
</task>

</tasks>

<prohibitions>
- Do not SQLite-open or mutate the protected source; all real-305 work begins with checkpointed filesystem copy and continues only on the owned copy.
- Do not write Turso, deploy Vercel, push, or describe local/fixture evidence as production.
- Do not enable redirects to draft/unresolved targets, create redirect chains/choice pages, or add runtime DB lookup to Edge middleware.
- Do not let fixture/browser mode overwrite, restore or mutate checked-in `src/generated/entity-redirects.ts`; it may write only registered temp roots and its unique fixture `distDir`.
- Do not reuse old reviews, auto-republish affected entities, restore deprecated stories, or approve real media.
- Do not repair/rerun Phase 20-04 or claim its PAGE-08 gap closed in Phase 21.
</prohibitions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| protected catalog -> owned copy | Filesystem snapshot is trusted evidence but must never be SQLite-opened in place. |
| audit/ledger -> generated redirect code | Only validated public same-site destinations may enter Edge runtime. |
| publication rows -> server HTML/browser | Internal taxonomy state becomes user-visible identity only through public_entities and reviewed fields. |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-21-17 | Tampering | protected source/owned copy | high | mitigate | Checkpoint-copy helper, sidecar/link guards, before/after fingerprints and owned-only writes. |
| T-21-18 | Elevation of Privilege | redirect to draft/wrong split target | high | mitigate | Generator joins exact lock-declared destination to the current public set; no fuzzy child/brand fallback is permitted. |
| T-21-19 | Spoofing | canonical metadata/UI | high | mitigate | One server snapshot drives H1, breadcrumb, metadata, JSON-LD, aliases and relations. |
| T-21-20 | Repudiation | full-set/net result | medium | mitigate | Deterministic artifacts, hashes, exact sets and command evidence. |
| T-21-21 | Information Disclosure | unresolved identity | high | mitigate | 404 and exclusion across every public surface; no internal blocker/status copy. |
</threat_model>

<source_audit>
| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Stable canonical identities with no duplicate/misleading regional, derived, mixed or disputed pages | 21-01..21-05 | COVERED | Substrate, manifest, mutation, topology and public proof |
| REQ | TAX-01 | Canonical aliases/regions | 21-02, 21-03, 21-05 | COVERED | Exact researched cases, redirect/HTML proof |
| REQ | TAX-02 | Mixed-page splits and evidence migration | 21-02, 21-04, 21-05 | COVERED | Per-child assignments and old-route proof |
| REQ | TAX-03 | PGS family/edition/variant | 21-02, 21-04, 21-05 | COVERED | Hierarchy without duplicate pages |
| REQ | TAX-04 | Aurora placeholder replacement | 21-02, 21-04, 21-05 | COVERED | Generic retired atomically with exact mandatory 88/Optima drafts; no defer |
| REQ | TAX-05 | Disputed identities fail closed | 21-03, 21-04, 21-05 | COVERED | No arbitrary maker/public route |
| REQ | TAX-06 | URL/relation/tag/source/media/reference integrity | 21-01, 21-03, 21-04, 21-05 | COVERED | Full dependency and semantic-orphan audit |
| REQ | EXP-01 | 109-row six-action/net reconciliation | 21-02, 21-05 | COVERED | Denominator/addendum/set delta separated |
| REQ | EXP-05 | Variant versus model boundary | 21-01, 21-02, 21-04, 21-05 | COVERED | Hierarchy and brand-count proof |
| RESEARCH | — | Taxonomy ledger/alias/variant substrate and hash invalidation | 21-01 | COVERED | Two blockers first |
| RESEARCH | — | Exact 109 manifest and official addendum isolation | 21-02 | COVERED | No fuzzy decisions |
| RESEARCH | — | Demote-first bounded transaction, complete topology, owned-copy audit | 21-03..21-05 | COVERED | No auto republish/production write |
| LOCK | SPLIT-IDENTITY | Exact Waterman/Opus 88/Leonardo/Aurora IDs, slugs, actions, routes and payload single-assignment | 21-02, 21-04, 21-05 | COVERED | Locked document overrides earlier fallback suggestions; no defer |
| UI | 21-UI-SPEC | Approved canonical identity, hierarchy, metadata and no-new-surface contract | 21-05 | COVERED | `status: approved`; no pending UI checker decision |
| CONTEXT | — | No Phase 21 CONTEXT.md; parent execution constraints | all | COVERED | No deferred CONTEXT decisions exist |
</source_audit>

<verification>
- `pnpm exec tsx --test tests/taxonomy/*.test.ts`
- `pnpm exec tsx scripts/check-taxonomy-contract.ts --source=data/fpkg.db --owned-copy --artifacts=.planning/phases/21-taxonomy/artifacts`
- `pnpm exec tsx scripts/check-taxonomy-contract.ts --fixture-browser --viewports=desktop,mobile`
- `pnpm exec tsc --noEmit`
- Exact changed-file Biome command from Task 3.
</verification>

<success_criteria>
- TAX-01..06 and EXP-01/05 all have full-set, data-contract and public-surface evidence.
- The protected source is fingerprint-identical; production/remote state is untouched.
- All affected/new outputs remain draft or retired until fresh content/evidence/media/reviews later publish them.
- Fixture browser proof leaves no sentinel or owned build residue and cannot change the checked-in redirect artifact.
- Phase 20-04 remains open as a production-release prerequisite without blocking the Phase 21 content track.
</success_criteria>

<output>Create `.planning/phases/21-taxonomy/21-05-SUMMARY.md` when done.</output>
