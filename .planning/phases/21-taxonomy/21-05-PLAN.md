---
phase: 21-taxonomy
plan: "05"
type: execute
wave: 5
depends_on: [21-04]
files_modified:
  - src/lib/audit/taxonomy-audit.ts
  - scripts/check-taxonomy-contract.ts
  - src/generated/entity-redirects.ts
  - tests/taxonomy/taxonomy-contract.test.ts
  - .planning/phases/21-taxonomy/artifacts/taxonomy-operations.ndjson
  - .planning/phases/21-taxonomy/artifacts/taxonomy-coverage.csv
  - .planning/phases/21-taxonomy/artifacts/taxonomy-summary.json
autonomous: true
requirements: [TAX-01, TAX-02, TAX-03, TAX-04, TAX-05, TAX-06, EXP-01, EXP-05]
must_haves:
  truths:
    - "A checkpointed caller-owned copy—not the protected catalog—is migrated, receives the manifest twice, and yields deterministic full-set artifacts."
    - "All 109 rows, out-of-matrix actions, before/after canonical sets, six action counts, actual net deltas and publication dispositions reconcile exactly."
    - "The checked-in redirect module is generated only by one explicit green full-audit write; it contains only exact currently-public destinations and no draft, fallback heuristic, chain or loop."
    - "Plan 21-05 does not build a browser fixture, DTO or UI; synthetic redirect injection and browser proof are owned exclusively by Plan 21-06."
  artifacts:
    - path: "src/lib/audit/taxonomy-audit.ts"
      provides: "Full-set identity, reference, relation, lifecycle and net-action audit"
      exports: ["runTaxonomyAudit", "taxonomyAuditVerdict", "serializeTaxonomyOperations", "serializeTaxonomyCoverage", "serializeTaxonomySummary"]
    - path: "scripts/check-taxonomy-contract.ts"
      provides: "Owned-copy full gate and audited checked-in redirect generator"
    - path: "src/generated/entity-redirects.ts"
      provides: "Deterministic public-target-only runtime redirect map"
    - path: ".planning/phases/21-taxonomy/artifacts/taxonomy-summary.json"
      provides: "Deterministic 109-row reconciliation and release-debt summary"
  key_links:
    - from: "scripts/check-taxonomy-contract.ts"
      to: "src/lib/audit/read-only-catalog.ts"
      via: "copyCheckpointedCatalogToDisposableCopy before any SQLite open/migration"
      pattern: "copyCheckpointedCatalogToDisposableCopy"
    - from: "scripts/check-taxonomy-contract.ts"
      to: "src/generated/entity-redirects.ts"
      via: "complete green audit followed by explicit atomic --write-generated"
      pattern: "check-redirects|write-generated"
---

<objective>
Prove the complete taxonomy outcome on a protected-source-owned copy and generate the one checked-in public-only redirect artifact from that audited result.

Purpose: Full identity correctness and the permanent route map must be settled before renderer/fixture work can consume them.
Output: Deterministic full-set audit artifacts, protected-source proof and checked-in redirect module.
</objective>

<execution_context>
@/Users/xz/.codex/gsd-core/workflows/execute-plan.md
@/Users/xz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/21-taxonomy/21-RESEARCH.md
@.planning/phases/21-taxonomy/21-PATTERNS.md
@.planning/phases/21-taxonomy/21-SPLIT-IDENTITY-LOCK.md
@.planning/phases/21-taxonomy/21-04-SUMMARY.md
@src/lib/audit/read-only-catalog.ts
@src/lib/audit/readiness-audit.ts

<interfaces>
Use only `copyCheckpointedCatalogToDisposableCopy()` for the real 305 source boundary. Snapshot/fingerprint main/WAL/SHM before and after; never call `openReadOnlyCatalog()` or otherwise SQLite-open the protected source. Migration, taxonomy writes and audit use only the returned owned copy.

The checked-in module is a production artifact. Only explicit `--write-generated`, after the complete audit and redirect preflight are green, may atomically replace `src/generated/entity-redirects.ts`; `--check-redirects` is read-only. This plan has no fixture-output or browser mode.

Locked routes are exact: Waterman old mixed path can target only currently-public Hémisphère; Opus 88 and Leonardo have no redirect entry under any state; Aurora old pen path can target only currently-public `/brand/aurora`.
</interfaces>
</context>

<execution_timeboxes>
- Focused synthetic contract selectors may iterate normally.
- Invoke the protected-source checkpoint-copy/full audit at most once. If it exceeds 8 minutes, a source fingerprint/ownership guard fails, or ownership becomes uncertain: stop, clean the registered copy, record the exact blocker and do not retry automatically.
- Redirect generation/checking after that audit is read-only except for the single explicit atomic `--write-generated`; no browser build/server is allowed in this plan.
</execution_timeboxes>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Audit the full manifest on a checkpointed owned copy and emit deterministic artifacts</name>
  <files>src/lib/audit/taxonomy-audit.ts, scripts/check-taxonomy-contract.ts, tests/taxonomy/taxonomy-contract.test.ts, .planning/phases/21-taxonomy/artifacts/*</files>
  <behavior>
    - Fixture/full owned copy -> exact manifest/action/before-after/publication/reference audit with deterministic sort and bytes.
    - Real source -> main/WAL/SHM fingerprints unchanged before/after; all SQLite work occurs only on one registered owned copy.
    - First apply -> exact declared changes; second same-checksum apply -> deterministic no-op; mismatch -> fail closed.
    - Audit -> exact 109 unique rows, six primary-action counts sum 109, out-of-matrix isolated, set-derived brand/pen/page deltas reconcile.
    - Integrity -> quick_check ok, foreign_key_check empty, semantic orphan/redirect/alias/reverse/made_by/publication violations zero.
    - Console `--limit` -> display-only; canonical NDJSON/CSV/summary bytes, hash, verdict and exit status unchanged.
  </behavior>
  <action>
Build `taxonomy-audit.ts` over the full decoded manifest and raw inventory, not the locked Phase 19 305 count after identity changes. Emit every operation and row, before/after raw and active canonical sets, applied/deferred/gated partitions, six action counts, out-of-matrix counts, brand/pen/page set deltas, alias/redirect/lineage status, publication status and blockers. Audit every reference surface from Plan 21-04, one-maker/reverse-model exact sets, old review non-inheritance, exact split-lock payload assignment and zero unexpected public outputs. Reuse Phase 19 stable NDJSON/CSV/formula-protection/atomic-write patterns, not its baseline assertions.

Use `check-taxonomy-contract.ts` as the single thin controller for fixture contracts and the protected-source boundary. It snapshots the source filesystem, uses `copyCheckpointedCatalogToDisposableCopy`, runs canonical migrations and Phase 21 apply only on the copy, replays once, audits, atomically writes the three artifacts, closes/cleans owned resources, then proves source fingerprints unchanged. Reject nonempty WAL/journals, aliases and unsafe destinations before SQLite access. Do not add browser/server/child lifecycle code, invoke Phase 19 wrappers, remote services or deployment.
  </action>
  <verify><automated>pnpm exec tsx --test --test-name-pattern "full taxonomy audit|artifact determinism|protected source" tests/taxonomy/taxonomy-contract.test.ts &amp;&amp; pnpm exec tsx scripts/check-taxonomy-contract.ts --source=data/fpkg.db --owned-copy --artifacts=.planning/phases/21-taxonomy/artifacts</automated></verify>
  <done>The full 109-row result is byte-stable, set-reconciled and proven only on an owned copy whose protected source is unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Generate the checked-in public-only redirect artifact</name>
  <files>scripts/check-taxonomy-contract.ts, src/generated/entity-redirects.ts, tests/taxonomy/taxonomy-contract.test.ts</files>
  <behavior>
    - Published rename/merge/alias -> old normalized path has one same-site 308 destination; draft/retired/unresolved target -> generated entry absent.
    - Waterman -> conditional public Hémisphère entry; Opus 88/Leonardo -> no entry; Aurora -> conditional public brand entry and never either model.
    - Candidate set -> no duplicate source, collision with legacy/article routes, chain, loop, external destination or fragment without a public canonical owner.
    - `--check-redirects` -> never writes; `--write-generated` without the exact green full-audit hash -> fails before touching the checked-in file.
    - Same audited input -> byte-identical module; atomic write failure leaves prior bytes intact.
  </behavior>
  <action>
Add deterministic redirect serialization to the audit controller. Validate normalized sources, exact lock-declared destinations, same-site `/brand|pen/` paths/fragments, current `public_entities` membership, route-map collisions and one-hop closure. Candidate rows targeting draft/retired/unresolved entities remain in the ledger/report but are omitted from runtime output. There is no ambiguous-split brand fallback.

Make `--check-redirects` compare expected bytes without writes. Gate `--write-generated` on the exact complete audit artifact hash and protected-source proof from Task 1, then atomically replace only `src/generated/entity-redirects.ts`. Do not accept an arbitrary output path, fixture flag, environment JSON or synthetic sentinel in this plan. Contract tests prove all four locked route policies and write/check idempotence.
  </action>
  <verify><automated>pnpm exec tsx --test --test-name-pattern "redirect generation|locked old routes|generated artifact" tests/taxonomy/taxonomy-contract.test.ts &amp;&amp; pnpm exec tsx scripts/check-taxonomy-contract.ts --write-generated --audit-summary=.planning/phases/21-taxonomy/artifacts/taxonomy-summary.json &amp;&amp; pnpm exec tsx scripts/check-taxonomy-contract.ts --check-redirects &amp;&amp; pnpm exec tsc --noEmit</automated></verify>
  <done>The checked-in module is an exact, deterministic projection of one green full audit and contains only lawful current-public redirects.</done>
</task>

</tasks>

<prohibitions>
- Do not SQLite-open or mutate the protected source; all real-305 work begins with a checkpointed filesystem copy and continues only on that copy.
- Do not write Turso, deploy Vercel, push, run a browser/server, or describe owned-copy evidence as production.
- Do not enable redirects to draft/unresolved/wrong split targets, create chains/choice pages or infer fallbacks.
- Do not add fixture redirect injection, DTO/UI changes, `next.config.ts` changes or E2E here; Plan 21-06 owns them.
</prohibitions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| protected catalog -> owned copy | Filesystem snapshot is trusted evidence but must never be SQLite-opened in place. |
| audit/ledger -> generated redirect code | Only a complete audited current-public same-site destination may enter the checked-in runtime module. |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-21-17 | Tampering | protected source/owned copy | high | mitigate | Checkpoint-copy helper, sidecar/link guards, before/after fingerprints and owned-only writes. |
| T-21-18 | Elevation of Privilege | redirect to draft/wrong split target | high | mitigate | Generator joins exact lock destination to current public set and requires a complete green audit hash. |
| T-21-20 | Repudiation | full-set/net result | medium | mitigate | Deterministic artifacts, hashes, exact sets and one explicit generated write. |
</threat_model>

<verification>
- `pnpm exec tsx --test --test-name-pattern "full taxonomy audit|artifact determinism|protected source|redirect generation|locked old routes|generated artifact" tests/taxonomy/taxonomy-contract.test.ts`
- `pnpm exec tsx scripts/check-taxonomy-contract.ts --source=data/fpkg.db --owned-copy --artifacts=.planning/phases/21-taxonomy/artifacts`
- `pnpm exec tsx scripts/check-taxonomy-contract.ts --write-generated --audit-summary=.planning/phases/21-taxonomy/artifacts/taxonomy-summary.json`
- `pnpm exec tsx scripts/check-taxonomy-contract.ts --check-redirects`
- `pnpm exec tsc --noEmit`
- `pnpm biome check src/lib/audit/taxonomy-audit.ts scripts/check-taxonomy-contract.ts src/generated/entity-redirects.ts tests/taxonomy/taxonomy-contract.test.ts`
</verification>

<success_criteria>
- All 109 rows and every identity/reference/publication outcome reconcile on one owned copy while the protected source remains fingerprint-identical.
- The checked-in redirect module is byte-stable, exact-lock compliant and generated only from the recorded complete audit.
- No DTO, UI, temporary fixture redirect or browser concern remains in this plan.
</success_criteria>

<output>Create `.planning/phases/21-taxonomy/21-05-SUMMARY.md` when done.</output>
