---
phase: 18-publication-gate
plan: "03"
type: execute
wave: 3
depends_on:
  - "18-02"
files_modified:
  - migrations/030_publication_gate.sql
  - src/lib/db.ts
  - src/lib/publication.ts
  - scripts/check-publication-gate.ts
autonomous: true
requirements:
  - PUB-01
  - PUB-02
  - PUB-05
  - PUB-06
must_haves:
  truths:
    - "brand/pen publication row 显式存在并默认 draft；type transition 不继承旧审核。"
    - "contract version 1 readiness 从当前原始数据计算，diagnostic snapshot 不授权。"
    - "全部 critical inputs 的 INSERT/UPDATE/DELETE 递增 revision 并立即隐藏旧发布。"
    - "server-only transaction 与 DB transition guard 双重约束 published。"
    - "backfill published=0 且 deprecated stories 逐行不变。"
  artifacts:
    - path: "migrations/030_publication_gate.sql"
      provides: "publication schema/views/triggers"
    - path: "src/lib/publication.ts"
      provides: "canonical hash and bounded publication transactions"
  key_links:
    - from: "publication critical tables"
      to: "entity_publications.content_revision"
      via: "row trigger invalidation"
      pattern: "content_revision"
    - from: "publishEntity"
      to: "public_entity_readiness/public_entities"
      via: "single write transaction recheck"
      pattern: "PUBLICATION_CONTRACT_VERSION"
---

<objective>
实现 030 publication/readiness/hash/state-machine 数据契约。

Purpose: 将“实体存在”与“可公开”从数据库层分离，并保证旧审核可确定性失效。
Output: schema、computed views、version-aware readiness guard、canonical hash、invalidation/type triggers、atomic publish fixtures。
</objective>

<execution_context>
@/Users/xz/.codex/gsd-core/workflows/execute-plan.md
@/Users/xz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/18-publication-gate/18-CONTEXT.md
@.planning/phases/18-publication-gate/18-RESEARCH.md
@.planning/phases/18-publication-gate/18-PATTERNS.md
@.planning/phases/18-publication-gate/18-VALIDATION.md
@.planning/phases/18-publication-gate/18-02-SUMMARY.md
@migrations/011_library_schema.sql
@migrations/023_retire_template_stories.sql
@src/lib/db.ts
@scripts/check-publication-gate.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 建立 030 schema、readiness 与 type state-machine</name>
  <files>migrations/030_publication_gate.sql, src/lib/db.ts, scripts/check-publication-gate.ts</files>
  <read_first>migrations/030_publication_gate.sql, src/lib/db.ts, scripts/check-publication-gate.ts, migrations/011_library_schema.sql, migrations/004_links.sql, migrations/006_fts.sql, migrations/023_retire_template_stories.sql</read_first>
  <action>新增 `entity_publications`、indexes、normalized computed `publication_blockers`、`public_entity_readiness`、显式列 `public_entities`。status 仅 draft/in_review/published/retired，contract version 固定 1；结构 blockers 覆盖 publication/summary/correct story/made_by/hash/revision/contract/reviewer/timestamps，status 本身不作为 readiness blocker，blockers_json 仅诊断。public_entities 对 brand/pen 及任何显式 publication row 使用 strict gate，无 publication row 的其他类型保留旧行为。entity INSERT/type UPDATE triggers 覆盖进入 brand/pen、brand↔pen、离开 brand/pen，均 upsert/reset draft 并清除审核；backfill 全部既有 brand/pen 为 draft、published=0，禁止改 stories。assertDatabaseReady 仅在 selected migrationsDir 含 030 时核对版本感知 sqlite_schema manifest。</action>
  <verify>
    <automated>pnpm check:publication-gate -- --migration && pnpm check:publication-gate -- --backfill</automated>
  </verify>
  <acceptance_criteria>
    - "fresh/upgrade/idempotent/custom pre-030 fixture 均通过。"
    - "四类 type transition 都保持 non-published 并清除旧审核。"
    - "deprecated story id/status/body hash diff 为 0，brand/pen published=0。"
  </acceptance_criteria>
  <done>030 建立 version-aware、fail-closed publication schema。</done>
</task>

<task type="auto">
  <name>Task 2: 固定 canonical hash 与全部 invalidation fan-out</name>
  <files>migrations/030_publication_gate.sql, src/lib/publication.ts, scripts/check-publication-gate.ts</files>
  <read_first>migrations/030_publication_gate.sql, src/lib/publication.ts, scripts/check-publication-gate.ts, src/lib/db.ts, migrations/011_library_schema.sql</read_first>
  <action>在 server-only publication module 定义固定 keys/explicit null/Unicode NFC/newline/type normalization/stable sort/dedup 的 `sha256:v1:` payload。纳入 entity、published story、spec/variant、claim/citation、source item review/allowed-use、source registry reliability/license/independence、reference、timeline、approved primary media、made_by。为 entities、stories、model_specs、model_variants、claims、citations、source_items、source_registry、entity_references、timeline_events、media_assets、entity_links 的相关 I/U/D 建 fan-out triggers：revision 原子递增，published 改 in_review，approved hash 保留。测试每表三种 mutation、source fan-out、乱序 hash 稳定及 critical field hash 变化。</action>
  <verify>
    <automated>pnpm check:publication-gate -- --invalidation</automated>
  </verify>
  <acceptance_criteria>
    - "相同语义乱序输入产生同一 sha256:v1 hash。"
    - "12 类 critical table 的 I/U/D fixture 全部通过。"
    - "critical edit 可提交且 transaction 后立即退出 public_entities。"
  </acceptance_criteria>
  <done>任何关键内容、证据、版本、许可、媒体或 made_by 变化都会使旧审核失效。</done>
</task>

<task type="auto">
  <name>Task 3: 实现 DB-guarded atomic publish transaction</name>
  <files>migrations/030_publication_gate.sql, src/lib/publication.ts, scripts/check-publication-gate.ts</files>
  <read_first>migrations/030_publication_gate.sql, src/lib/publication.ts, scripts/check-publication-gate.ts, src/lib/db.ts</read_first>
  <action>实现无 retry 的 `publishEntity(db,{entityId,reviewer})`：读稳定输入/算 hash、写 review metadata/revision/contract、重查 readiness、零 blocker 才 published、断言 public_entities membership、失败 rollback。`setEntityPublicationStatus` 只允许 draft/in_review/retired。BEFORE transition trigger 拒绝 direct SQL 绕过，要求 readiness=0、revision/contract 匹配、hash format 与 reviewer/reviewed_at/published_at 完整。fixture 覆盖 missing/draft/in_review/retired/stale/malformed/direct SQL/valid brand+pen 及 rollback。</action>
  <verify>
    <automated>pnpm check:publication-gate -- --publish</automated>
  </verify>
  <acceptance_criteria>
    - "direct SQL 不能仅改 status 获得 published。"
    - "失败 transaction 不留下半审核状态且无 write retry。"
    - "valid fixture 发布、critical edit 隐藏、重新审核发布全部通过。"
  </acceptance_criteria>
  <done>publication 只能经 server transaction 与 DB guard 双重授权原子进入。</done>
</task>

</tasks>

## Artifacts This Plan Produces

- migration `030_publication_gate.sql`
- table `entity_publications`
- views `publication_blockers`, `public_entity_readiness`, `public_entities`
- `PUBLICATION_CONTRACT_VERSION = 1`
- `computePublicationContentHash()`, `publishEntity()`, `setEntityPublicationStatus()`

<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation |
|---|---|---|---|---|---|
| T-18-04 | Elevation of Privilege | direct SQL publish | high | mitigate | DB transition guard + negative fixtures |
| T-18-05 | Tampering | stale review/hash | high | mitigate | revision triggers + computed readiness |
</threat_model>

<verification>
- `pnpm check:publication-gate -- --migration`
- `pnpm check:publication-gate -- --backfill`
- `pnpm check:publication-gate -- --invalidation`
- `pnpm check:publication-gate -- --publish`
- `pnpm check:migrations && pnpm check:data-contract && pnpm build`
- 不运行 remote migration、push 或 deploy。
</verification>

<success_criteria>
- PUB-01/PUB-02/PUB-05/PUB-06 的 DB contract 全部可由 disposable fixture 复验。
- 本计划只修改 4 个文件；每个 task 少于 15 个 files。
</success_criteria>

<output>
Create `.planning/phases/18-publication-gate/18-03-SUMMARY.md` when done.
</output>
