---
phase: 18-publication-gate
plan: "03"
type: execute
wave: 3
depends_on:
  - "18-02"
files_modified:
  - src/lib/public-visibility.ts
  - src/lib/browse-data.ts
  - src/lib/recommend.ts
  - src/lib/concept-engine.ts
  - src/lib/library.ts
  - src/components/MarkdownRenderer.tsx
  - src/app/[type]/[slug]/page.tsx
  - src/app/page.tsx
  - src/app/browse/page.tsx
  - src/app/by/[dimension]/page.tsx
  - src/app/graph/page.tsx
  - src/app/sitemap.ts
  - src/app/timeline/page.tsx
  - src/app/exhibits/page.tsx
  - src/app/exhibits/[slug]/page.tsx
  - src/app/library/page.tsx
  - src/app/library/sources/page.tsx
  - src/app/library/diagrams/page.tsx
  - src/app/api/browse/route.ts
  - src/app/api/links/route.ts
  - src/app/api/entities/route.ts
  - src/app/api/entities/[slug]/route.ts
  - src/app/api/entities/[slug]/preview/route.ts
  - src/app/api/image-proxy/route.ts
  - src/middleware.ts
  - scripts/check-public-boundary.ts
autonomous: true
requirements:
  - PUB-03
  - PUB-04
  - PUB-05
must_haves:
  truths:
    - "detail、metadata、完整列表、聚合、关系与间接发现 surface 全部从同一 public_entities universe 授权。"
    - "未发布 brand/pen 直接 404 且不生成 canonical、Open Graph 或 JSON-LD；canonical legacy identity 仍先 redirect。"
    - "每个 join alias、neighbor、owner、parent brand 与 candidate 都独立 gate，不能只过滤主实体。"
    - "public API 仅返回现有 allowlist，不暴露 publication、review、hash、blocker 或数据库 id。"
    - "所有 entity-bearing 页面/API 采用 dynamic/no-store，离线 critical edit 后下一请求立即反映 public_entities。"
  artifacts:
    - path: "src/lib/public-visibility.ts"
      provides: "alias-safe public_entities EXISTS filter 与 direct public lookup"
    - path: "src/lib/browse-data.ts"
      provides: "browse rows/counts/facets/type counts 的统一 public universe"
    - path: "src/lib/library.ts"
      provides: "owner-aware representative/source/media/exhibit/timeline/diagram public links"
    - path: "scripts/check-public-boundary.ts"
      provides: "逐组 surface gate 与 cache-policy 快速检查"
  key_links:
    - from: "all public SQL aliases"
      to: "public_entities"
      via: "publicEntityFilter(alias) 或 direct view lookup"
      pattern: "public_entities"
    - from: "metadata/detail/API"
      to: "getPublicEntityBySlug(type, slug)"
      via: "redirect 后的逐 ID authorization"
      pattern: "getPublicEntityBySlug"
    - from: "media/source/wiki/library links"
      to: "public owner"
      via: "owner-aware join/slug resolution"
      pattern: "publicEntityFilter"
---

<objective>
把 canonical `public_entities` gate 接到每个公开 surface，并统一改为 dynamic/no-store，使数据库失效结果不会被旧 ISR/CDN cache 继续公开。

Purpose: 实现 PUB-03/PUB-04 的单一公开真相，并完成 PUB-05 从数据库即时失效到下一 HTTP 请求即时隐藏的运行时闭环。
Output: canonical visibility helpers、全量 primary/secondary surface 接线、public DTO leak guard、owner-aware media/link 访问与保守缓存策略。
</objective>

<execution_context>
@/Users/xz/.codex/gsd-core/workflows/execute-plan.md
@/Users/xz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-publication-gate/18-CONTEXT.md
@.planning/phases/18-publication-gate/18-RESEARCH.md
@.planning/phases/18-publication-gate/18-PATTERNS.md
@.planning/phases/18-publication-gate/18-VALIDATION.md
@.planning/phases/18-publication-gate/18-02-SUMMARY.md
@src/lib/public-visibility.ts
@src/app/[type]/[slug]/page.tsx
@src/lib/browse-data.ts
@src/lib/library.ts
@scripts/check-public-boundary.ts

<interfaces>
Preserve `publicEntityFilter(alias)` alias validation but replace its predicate with an EXISTS against `public_entities`.
Create `getPublicEntityBySlug(type, slug)` as the sole detail/metadata/public-detail-API lookup.
Retain `isPublicEntity()` only for deterministic non-brand/pen compatibility or redirect support; it must never authorize a raw brand/pen row.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 接通 canonical helper、detail/metadata 与 public entity APIs</name>
  <files>src/lib/public-visibility.ts, src/app/[type]/[slug]/page.tsx, src/app/api/entities/route.ts, src/app/api/entities/[slug]/route.ts, src/app/api/entities/[slug]/preview/route.ts, src/app/sitemap.ts, scripts/check-public-boundary.ts</files>
  <read_first>src/lib/public-visibility.ts, src/app/[type]/[slug]/page.tsx, src/app/api/entities/route.ts, src/app/api/entities/[slug]/route.ts, src/app/api/entities/[slug]/preview/route.ts, src/app/sitemap.ts, .planning/phases/18-publication-gate/18-PATTERNS.md</read_first>
  <action>重写 `publicEntityFilter(alias)` 为经过现有 identifier regex 验证的 `EXISTS (SELECT 1 FROM public_entities ...)`；移除 hard-coded alias constant 的授权用途。新增 `getPublicEntityBySlug(type,slug)`，只返回 public view 的显式 public columns。metadata 与 page 保持 `getCanonicalEntityPath()`/redirect 在先，之后共同调用 direct public lookup；未发布 target `notFound()`，相关实体与 pen parent brand 的每个 alias 也 gate，因而 404 分支不能生成 canonical/Open Graph/JSON-LD。entities list/detail/hover-preview API 与 sitemap 改读同一 view/helper，response 继续 exact DTO，明确 deny publication status、depth、score、blockers、hash、revision、contract、reviewer/timestamps/notes 与 DB id；sitemap DB/gate 错误不得 catch 成静默空集合。</action>
  <verify>
    <automated>pnpm check:public-boundary -- --detail-api</automated>
  </verify>
  <acceptance_criteria>
    - "detail、metadata、entity detail API、preview 对每个 fixture id 的可达性与 public_entities 等价。"
    - "legacy canonical identity 先 redirect；canonical target 未发布时最终 404。"
    - "unpublished response 不含 canonical、Open Graph、JSON-LD 或 publication internals。"
    - "pnpm check:public-boundary -- --detail-api 退出码为 0。"
  </acceptance_criteria>
  <done>逐 ID 公开路径与 public_entities 完全同源，raw brand/pen 与 JS predicate 不再授权公开。</done>
</task>

<task type="auto">
  <name>Task 2: 覆盖完整列表、聚合、graph、recommend 与所有间接 public owner</name>
  <files>src/lib/browse-data.ts, src/lib/recommend.ts, src/lib/concept-engine.ts, src/lib/library.ts, src/components/MarkdownRenderer.tsx, src/app/page.tsx, src/app/browse/page.tsx, src/app/by/[dimension]/page.tsx, src/app/graph/page.tsx, src/app/timeline/page.tsx, src/app/exhibits/page.tsx, src/app/exhibits/[slug]/page.tsx, src/app/library/page.tsx, src/app/library/sources/page.tsx, src/app/library/diagrams/page.tsx, src/app/api/browse/route.ts, src/app/api/links/route.ts, src/app/api/image-proxy/route.ts, src/middleware.ts, scripts/check-public-boundary.ts</files>
  <read_first>src/lib/browse-data.ts, src/lib/recommend.ts, src/lib/concept-engine.ts, src/lib/library.ts, src/components/MarkdownRenderer.tsx, src/app/graph/page.tsx, src/app/api/links/route.ts, src/app/api/image-proxy/route.ts, src/middleware.ts, .planning/phases/18-publication-gate/18-RESEARCH.md</read_first>
  <action>按 18-PATTERNS Surface Anchor Map 逐 query 审计每个 entity alias。browse rows/counts/facets/type counts、home stats/featured/stars、by-dimension brand/pen counts 均以 public view 聚合；graph 的 hub/selected/degree/neighbor 与 links API 的 center/source/target/backlink/two-hop 全部 gate；recommend 的 current/direct/model/tag/brand candidate 只从 public set 产生。concept recompute 只写 public matches，读取时再次 gate，避免物化 stale row 泄漏。Markdown wiki slug 只解析 canonical public entity。library 的 representative model、featured brand、parent brand、source index 两个 UNION owner、media owner、exhibit/timeline/diagram/section JSON link 全部 gate；`getLibraryCoverageReport()` 保持明确 private/admin raw-backlog helper，且没有 public route 可达。image proxy 根据 media owner join public set，unpublished owner 返回 404。middleware 只保留 namespace/tool 屏蔽与 canonical 辅助，不再把 slug blacklist 当 brand/pen truth source。所有 DTO 继续显式 projection。</action>
  <verify>
    <automated>pnpm check:public-boundary -- --discovery &amp;&amp; pnpm check:public-boundary -- --relationships</automated>
  </verify>
  <acceptance_criteria>
    - "browse/sitemap/entity-list 的完整集合来自 public_entities，facets/statistics 只聚合该集合。"
    - "graph、links、recommend、representative、wiki、source、media、exhibit、timeline、diagram 的每个返回实体均为 public_entities 子集。"
    - "unpublished media owner 的 image proxy 为 404，public payload 不泄漏 owner DB id。"
    - "两条 18-VALIDATION.md 自动命令均退出码为 0。"
  </acceptance_criteria>
  <done>所有直接和间接发现路径都使用 canonical public gate，neighbor/owner/parent/candidate 无漏网 alias。</done>
</task>

<task type="auto">
  <name>Task 3: 将全部 entity-bearing response 固定为 dynamic/no-store</name>
  <files>src/app/[type]/[slug]/page.tsx, src/app/page.tsx, src/app/browse/page.tsx, src/app/by/[dimension]/page.tsx, src/app/graph/page.tsx, src/app/sitemap.ts, src/app/timeline/page.tsx, src/app/exhibits/page.tsx, src/app/exhibits/[slug]/page.tsx, src/app/library/page.tsx, src/app/library/sources/page.tsx, src/app/library/diagrams/page.tsx, src/app/api/browse/route.ts, src/app/api/links/route.ts, src/app/api/entities/route.ts, src/app/api/entities/[slug]/route.ts, src/app/api/entities/[slug]/preview/route.ts, src/app/api/image-proxy/route.ts, scripts/check-public-boundary.ts</files>
  <read_first>src/app/[type]/[slug]/page.tsx, src/app/api/browse/route.ts, src/app/api/image-proxy/route.ts, playwright.config.ts, .planning/phases/18-publication-gate/18-CONTEXT.md, .planning/phases/18-publication-gate/18-VALIDATION.md</read_first>
  <action>选择 CONTEXT 已锁定的 conservative 策略：所有列出的 entity-bearing server pages/sitemap 使用 `dynamic = "force-dynamic"` 并移除 `revalidate = 600`；所有列出的 GET APIs 与 owner-bound image proxy 返回 `Cache-Control: no-store`，移除 browse 的 shared SWR 与 image success 的长 CDN cache。不要引入 `revalidatePath`/`revalidateTag` 或假设离线 importer 能 purge Next cache。扩展 boundary cache-policy check，验证源码无 entity-bearing TTL/SWR、HTTP header 为 no-store，并在隔离 server 上覆盖 draft 404→publish 200、published 200→critical edit/retire 404、重新审核→200 的下一请求结果；同时更新本任务触及的旧 cache expectation，使它断言 no-store。</action>
  <verify>
    <automated>pnpm check:public-boundary -- --cache-policy</automated>
  </verify>
  <acceptance_criteria>
    - "所有 entity-bearing page/API 均无 600 秒 ISR、shared SWR 或 30 天 owner-media cache。"
    - "离线 DB mutation 后下一请求即反映 publication membership，无 purge endpoint/outbox 依赖。"
    - "draft→published、published→edit/retired、re-review→published 三个方向均有隔离 fixture。"
    - "pnpm check:public-boundary -- --cache-policy 退出码为 0。"
  </acceptance_criteria>
  <done>database fail-closed 状态与下一 HTTP 请求一致，不会被旧页面/API/CDN cache 延迟十分钟或更久。</done>
</task>

</tasks>

## Artifacts This Plan Produces

- canonical SQL helper `publicEntityFilter(alias)` backed by `public_entities`
- direct lookup `getPublicEntityBySlug(type, slug)`
- explicit dynamic/no-store contract for all entity-bearing pages and APIs
- CLI checks `--detail-api`, `--discovery`, `--relationships`, `--cache-policy`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| database → public HTTP surface | 内部/draft rows 不能通过任一 route、API、join 或 cache 泄漏 |
| public API → client | response 只能暴露 allowlisted public fields |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-07 | Information Disclosure | ungated alias/owner/neighbor | high | mitigate | 全量 surface anchor audit + relationship subset checks |
| T-18-08 | Information Disclosure | public API DTO | high | mitigate | explicit projection、exact-key allowlist、publication field denylist |
| T-18-09 | Information Disclosure | stale ISR/CDN response | high | mitigate | force-dynamic/no-store + transition-direction integration tests |
</threat_model>

<verification>
- `pnpm check:public-boundary -- --detail-api`
- `pnpm check:public-boundary -- --discovery`
- `pnpm check:public-boundary -- --relationships`
- `pnpm check:public-boundary -- --cache-policy`
- `pnpm check:migrations && pnpm check:data-contract && pnpm check:publication-gate && pnpm check:public-boundary && pnpm check:library && pnpm lint && pnpm build`
- 不运行 remote migration、push 或 deploy。
</verification>

<success_criteria>
- 完整列表、逐 ID、聚合与上下文子集四类 surface 均按各自语义共享 public_entities。
- 未发布实体从 detail/metadata/browse/facets/sitemap/graph/recommend/library/wiki/media/public API 全部消失。
- canonical redirect 保留，未达标 canonical target 返回 404。
- public API 无 publication internal field 或 DB id 泄漏。
- 所有 entity-bearing response dynamic/no-store，critical edit/retire 下一请求立即隐藏。
</success_criteria>

<output>
Create `.planning/phases/18-publication-gate/18-03-SUMMARY.md` when done.
</output>
