---
phase: 18-publication-gate
plan: "05"
type: execute
wave: 5
depends_on:
  - "18-04"
files_modified:
  - src/lib/browse-data.ts
  - src/app/page.tsx
  - src/app/browse/page.tsx
  - src/app/by/[dimension]/page.tsx
  - src/app/graph/page.tsx
  - src/app/api/browse/route.ts
  - src/app/api/links/route.ts
  - scripts/check-public-boundary.ts
autonomous: true
requirements:
  - PUB-03
  - PUB-04
  - PUB-05
must_haves:
  truths:
    - "browse rows/counts/facets、home、by-dimension 聚合只计算 public_entities。"
    - "graph 与 links API 的 center/source/target/neighbor/two-hop 每个 alias 都 gate。"
    - "本计划每个 entity-bearing page/API 随修改同步 dynamic/no-store。"
  artifacts:
    - path: "src/lib/browse-data.ts"
      provides: "canonical browse rows and aggregates"
  key_links:
    - from: "browse/home/by/graph/links"
      to: "public_entities"
      via: "alias-safe filters on every entity role"
      pattern: "publicEntityFilter"
---

<objective>
将 browse、home、by-dimension、graph 与 links API 发现面接到 canonical public universe。

Purpose: 封住最主要的列表、聚合和图关系发现路径。
Output: exact browse aggregates、public-only graph/links、discovery no-store checks。
</objective>

<execution_context>
@/Users/xz/.codex/gsd-core/workflows/execute-plan.md
@/Users/xz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/18-publication-gate/18-CONTEXT.md
@.planning/phases/18-publication-gate/18-PATTERNS.md
@.planning/phases/18-publication-gate/18-VALIDATION.md
@.planning/phases/18-publication-gate/18-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 统一 browse、home 与 by-dimension 列表/聚合</name>
  <files>src/lib/browse-data.ts, src/app/page.tsx, src/app/browse/page.tsx, src/app/by/[dimension]/page.tsx, src/app/api/browse/route.ts</files>
  <read_first>src/lib/browse-data.ts, src/app/page.tsx, src/app/browse/page.tsx, src/app/by/[dimension]/page.tsx, src/app/api/browse/route.ts, src/lib/public-visibility.ts</read_first>
  <action>browse rows、total/count、facets、type counts 的每组 query 都从 public view/alias helper 计算；home stats/featured/per-type stars 与 by-dimension brands/pens/counts 使用同一集合并保持 explicit DTO。home、browse、by page 设置 force-dynamic 并移除 revalidate；browse API 将 shared SWR 改为 Cache-Control no-store。checker 分别比较完整 row set 双向 equality 与 facets/statistics 按 key 聚合 equality，不能用数量阈值替代。</action>
  <verify>
    <automated>pnpm check:public-boundary -- --discovery-lists</automated>
  </verify>
  <acceptance_criteria>
    - "browse rows 双向等于 public_entities，facets/type/home/by aggregates 按 key 等价。"
    - "home/browse/by/API 无 ISR 或 shared SWR。"
    - "pnpm check:public-boundary -- --discovery-lists 退出码为 0。"
  </acceptance_criteria>
  <done>所有主要列表与聚合只统计可公开实体。</done>
</task>

<task type="auto">
  <name>Task 2: gate graph 与 links API 的全部 entity aliases</name>
  <files>src/app/graph/page.tsx, src/app/api/links/route.ts, scripts/check-public-boundary.ts</files>
  <read_first>src/app/graph/page.tsx, src/app/api/links/route.ts, scripts/check-public-boundary.ts, src/lib/public-visibility.ts</read_first>
  <action>graph hubs、selected entity、degree/neighbor aliases 全部 gate；links API 的 center、forward/backlink source/target、neighbor 与 second-hop 每个 alias 都 gate，继续只返回 public identity fields。graph page force-dynamic；links GET no-store。checker 使用上下文 subset 语义，逐项证明返回 id 属于 public_entities 并验证 unpublished neighbor/owner 不泄漏，不能要求 LIMIT 结果与全集相等。</action>
  <verify>
    <automated>pnpm check:public-boundary -- --discovery-graph</automated>
  </verify>
  <acceptance_criteria>
    - "graph/links 每个返回 entity 都是 public_entities 子集。"
    - "center/source/target/neighbor/two-hop 无漏 gate alias。"
    - "graph/links response dynamic/no-store。"
  </acceptance_criteria>
  <done>图谱与关系 API 不再从 unpublished neighbor 或 owner 间接泄漏实体。</done>
</task>

</tasks>

## Artifacts This Plan Produces

- checker flags `--discovery-lists`, `--discovery-graph`
- exact aggregate and graph-subset contracts

<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation |
|---|---|---|---|---|---|
| T-18-07 | Information Disclosure | discovery aliases | high | mitigate | every-role gate + aggregate/subset checks |
</threat_model>

<verification>
- `pnpm check:public-boundary -- --discovery-lists`
- `pnpm check:public-boundary -- --discovery-graph`
- `pnpm check:public-boundary && pnpm lint && pnpm build`
- 不运行 remote migration、push 或 deploy。
</verification>

<success_criteria>
- 列表/聚合与 graph/subset 语义分别正确。
- 每个修改 surface 随任务切换 dynamic/no-store。
- 本计划只修改 8 个文件；每个 task 少于 15 个 files。
</success_criteria>

<output>
Create `.planning/phases/18-publication-gate/18-05-SUMMARY.md` when done.
</output>
