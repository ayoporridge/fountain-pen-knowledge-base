---
phase: 18-publication-gate
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/db.ts
  - scripts/import-brand-completion-sources.ts
  - scripts/import-exhibit-content.ts
  - scripts/import-model-gap-sources.ts
  - scripts/import-official-brand-sources.ts
  - scripts/import-official-brand-stories.ts
  - scripts/import-official-model-diagrams.ts
  - scripts/import-official-model-sources.ts
  - scripts/import-research-gap-sources.ts
  - scripts/import-warm-pen-atlas-media.ts
  - scripts/import-wikidata-brands.ts
  - scripts/import-csv.ts
  - scripts/import-markdown.ts
  - scripts/seed.ts
  - scripts/seed-tags.ts
  - scripts/seed-concepts.ts
  - scripts/seed-library-samples.ts
  - scripts/check-migration-safety.ts
  - scripts/check-publication-gate.ts
  - playwright.config.ts
  - package.json
autonomous: true
requirements:
  - PUB-01
  - PUB-06
must_haves:
  truths:
    - "所有 importer 在写入前只做只读 readiness 检查，不能执行 migration SQL 或写 migration marker。"
    - "所有 seed 入口只调用 src/lib/db.ts 的 canonical migrateDatabase()，不存在第二套 migration 实现。"
    - "缺少 pending migration 的数据库会让 importer 在首次业务写入前失败，且 migrations 表不会新增伪 marker。"
    - "migration、publication contract 与 Playwright fixture 使用临时数据库，绝不修改 data/fpkg.db。"
  artifacts:
    - path: "scripts/check-migration-safety.ts"
      provides: "migration writer ownership guard 与 missing-migration regression"
    - path: "scripts/check-publication-gate.ts"
      provides: "后续 schema、publication 与 E2E 共用的隔离数据库生命周期"
    - path: "src/lib/db.ts"
      provides: "server-only file database override 与 canonical migration/readiness API"
    - path: "playwright.config.ts"
      provides: "以隔离数据库启动本地 Next server 的 Playwright 配置"
  key_links:
    - from: "scripts/import-*.ts"
      to: "src/lib/db.ts::assertDatabaseReady"
      via: "任何写入开始前的只读 preflight"
      pattern: "assertDatabaseReady"
    - from: "scripts/seed*.ts"
      to: "src/lib/db.ts::migrateDatabase"
      via: "唯一 migration owner"
      pattern: "migrateDatabase"
    - from: "playwright.config.ts"
      to: "scripts/check-publication-gate.ts"
      via: "临时 DB server harness"
      pattern: "check:publication-gate"
---

<objective>
先收回 migration ownership，并建立可复用的隔离数据库测试底座，保证 publication migration 加入前不存在“marker 已写、SQL 未执行”的旁路。

Purpose: PUB-01/PUB-06 的 fail-closed 迁移只有在 canonical runner 真正独占 migration 写入时才可信；隔离 fixture 则保证后续正向 publish/E2E 不污染真实目录库。
Output: 全部 importer/seed runner 收敛、静态 ownership gate、缺 migration 的行为回归、`check:publication-gate` 临时库 harness、Playwright 隔离数据库启动路径。
</objective>

<execution_context>
@/Users/xz/.codex/gsd-core/workflows/execute-plan.md
@/Users/xz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-publication-gate/18-CONTEXT.md
@.planning/phases/18-publication-gate/18-RESEARCH.md
@.planning/phases/18-publication-gate/18-PATTERNS.md
@.planning/phases/18-publication-gate/18-VALIDATION.md
@src/lib/db.ts
@scripts/migrate.ts
@scripts/check-migration-safety.ts
@playwright.config.ts

<interfaces>
现有 canonical 接口必须保留并复用：
- `getDb(): Client`
- `migrateDatabase(db: Client, options?: MigrationOptions): Promise&lt;MigrationResult&gt;`
- `assertDatabaseReady(db: Client, options?: MigrationOptions): Promise&lt;void&gt;`
- `createReadinessGuard(check): () =&gt; Promise&lt;void&gt;`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 收回全部 importer 与 seed 的 migration ownership</name>
  <files>scripts/import-brand-completion-sources.ts, scripts/import-exhibit-content.ts, scripts/import-model-gap-sources.ts, scripts/import-official-brand-sources.ts, scripts/import-official-brand-stories.ts, scripts/import-official-model-diagrams.ts, scripts/import-official-model-sources.ts, scripts/import-research-gap-sources.ts, scripts/import-warm-pen-atlas-media.ts, scripts/import-wikidata-brands.ts, scripts/import-csv.ts, scripts/import-markdown.ts, scripts/seed.ts, scripts/seed-tags.ts, scripts/seed-concepts.ts, scripts/seed-library-samples.ts</files>
  <read_first>src/lib/db.ts, scripts/migrate.ts, scripts/import-official-model-sources.ts, scripts/seed-library-samples.ts, scripts/seed-concepts.ts, scripts/import-csv.ts, scripts/import-markdown.ts, .planning/phases/18-publication-gate/18-RESEARCH.md</read_first>
  <action>逐一删除 11 个 false-apply runner 以及 `import-csv.ts`、`import-markdown.ts` 中自行枚举、拆分或执行 migration SQL 的代码。十个 source/content importer 加 CSV/Markdown importer 在创建 client 后、任何 transaction 或业务写入前调用 `assertDatabaseReady(client)`；缺 schema 时立即退出，不能修复 marker。`seed.ts`、`seed-tags.ts`、`seed-concepts.ts`、`seed-library-samples.ts` 只调用 `migrateDatabase(client)` 后再 seed，不保留 checksum-free runner、`executeMultiple()` migration loop 或直接 `INSERT/UPDATE migrations`。保持 migration/import/seed write 无自动 retry，且不要改写历史 migration 文件。</action>
  <verify>
    <automated>pnpm check:migrations</automated>
  </verify>
  <acceptance_criteria>
    - "上述 12 个 importer 均在业务写入前调用 assertDatabaseReady(client)。"
    - "上述 4 个 seed 入口均调用 migrateDatabase(client)，且不直接写 migrations 表。"
    - "import-csv.ts、import-markdown.ts、seed-concepts.ts 不再读取 migration 目录或执行 migration 文件。"
    - "pnpm check:migrations 退出码为 0。"
  </acceptance_criteria>
  <done>16 个旁路脚本全部收敛到 canonical migration/readiness API，仓库只剩 src/lib/db.ts 执行 SQL 与 marker 的原子提交。</done>
</task>

<task type="auto">
  <name>Task 2: 建立 server-only 隔离数据库与 Playwright harness</name>
  <files>src/lib/db.ts, scripts/check-publication-gate.ts, playwright.config.ts, package.json</files>
  <read_first>src/lib/db.ts, scripts/check-migration-safety.ts, playwright.config.ts, package.json, .planning/phases/18-publication-gate/18-VALIDATION.md</read_first>
  <action>在 `getDb()` 增加 server-only 的 `FPKG_DATABASE_URL` file URL override：与 `TURSO_DATABASE_URL` 同时出现时硬失败；`PUBLICATION_GATE_FIXTURE=1` 时只接受 `file:` URL，并拒绝解析到仓库 `data/fpkg.db`。新增 `scripts/check-publication-gate.ts`，用 `mkdtempSync()`、独立 file database、显式 close 与 finally/signal cleanup 管理 fixture；先提供 `--fixture-isolation` 和 `--serve-e2e --port 3107` 入口，后续计划在同一文件补 schema/fixture 语义。`--serve-e2e` 必须在临时库执行 canonical migrations、准备 synthetic fixture、以清空 Turso 凭据且设置 `FPKG_DATABASE_URL` 的环境启动 Next，收到终止信号后同时关闭 child process/client 并删除临时目录。将 Playwright 本地 webServer 改走该 harness；外部 `E2E_BASE_URL` 行为保持不变。package.json 新增 `check:publication-gate`，不新增依赖。</action>
  <verify>
    <automated>pnpm check:migrations -- --fixture-isolation</automated>
  </verify>
  <acceptance_criteria>
    - "fixture 模式拒绝 data/fpkg.db，也拒绝与 TURSO_DATABASE_URL 混用。"
    - "check:publication-gate 使用临时 file database，并在成功与失败路径都删除临时目录。"
    - "playwright.config.ts 的本地 webServer 通过 publication fixture harness 启动，不直接连接真实目录库。"
    - "pnpm check:migrations -- --fixture-isolation 退出码为 0。"
  </acceptance_criteria>
  <done>DB contract 与 Playwright 具备同一条可注入、可清理、不会触碰 data/fpkg.db 的隔离路径。</done>
</task>

<task type="auto">
  <name>Task 3: 用静态与行为测试封死 migration 旁路</name>
  <files>scripts/check-migration-safety.ts, scripts/check-publication-gate.ts</files>
  <read_first>scripts/check-migration-safety.ts, scripts/check-publication-gate.ts, src/lib/db.ts, scripts/import-csv.ts, scripts/import-markdown.ts, scripts/seed-concepts.ts, .planning/phases/18-publication-gate/18-PATTERNS.md</read_first>
  <action>扩展 `check-migration-safety.ts`，全量扫描 `scripts/**/*.ts` 的 migration table 写入、migration 目录枚举、migration SQL 拆分/执行与自制 marker 逻辑；只允许 `src/lib/db.ts` 拥有 SQL+marker 原子实现，允许 `scripts/migrate.ts` 调用 canonical API，历史 `migrations/*.sql` 和 dedicated test fixture 不纳入 source ownership 告警。增加行为 fixture：创建少一个 pending migration 的临时库，运行一个已收敛 importer 的最小本地输入，断言它在业务写入前失败、缺失 migration 无 marker、原有 marker/checksum 不变。保留既有 replay、idempotency、checksum drift、rollback、readiness 与 foreign key checks；`--fixture-isolation` 额外证明临时库路径与 `data/fpkg.db` 的 stat/hash 均未变化。</action>
  <verify>
    <automated>pnpm check:migrations &amp;&amp; pnpm check:migrations -- --fixture-isolation</automated>
  </verify>
  <acceptance_criteria>
    - "ownership scan 覆盖 scripts 全量，而不是硬编码研究清单。"
    - "缺 migration 的 importer fixture 在任何业务 row 或 marker 写入前失败。"
    - "canonical runner 的 replay、checksum、rollback 与 readiness 既有断言继续通过。"
    - "两条 18-VALIDATION.md 自动命令均退出码为 0，data/fpkg.db 未变化。"
  </acceptance_criteria>
  <done>任何新脚本重新取得 migration ownership 都会被静态 gate 阻止，现有 importer 缺 schema 时也会由行为测试 fail closed。</done>
</task>

</tasks>

## Artifacts This Plan Produces

- `scripts/check-publication-gate.ts`
- package script `check:publication-gate`
- environment contract `FPKG_DATABASE_URL`
- fixture guard `PUBLICATION_GATE_FIXTURE=1`
- CLI flags `--fixture-isolation`, `--serve-e2e`, `--port`
- migration-writer ownership scan in `scripts/check-migration-safety.ts`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| importer/seed → database | 本地脚本可写业务数据，但不能伪造 schema 已迁移状态 |
| test runner → local filesystem | fixture 可创建/删除临时数据库，但不能触碰真实目录库 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-01 | Tampering | embedded migration runners | high | mitigate | 全量 ownership scan + missing-migration 行为 fixture，禁止 importer 写 marker |
| T-18-02 | Tampering | Playwright/contract fixture path | high | mitigate | fixture mode 拒绝真实 DB 路径，成功/失败均核对目录库未变 |
| T-18-03 | Denial of Service | leaked child process/temp DB | medium | mitigate | signal/finally cleanup，关闭 client/server 后删除 temp root |
</threat_model>

<verification>
- `pnpm check:migrations`
- `pnpm check:migrations -- --fixture-isolation`
- `pnpm check:data-contract`
- `pnpm build`
- 确认 git diff 只包含本计划列出的本地文件；不得运行 `migrate:remote`、push 或 deploy。
</verification>

<success_criteria>
- `src/lib/db.ts` 是唯一 migration SQL+marker owner。
- 全部旁路 runner 已按 importer/seed 角色收敛，明确覆盖 seed-concepts、import-csv、import-markdown。
- 缺 migration 的 importer 不写 marker、不写业务数据。
- contract 与浏览器 fixture 可在 disposable DB 上运行，`data/fpkg.db` 前后不变。
- 本计划不安装 package，不连接 Turso，不部署生产。
</success_criteria>

<output>
Create `.planning/phases/18-publication-gate/18-01-SUMMARY.md` when done.
</output>
