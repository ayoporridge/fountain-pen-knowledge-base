---
phase: 18-publication-gate
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/import-brand-completion-sources.ts
  - scripts/import-exhibit-content.ts
  - scripts/import-model-gap-sources.ts
  - scripts/import-official-brand-sources.ts
  - scripts/import-official-brand-stories.ts
  - scripts/import-official-model-diagrams.ts
  - scripts/import-official-model-sources.ts
  - scripts/import-research-gap-sources.ts
  - scripts/import-warm-pen-atlas-media.ts
  - scripts/import-wikidata-brands.ts
  - scripts/check-migration-safety.ts
autonomous: true
requirements:
  - PUB-01
  - PUB-06
must_haves:
  truths:
    - "十个 importer 在任何业务写入前只调用 canonical readiness guard，不能执行 migration SQL 或写 marker。"
    - "migration ownership checker 全量扫描 scripts，新增旁路会确定性失败。"
    - "缺 pending migration 的 importer 在业务写入前失败，且 migrations marker 不变。"
  artifacts:
    - path: "scripts/check-migration-safety.ts"
      provides: "migration ownership static/behavior gate"
  key_links:
    - from: "ten importer scripts"
      to: "src/lib/db.ts::assertDatabaseReady"
      via: "write 前只读 preflight"
      pattern: "assertDatabaseReady"
---

<objective>
先收回十个 importer 的 migration ownership，并用静态与行为 gate 防止 false-applied migration 回归。

Purpose: 在引入 030 前消除“marker 已写、SQL 未执行”的最高风险路径。
Output: 十个 importer 收敛到 assertDatabaseReady，ownership scan 与 missing-migration regression。
</objective>

<execution_context>
@/Users/xz/.codex/gsd-core/workflows/execute-plan.md
@/Users/xz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/18-publication-gate/18-CONTEXT.md
@.planning/phases/18-publication-gate/18-RESEARCH.md
@.planning/phases/18-publication-gate/18-PATTERNS.md
@.planning/phases/18-publication-gate/18-VALIDATION.md
@src/lib/db.ts
@scripts/migrate.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 将十个 importer 改为 readiness-only writer</name>
  <files>scripts/import-brand-completion-sources.ts, scripts/import-exhibit-content.ts, scripts/import-model-gap-sources.ts, scripts/import-official-brand-sources.ts, scripts/import-official-brand-stories.ts, scripts/import-official-model-diagrams.ts, scripts/import-official-model-sources.ts, scripts/import-research-gap-sources.ts, scripts/import-warm-pen-atlas-media.ts, scripts/import-wikidata-brands.ts</files>
  <read_first>scripts/import-brand-completion-sources.ts, scripts/import-exhibit-content.ts, scripts/import-model-gap-sources.ts, scripts/import-official-brand-sources.ts, scripts/import-official-brand-stories.ts, scripts/import-official-model-diagrams.ts, scripts/import-official-model-sources.ts, scripts/import-research-gap-sources.ts, scripts/import-warm-pen-atlas-media.ts, scripts/import-wikidata-brands.ts, src/lib/db.ts, scripts/migrate.ts</read_first>
  <action>逐文件删除自行枚举 migration、拆分/执行 SQL、写入或补齐 migrations marker/checksum 的 compatibility runner。每个 importer 创建 client 后、任何 transaction 或业务 INSERT/UPDATE 前调用 `assertDatabaseReady(client)`；缺 schema 时立即退出且不修 marker。保留现有业务导入语义和无 write retry 边界，不改 seed、CSV/Markdown importer 或历史 migration。</action>
  <verify>
    <automated>pnpm check:migrations</automated>
  </verify>
  <acceptance_criteria>
    - "十个目标文件均在首个业务写入前调用 assertDatabaseReady(client)。"
    - "十个目标文件均不再读取 migrations 目录、execute migration SQL 或直接写 migrations 表。"
    - "pnpm check:migrations 退出码为 0。"
  </acceptance_criteria>
  <done>十个 false-apply importer 全部成为 readiness-only writer。</done>
</task>

<task type="auto">
  <name>Task 2: 增加 migration ownership static 与 behavior gate</name>
  <files>scripts/check-migration-safety.ts</files>
  <read_first>scripts/check-migration-safety.ts, scripts/import-brand-completion-sources.ts, scripts/import-exhibit-content.ts, scripts/import-model-gap-sources.ts, scripts/import-official-brand-sources.ts, scripts/import-official-brand-stories.ts, scripts/import-official-model-diagrams.ts, scripts/import-official-model-sources.ts, scripts/import-research-gap-sources.ts, scripts/import-warm-pen-atlas-media.ts, scripts/import-wikidata-brands.ts, src/lib/db.ts</read_first>
  <action>扩展 checker 的 `--migration-ownership` 分支，全量扫描 `scripts/**/*.ts` 的 migrations table 写入、migration 目录枚举和 migration SQL 执行；只允许 `src/lib/db.ts` 拥有 SQL+marker 原子实现，`scripts/migrate.ts` 只能调用 canonical API，历史 `migrations/*.sql` 与 dedicated fixture 排除。增加 synthetic missing-migration behavior fixture，证明代表 importer 在业务写入前失败且 marker/checksum/业务 row 不变。保留既有 replay、checksum、rollback 与 readiness tests。</action>
  <verify>
    <automated>pnpm check:migrations -- --migration-ownership</automated>
  </verify>
  <acceptance_criteria>
    - "ownership scan 覆盖 scripts 全量，不只硬编码十个文件。"
    - "missing-migration fixture 不新增 marker 或业务 row。"
    - "pnpm check:migrations -- --migration-ownership 退出码为 0。"
  </acceptance_criteria>
  <done>任何 importer 再取得 migration ownership 都会在 Plan 02 前被阻止。</done>
</task>

</tasks>

## Artifacts This Plan Produces

- CLI flag `--migration-ownership`
- migration writer ownership scan
- missing-migration importer regression

<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation |
|---|---|---|---|---|---|
| T-18-01 | Tampering | importer migration marker | high | mitigate | readiness-only writers + full ownership scan |
</threat_model>

<verification>
- `pnpm check:migrations`
- `pnpm check:migrations -- --migration-ownership`
- 不运行 remote migration、push 或 deploy。
</verification>

<success_criteria>
- 十个 importer 全部收敛。
- checker 可阻止任何 scripts migration bypass。
- 本计划只修改 11 个文件；每个 task 少于 15 个 files。
</success_criteria>

<output>
Create `.planning/phases/18-publication-gate/18-01-SUMMARY.md` when done.
</output>
