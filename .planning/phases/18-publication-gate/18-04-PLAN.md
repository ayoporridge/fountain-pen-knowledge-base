---
phase: 18-publication-gate
plan: "04"
type: execute
wave: 4
depends_on:
  - "18-03"
files_modified:
  - src/lib/public-visibility.ts
  - src/app/[type]/[slug]/page.tsx
  - src/app/sitemap.ts
  - scripts/migrate.ts
  - src/app/api/entities/route.ts
  - src/app/api/entities/[slug]/route.ts
  - src/app/api/entities/[slug]/preview/route.ts
  - src/middleware.ts
  - scripts/check-public-boundary.ts
autonomous: true
requirements:
  - PUB-03
  - PUB-04
  - PUB-05
must_haves:
  truths:
    - "canonical helper、detail、metadata、sitemap 与 entity APIs 只由 public_entities 授权。"
    - "legacy identity 先 redirect；未发布 canonical target 404 且无 indexable metadata。"
    - "全部本计划 entity-bearing surface dynamic/no-store。"
    - "public entity APIs 不泄漏 publication internals 或 DB id。"
  artifacts:
    - path: "src/lib/public-visibility.ts"
      provides: "alias-safe public view filter and direct lookup"
  key_links:
    - from: "detail/metadata/sitemap/entities APIs"
      to: "public_entities"
      via: "publicEntityFilter or getPublicEntityBySlug"
      pattern: "public_entities"
---

<objective>
接通 canonical public helper 与核心逐 ID/list surfaces，并在修改 surface 时同步落实 no-store。

Purpose: 先封住详情、metadata、sitemap 与 entity APIs 的直接泄漏路径。
Output: public helper、redirect-first 404、core API allowlist、core no-store checks。
</objective>

<execution_context>
@/Users/xz/.codex/gsd-core/workflows/execute-plan.md
@/Users/xz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/18-publication-gate/18-CONTEXT.md
@.planning/phases/18-publication-gate/18-PATTERNS.md
@.planning/phases/18-publication-gate/18-VALIDATION.md
@.planning/phases/18-publication-gate/18-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 修复隔离 migration 入口并接通 public helper、detail/metadata、sitemap 与 middleware</name>
  <files>scripts/migrate.ts, src/lib/public-visibility.ts, src/app/[type]/[slug]/page.tsx, src/app/sitemap.ts, src/middleware.ts</files>
  <read_first>scripts/migrate.ts, src/lib/db.ts, src/lib/public-visibility.ts, src/app/[type]/[slug]/page.tsx, src/app/sitemap.ts, src/middleware.ts, migrations/030_publication_gate.sql</read_first>
  <action>先修复本轮执行发现的安全缺口：local `scripts/migrate.ts` 必须复用 `resolveDatabaseConnection()` 并尊重 `FPKG_DATABASE_URL` / `PUBLICATION_GATE_FIXTURE`，不得硬编码回真实 `data/fpkg.db`；`--core-detail` 用子进程回归证明 disposable DB 收到 030、真实 DB 主文件 stat/hash 不变且临时目录清理。随后保留 alias regex，将 `publicEntityFilter(alias)` 改为 EXISTS public_entities；新增只返回 public columns 的 `getPublicEntityBySlug(type,slug)`，isPublicEntity 不再授权 raw brand/pen。metadata/page 保持 canonical redirect 在先，之后共同 direct lookup，未发布 target notFound；related/parent aliases 同样 gate，404 分支不生成 canonical/OG/JSON-LD。sitemap dynamic entity rows只读 view且不 catch gate error。detail 与 sitemap 使用 `dynamic = "force-dynamic"` 并移除 revalidate。middleware 只保留 namespace/tool/canonical 辅助，不再用 slug blacklist 授权 brand/pen。</action>
  <verify>
    <automated>pnpm check:public-boundary -- --core-detail</automated>
  </verify>
  <acceptance_criteria>
    - "detail/metadata 对每个 fixture id 与 public_entities 可达性等价。"
    - "local migrate fixture 只写 disposable FPKG_DATABASE_URL，真实 data/fpkg.db 主文件 stat/hash 不变。"
    - "legacy redirect 后未发布 canonical target 最终 404。"
    - "detail/sitemap 无 ISR TTL，未发布页无 canonical/OG/JSON-LD。"
  </acceptance_criteria>
  <done>core page/metadata/sitemap 均使用 canonical gate 且 no-store。</done>
</task>

<task type="auto">
  <name>Task 2: 接通 entity list/detail/preview APIs 与 core checks</name>
  <files>src/app/api/entities/route.ts, src/app/api/entities/[slug]/route.ts, src/app/api/entities/[slug]/preview/route.ts, scripts/check-public-boundary.ts</files>
  <read_first>src/app/api/entities/route.ts, src/app/api/entities/[slug]/route.ts, src/app/api/entities/[slug]/preview/route.ts, scripts/check-public-boundary.ts, src/lib/public-visibility.ts</read_first>
  <action>entities list/detail/hover-preview 全部从 canonical view/helper 获取，返回现有 exact DTO；deny entity_id/status/depth/score/blockers/hash/revision/contract/reviewer/timestamps/notes。三个 GET route 显式返回 `Cache-Control: no-store`。扩展 core checker，覆盖 list 双向 equality、detail/preview 逐 ID equivalence、API exact keys、publication denylist、detail/sitemap/API no-store；expected fixtures 不复用 runtime helper。</action>
  <verify>
    <automated>pnpm check:public-boundary -- --core-api-cache</automated>
  </verify>
  <acceptance_criteria>
    - "entities list 与 public_entities 双向集合相等。"
    - "detail/preview APIs 逐 ID 可达性等价并保持 exact DTO。"
    - "三个 API 都返回 no-store，publication internals denylist 零命中。"
  </acceptance_criteria>
  <done>核心 public API 与 public view 同源且无缓存/字段泄漏。</done>
</task>

</tasks>

## Artifacts This Plan Produces

- canonical `publicEntityFilter(alias)`
- `getPublicEntityBySlug(type, slug)`
- checker flags `--core-detail`, `--core-api-cache`

<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation |
|---|---|---|---|---|---|
| T-18-06 | Information Disclosure | detail/entity APIs | high | mitigate | direct view lookup + exact DTO + no-store |
</threat_model>

<verification>
- `pnpm check:public-boundary -- --core-detail`
- `pnpm check:public-boundary -- --core-api-cache`
- `pnpm check:migrations && pnpm check:data-contract && pnpm check:publication-gate && pnpm build`
- 不运行 remote migration、push 或 deploy。
</verification>

<success_criteria>
- 核心 8 个文件形成同一 public gate。
- 每个修改 surface 同步 dynamic/no-store。
- 本计划及每个 task 均少于 15 个 files。
</success_criteria>

<output>
Create `.planning/phases/18-publication-gate/18-04-SUMMARY.md` when done.
</output>
