---
phase: 18-publication-gate
plan: "04"
type: execute
wave: 4
depends_on:
  - "18-03"
files_modified:
  - scripts/check-publication-gate.ts
  - scripts/check-public-boundary.ts
  - tests/e2e/publication-gate.spec.ts
  - tests/e2e/site-quality.spec.ts
  - tests/e2e/library.spec.ts
autonomous: true
requirements:
  - PUB-01
  - PUB-02
  - PUB-03
  - PUB-04
  - PUB-05
  - PUB-06
must_haves:
  truths:
    - "fresh replay、upgrade、idempotency、constraints、backfill、type transition、invalidation 与 publish 由 disposable DB 独立复验。"
    - "browse/sitemap/entities list 使用双向集合相等；detail/metadata/API 使用逐 ID 等价；facets/statistics 使用聚合等价；graph/recommend/library 使用 public 子集语义。"
    - "expected oracle 不导入 runtime authorization helper，不会因 expected/actual 共享 bug 形成 tautology。"
    - "Montblanc 149 draft shell 在全部 public surface 缺席且直接 404；synthetic valid brand/pen 在全部适用 surface 同现。"
    - "旧 `>500`/`>=550`、真实 Pilot/LAMY 必须公开、允许 shared cache 等错误回归已被语义断言替换。"
    - "完整 local regression 通过，且 Phase 18 不执行 remote migration、push 或 deploy。"
  artifacts:
    - path: "scripts/check-publication-gate.ts"
      provides: "完整 migration/publication/compatibility matrix"
    - path: "scripts/check-public-boundary.ts"
      provides: "独立 expected oracle 与四类 surface parity"
    - path: "tests/e2e/publication-gate.spec.ts"
      provides: "fail-closed/valid/transition browser and API regression"
    - path: "tests/e2e/site-quality.spec.ts"
      provides: "精确集合与全量 traversal 的新回归契约"
  key_links:
    - from: "independent contract SQL"
      to: "public HTTP surfaces"
      via: "bidirectional/per-ID/aggregate/subset comparisons"
      pattern: "EXCEPT"
    - from: "tests/e2e/publication-gate.spec.ts"
      to: "scripts/check-publication-gate.ts fixture server"
      via: "Playwright isolated database harness"
      pattern: "publication-gate"
---

<objective>
用独立 oracle、synthetic fixtures 与重写后的全量 E2E 证明 Phase 18 的数据库和每个 public surface 契约真实成立，而不是靠数量阈值、真实目录行或同源 helper 自证。

Purpose: 为 PUB-01..PUB-06 建立可重复的最终验收，修掉旧 suite 与“migration 后 brand/pen 全 draft”之间的必然冲突。
Output: 完整 migration/publication matrix、独立四语义 parity、Montblanc 149 fail-closed E2E、valid fixture E2E、旧 site/library regression 改写与 full local gate。
</objective>

<execution_context>
@/Users/xz/.codex/gsd-core/workflows/execute-plan.md
@/Users/xz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-publication-gate/18-CONTEXT.md
@.planning/phases/18-publication-gate/18-RESEARCH.md
@.planning/phases/18-publication-gate/18-PATTERNS.md
@.planning/phases/18-publication-gate/18-VALIDATION.md
@.planning/phases/18-publication-gate/18-03-SUMMARY.md
@scripts/check-publication-gate.ts
@scripts/check-public-boundary.ts
@tests/e2e/site-quality.spec.ts
@tests/e2e/library.spec.ts

<interfaces>
Expected-side parity must use independent contract SQL/fixture facts and may not import `publicEntityFilter`, `getPublicEntityBySlug`, `isPublicEntity`, browse helpers, recommendation helpers, or library public predicates.
The E2E server is the disposable fixture harness created in Plan 01 and populated through Plan 02's real migration/publication transaction.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 完成 migration/publication matrix 与 non-brand compatibility</name>
  <files>scripts/check-publication-gate.ts</files>
  <read_first>scripts/check-publication-gate.ts, migrations/030_publication_gate.sql, src/lib/publication.ts, src/lib/db.ts, migrations/023_retire_template_stories.sql, .planning/phases/18-publication-gate/18-VALIDATION.md</read_first>
  <action>补全 disposable fixtures：empty DB fresh replay、当前 `data/fpkg.db` 的临时副本 upgrade（只复制，原库前后 stat/hash 不变）、second-run idempotency/checksum、`PRAGMA quick_check`、`foreign_key_check`、critical sqlite_schema objects 与 illegal CHECK/trigger writes。对 migration 前后所有 deprecated brand/model story 保存 id/type/status/body hash 并逐行 diff；断言每个既有 brand/pen 有 draft row、published=0、无 entity 内容变化。覆盖 entity create/type transition 四态、每类 critical input I/U/D、direct SQL guard、transaction rollback。compatibility expected side 在 upgrade 前保存非-brand/pen legacy public set，upgrade 后以双向 `EXCEPT` 证明零 diff，并验证显式 publication row 会切换到 strict gate。所有子命令汇总全部 failures 后一次退出，不能只报第一项。</action>
  <verify>
    <automated>pnpm check:publication-gate -- --migration-full &amp;&amp; pnpm check:publication-gate -- --compatibility</automated>
  </verify>
  <acceptance_criteria>
    - "fresh、upgrade、idempotent、quick_check、foreign_key_check、schema object 与 constraint matrix 全绿。"
    - "deprecated stories 逐行 diff 为 0；brand/pen draft backfill 完整且 published 为 0。"
    - "非-brand/pen legacy/public 新集合两个 EXCEPT 方向均为空。"
    - "两条 18-VALIDATION.md 自动命令均退出码为 0，真实 data/fpkg.db 未变化。"
  </acceptance_criteria>
  <done>PUB-01/PUB-02/PUB-05/PUB-06 在 fresh 与 production-shaped upgrade fixture 上都可独立复验。</done>
</task>

<task type="auto">
  <name>Task 2: 建立不复用 runtime predicate 的四语义 parity oracle</name>
  <files>scripts/check-public-boundary.ts</files>
  <read_first>scripts/check-public-boundary.ts, src/lib/public-visibility.ts, src/lib/browse-data.ts, src/lib/recommend.ts, src/lib/library.ts, src/app/sitemap.ts, .planning/phases/18-publication-gate/18-RESEARCH.md</read_first>
  <action>重写 expected side，使它直接读取 fixture/publication contract SQL 与已知 fixture ids，禁止导入任何 runtime authorization predicate/helper。按 surface 语义分别验收：browse rows、sitemap dynamic entity URLs、entities list API 与 public_entities 做双向集合相等；detail、metadata、entity detail API、preview 对每个 governed fixture 做可达性等价；browse facets/type counts、home/by-dimension statistics 做按键聚合等价；graph hubs/selected/neighbors、links two-hop、recommend candidates、representative models、wiki/concept/exhibit/timeline/diagram/source/media results 逐项断言为 public_entities 子集，并验证适用 fixture 不被错误遗漏。每个 alias/owner/neighbor 单独记录 mismatch；API exact-key allowlist 与 publication-internal denylist纳入同一 `--all` 报告。删除 sitemap 数量 KPI，集合差异输出 actual-only/expected-only ids。</action>
  <verify>
    <automated>pnpm check:public-boundary -- --all</automated>
  </verify>
  <acceptance_criteria>
    - "完整列表使用双向 equality，逐 ID、aggregate、context subset 不被错误混成一种比较。"
    - "expected oracle 不导入 runtime public visibility/browse/recommend/library helper。"
    - "任何 unpublished owner/neighbor/candidate 或 publication internal field 都形成明确 failure。"
    - "pnpm check:public-boundary -- --all 退出码为 0。"
  </acceptance_criteria>
  <done>PUB-03/PUB-04 的每类 surface 都由独立、语义正确、可定位差异的 oracle 覆盖。</done>
</task>

<task type="auto">
  <name>Task 3: 新增 publication E2E、重写旧假设并运行完整 local regression</name>
  <files>tests/e2e/publication-gate.spec.ts, tests/e2e/site-quality.spec.ts, tests/e2e/library.spec.ts</files>
  <read_first>tests/e2e/site-quality.spec.ts, tests/e2e/library.spec.ts, playwright.config.ts, scripts/check-publication-gate.ts, scripts/check-public-boundary.ts, .planning/phases/18-publication-gate/18-VALIDATION.md</read_first>
  <action>新增 `publication-gate.spec.ts`，只使用隔离 synthetic fixtures：Montblanc 149 保持 deliberate draft/no-content，断言 detail 404、metadata 无 canonical/OG/JSON-LD、browse/sitemap/entities/graph/links/recommend/library/wiki/media 全缺席；完整 valid brand/pen 通过真实 publish transaction 后在所有适用 surface 同现且 DTO allowlist 正确。覆盖 draft 404→publish 200、published→summary/story/spec/source/license/variant/primary-media/made_by critical edit 或 retire→下一请求 404、重新审核→200。改写 site-quality/library 旧契约：删除 `>500`/`>=550` 数量阈值、Pilot/LAMY/真实目录行天然 200、无规格仍公开、browse shared cache 必须存在等断言；改为 exact public set、dynamic/no-store、全量 sitemap traversal 与 failure aggregation。desktop 与 mobile 都使用 disposable DB；不得通过 retry、skip 或修改真实 catalog row 获得绿色结果。最后按 18-VALIDATION.md 完整顺序运行所有 local gates。</action>
  <verify>
    <automated>pnpm exec playwright test tests/e2e/publication-gate.spec.ts --project=desktop &amp;&amp; pnpm check:migrations &amp;&amp; pnpm check:data-contract &amp;&amp; pnpm check:publication-gate &amp;&amp; pnpm check:public-boundary &amp;&amp; pnpm check:library &amp;&amp; pnpm lint &amp;&amp; pnpm build &amp;&amp; pnpm test:e2e:desktop &amp;&amp; pnpm test:e2e:mobile</automated>
  </verify>
  <acceptance_criteria>
    - "Montblanc 149 draft fixture 在所有 public surface 缺席；valid fixture 在所有适用 surface 同现。"
    - "旧数量阈值、真实目录 publication 假设与 shared-cache expectation 已被精确语义断言替换。"
    - "desktop/mobile 不使用 retry 或 skip，Playwright 运行前后 data/fpkg.db 未变化。"
    - "18-VALIDATION.md targeted 与 full-suite 命令全部退出码为 0。"
  </acceptance_criteria>
  <done>四类 parity、fail-closed browser behavior、transition immediacy 与全部既有 local regression 同时通过。</done>
</task>

</tasks>

## Artifacts This Plan Produces

- `tests/e2e/publication-gate.spec.ts`
- independent parity oracle in `scripts/check-public-boundary.ts`
- CLI flags `--migration-full`, `--compatibility`, `--all`
- synthetic Montblanc 149 draft fixture and valid brand/pen fixtures
- rewritten exact-set/no-store regressions in `site-quality.spec.ts` and `library.spec.ts`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime implementation → verification oracle | expected 与 actual 不能共享 authorization bug |
| test harness → catalog/database | E2E 只能写 disposable fixture，不能改变真实目录 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-10 | Repudiation | tautological parity result | high | mitigate | independent contract SQL/fixture ids，禁止导入 runtime predicates |
| T-18-11 | Tampering | real catalog fixture mutation | high | mitigate | isolated server harness + pre/post real DB hash |
| T-18-12 | Information Disclosure | partial/subset surface leak | high | mitigate | 四语义全量 comparisons + actual-only mismatch reporting |
</threat_model>

<verification>
- `pnpm check:publication-gate -- --migration-full`
- `pnpm check:publication-gate -- --compatibility`
- `pnpm check:public-boundary -- --all`
- `pnpm exec playwright test tests/e2e/publication-gate.spec.ts --project=desktop`
- `pnpm check:migrations && pnpm check:data-contract && pnpm check:publication-gate && pnpm check:public-boundary && pnpm check:library && pnpm lint && pnpm build && pnpm test:e2e:desktop && pnpm test:e2e:mobile`
- 检查 git diff/命令历史：不得出现 `migrate:remote`、push、Vercel deploy 或 production alias 变更。
</verification>

<success_criteria>
- PUB-01..PUB-06 均有独立 automated evidence。
- 双向集合、逐 ID、聚合、上下文子集四种语义分别通过。
- Montblanc 149 fail-closed 与 valid fixture 正向发布/失效/重发完整通过。
- 所有旧数量/真实目录/缓存错误假设已重写，完整 desktop/mobile regression 通过。
- Phase 18 仅产生本地代码与 disposable DB evidence；远程数据库和生产站保持不变。
</success_criteria>

<output>
Create `.planning/phases/18-publication-gate/18-04-SUMMARY.md` when done.
</output>
