---
phase: 18-publication-gate
plan: "06"
type: execute
wave: 6
depends_on:
  - "18-05"
files_modified:
  - src/lib/recommend.ts
  - src/lib/concept-engine.ts
  - src/components/MarkdownRenderer.tsx
  - src/lib/library.ts
  - src/app/api/image-proxy/route.ts
  - src/app/library/page.tsx
  - src/app/library/sources/page.tsx
  - src/app/library/diagrams/page.tsx
  - src/app/exhibits/page.tsx
  - src/app/exhibits/[slug]/page.tsx
  - src/app/timeline/page.tsx
  - scripts/check-public-boundary.ts
autonomous: true
requirements:
  - PUB-03
  - PUB-04
  - PUB-05
must_haves:
  truths:
    - "recommend、concept cache 与 wiki resolution 只产生 public entity targets。"
    - "library representative/source/media/diagram links 对 candidate 与 owner 都 gate。"
    - "exhibit/timeline JSON 内的实体引用解析后 gate。"
    - "library、source、diagram、exhibit、timeline 与 image response 随修改同步 dynamic/no-store。"
  artifacts:
    - path: "src/lib/library.ts"
      provides: "owner-aware secondary public discovery"
  key_links:
    - from: "secondary links/media/source records"
      to: "public entity owner"
      via: "owner-aware join and canonical slug resolution"
      pattern: "publicEntityFilter"
---

<objective>
完成 recommend、concept/wiki、library/source/media、exhibit/timeline 等 secondary surface 的 canonical gate。

Purpose: 防止 unpublished entity 经候选、缓存、媒体 owner 或 JSON 内嵌链接间接泄漏。
Output: secondary public-only queries、owner-aware image proxy、secondary no-store checks。
</objective>

<execution_context>
@/Users/xz/.codex/gsd-core/workflows/execute-plan.md
@/Users/xz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/18-publication-gate/18-CONTEXT.md
@.planning/phases/18-publication-gate/18-RESEARCH.md
@.planning/phases/18-publication-gate/18-PATTERNS.md
@.planning/phases/18-publication-gate/18-VALIDATION.md
@.planning/phases/18-publication-gate/18-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: gate recommendations、concept cache 与 wiki links</name>
  <files>src/lib/recommend.ts, src/lib/concept-engine.ts, src/components/MarkdownRenderer.tsx</files>
  <read_first>src/lib/recommend.ts, src/lib/concept-engine.ts, src/components/MarkdownRenderer.tsx, src/lib/public-visibility.ts</read_first>
  <action>recommend current/direct/model/tag/brand candidate 每个 alias 都 gate。concept recompute 只写 public matches，read 再 gate，避免 publication transition 后 materialized stale row 泄漏；明确 publish 后缺少 match 属于可见性完整性而非授权旁路。Markdown wiki slug 只解析 canonical public entity，unpublished target 退化为非链接文字。checker 使用 subset/target-resolution 语义覆盖 stale match、unpublished candidate 与 wiki target。</action>
  <verify>
    <automated>pnpm check:public-boundary -- --secondary-links</automated>
  </verify>
  <acceptance_criteria>
    - "recommend 返回项、concept match target、wiki link target 全是 public_entities 子集。"
    - "stale concept row 和 unpublished slug 不产生公开链接。"
    - "pnpm check:public-boundary -- --secondary-links 退出码为 0。"
  </acceptance_criteria>
  <done>recommend/concept/wiki 不再绕过 canonical gate。</done>
</task>

<task type="auto">
  <name>Task 2: gate library、source、diagram 与 media owners</name>
  <files>src/lib/library.ts, src/app/api/image-proxy/route.ts, src/app/library/page.tsx, src/app/library/sources/page.tsx, src/app/library/diagrams/page.tsx</files>
  <read_first>src/lib/library.ts, src/app/api/image-proxy/route.ts, src/app/library/page.tsx, src/app/library/sources/page.tsx, src/app/library/diagrams/page.tsx, src/lib/public-visibility.ts</read_first>
  <action>library representative models、featured/parent brands、source index 的每个 UNION owner、media owner、diagram/entity path 全部 gate；section JSON 中 slug resolve 后再 gate。`getLibraryCoverageReport()` 保持 private/admin raw-backlog helper，确认 public route 不可达。image proxy 按 media id join public owner，unpublished owner 404，并将 success/cache headers 改为 no-store。library、sources、diagrams pages force-dynamic，移除 revalidate。</action>
  <verify>
    <automated>pnpm check:public-boundary -- --secondary-library-media</automated>
  </verify>
  <acceptance_criteria>
    - "representative/source/media/diagram 每个 owner 与 target 都属于 public_entities。"
    - "unpublished media owner image proxy 返回 404 且 response no-store。"
    - "library/source/diagram pages force-dynamic。"
  </acceptance_criteria>
  <done>library 与媒体二级入口实现 owner-aware fail-closed。</done>
</task>

<task type="auto">
  <name>Task 3: gate exhibit/timeline 内嵌实体并完成 secondary cache policy</name>
  <files>src/app/exhibits/page.tsx, src/app/exhibits/[slug]/page.tsx, src/app/timeline/page.tsx, scripts/check-public-boundary.ts</files>
  <read_first>src/app/exhibits/page.tsx, src/app/exhibits/[slug]/page.tsx, src/app/timeline/page.tsx, scripts/check-public-boundary.ts, src/lib/library.ts</read_first>
  <action>exhibit list/detail 与 timeline 中所有 relation、section JSON、entity slug/path 解析后只输出 public target；非实体内容保持既有公开行为。三个 page force-dynamic 并移除 revalidate。扩展 secondary/cache checker，覆盖本计划全部 11 个运行时文件不存在 entity-bearing TTL/SWR，critical edit/retire 后下一请求无法通过 recommend/wiki/library/media/exhibit/timeline 找到目标。</action>
  <verify>
    <automated>pnpm check:public-boundary -- --secondary-exhibit-timeline-cache</automated>
  </verify>
  <acceptance_criteria>
    - "exhibit/timeline 所有 entity targets 都是 public_entities 子集。"
    - "本计划修改的所有 entity-bearing page/API 无 TTL/SWR。"
    - "pnpm check:public-boundary -- --secondary-exhibit-timeline-cache 退出码为 0。"
  </acceptance_criteria>
  <done>全部 secondary surface 与 cache policy 完成 canonical gate。</done>
</task>

</tasks>

## Artifacts This Plan Produces

- checker flags `--secondary-links`, `--secondary-library-media`, `--secondary-exhibit-timeline-cache`
- owner-aware image proxy authorization

<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation |
|---|---|---|---|---|---|
| T-18-08 | Information Disclosure | secondary owner/target | high | mitigate | owner-aware joins + subset/cache checks |
</threat_model>

<verification>
- `pnpm check:public-boundary -- --secondary-links`
- `pnpm check:public-boundary -- --secondary-library-media`
- `pnpm check:public-boundary -- --secondary-exhibit-timeline-cache`
- `pnpm check:library && pnpm lint && pnpm build`
- 不运行 remote migration、push 或 deploy。
</verification>

<success_criteria>
- recommend/concept/wiki/library/source/media/exhibit/timeline 无 unpublished target。
- 所有修改 surface 随任务切换 dynamic/no-store。
- 本计划只修改 12 个文件；每个 task 少于 15 个 files。
</success_criteria>

<output>
Create `.planning/phases/18-publication-gate/18-06-SUMMARY.md` when done.
</output>
