---
phase: 18-publication-gate
plan: "02"
type: execute
wave: 2
depends_on:
  - "18-01"
files_modified:
  - migrations/030_publication_gate.sql
  - src/lib/db.ts
  - src/lib/publication.ts
  - scripts/check-publication-gate.ts
autonomous: true
requirements:
  - PUB-01
  - PUB-02
  - PUB-05
  - PUB-06
must_haves:
  truths:
    - "每个 brand/pen 都有显式 publication row，新建、迁移及 type transition 均默认 draft。"
    - "当前 contract version 的 readiness 是从当前原始数据计算的 fail-closed 结果，诊断 snapshot 不参与授权。"
    - "任何 publication-critical INSERT/UPDATE/DELETE 都递增 revision，并让已发布实体立即退出 public_entities。"
    - "只有 server-only transaction 与数据库 transition guard 同时通过时才能进入 published。"
    - "迁移后既有 brand/pen published 数为 0，deprecated story 的逐行状态与内容不变。"
  artifacts:
    - path: "migrations/030_publication_gate.sql"
      provides: "publication schema、computed readiness/public views、state-machine 与 invalidation triggers"
    - path: "src/lib/publication.ts"
      provides: "canonical hash 与 bounded server-only publish transaction"
    - path: "scripts/check-publication-gate.ts"
      provides: "migration、backfill、invalidation、publish 与 type-transition fixtures"
  key_links:
    - from: "publication-critical tables"
      to: "entity_publications.content_revision"
      via: "SQLite row triggers"
      pattern: "content_revision"
    - from: "src/lib/publication.ts"
      to: "public_entity_readiness/public_entities"
      via: "同一 write transaction 内复查 readiness 与最终 membership"
      pattern: "PUBLICATION_CONTRACT_VERSION"
    - from: "src/lib/db.ts::assertDatabaseReady"
      to: "migrations/030_publication_gate.sql objects"
      via: "版本感知 sqlite_schema object manifest"
      pattern: "sqlite_schema"
---

<objective>
实现 migration `030` 的 publication/readiness/hash/state-machine 数据契约，使 brand/pen 从数据库层开始 fail closed，并能被确定性失效和原子发布。

Purpose: 把“row 存在”与“可公开”彻底分离，满足 PUB-01、PUB-02、PUB-05、PUB-06，同时为 Phase 19 的下一 contract version 留下可升级边界。
Output: `entity_publications`、normalized blocker/readiness/public views、全部 invalidation/type-transition triggers、canonical hash、server-only publish transaction 与隔离 DB fixtures。
</objective>

<execution_context>
@/Users/xz/.codex/gsd-core/workflows/execute-plan.md
@/Users/xz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-publication-gate/18-CONTEXT.md
@.planning/phases/18-publication-gate/18-RESEARCH.md
@.planning/phases/18-publication-gate/18-PATTERNS.md
@.planning/phases/18-publication-gate/18-VALIDATION.md
@.planning/phases/18-publication-gate/18-01-SUMMARY.md
@src/lib/db.ts
@migrations/011_library_schema.sql
@migrations/023_retire_template_stories.sql
@scripts/check-publication-gate.ts

<interfaces>
Plan 01 provides canonical migration/readiness APIs and the disposable database harness.
This plan creates `PUBLICATION_CONTRACT_VERSION`, `computePublicationContentHash()`, `publishEntity()`, and `setEntityPublicationStatus()` in `src/lib/publication.ts`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 建立 030 schema、computed readiness 与完整 type state-machine</name>
  <files>migrations/030_publication_gate.sql, src/lib/db.ts, scripts/check-publication-gate.ts</files>
  <read_first>migrations/011_library_schema.sql, migrations/004_links.sql, migrations/006_fts.sql, migrations/023_retire_template_stories.sql, src/lib/db.ts, scripts/check-publication-gate.ts, .planning/phases/18-publication-gate/18-RESEARCH.md</read_first>
  <action>新增 `030_publication_gate.sql`，使用 plain SQLite/Turso-compatible DDL 创建 `entity_publications`（status 仅 `draft|in_review|published|retired`，默认 draft；revision/review/hash/contract/reviewer/timestamp 字段及 CHECK）、必要 indexes、normalized computed `publication_blockers` view、`public_entity_readiness` view 和显式列的 `public_entities` view。readiness contract version 固定为 `1`，结构 blockers 至少覆盖 publication 缺失、空 summary、正确类型 published story 不是恰好一篇、pen 的 `made_by` 不是恰好一条、hash 缺失/格式错误、reviewed/current revision 不等、reviewed contract version 不等、reviewer/review timestamp 缺失；status 本身不作为 readiness blocker。`blockers_json` 只保存带 contract/revision 的诊断 snapshot，public authorization 只读 computed views。public_entities 对 brand/pen 及任何显式 publication row 使用严格 gate；无 publication row 的其他类型精确保留旧公开行为。加入 entity INSERT/type UPDATE triggers：进入 brand/pen upsert/reset draft；brand↔pen 重置所有审核/hash；离开 brand/pen 保留 draft 非发布 row，不能删除后落入 legacy branch。动态 backfill 给全部既有 brand/pen 建 draft row，published 数为 0，禁止修改 stories。扩展 `assertDatabaseReady()`：只有所选 migrationsDir 实际包含 `030_publication_gate.sql` 时才要求对应 table/view/index/trigger manifest；marker 完整但对象缺失必须失败，自定义旧 fixture 不被误判。</action>
  <verify>
    <automated>pnpm check:publication-gate -- --migration &amp;&amp; pnpm check:publication-gate -- --backfill</automated>
  </verify>
  <acceptance_criteria>
    - "fresh replay、当前本地库副本 upgrade、二次运行与 custom pre-030 migrationsDir 均有确定性断言。"
    - "brand/pen 新建、转入、brand↔pen、转出四类 transition 均保持非 published 并清除旧审核授权。"
    - "030 前后 deprecated story 的 id/status/body hash 逐行相同，全部既有 brand/pen 为 draft，published 为 0。"
    - "缺少任一 030 critical object 时 assertDatabaseReady() 失败。"
  </acceptance_criteria>
  <done>030 在 fresh/upgrade 路径建立版本感知、fail-closed 的 publication schema，并且不会 grandfather 任何旧 brand/pen 或复活 deprecated story。</done>
</task>

<task type="auto">
  <name>Task 2: 固定 canonical hash 并覆盖全部 critical invalidation</name>
  <files>migrations/030_publication_gate.sql, src/lib/publication.ts, scripts/check-publication-gate.ts</files>
  <read_first>src/lib/db.ts, migrations/011_library_schema.sql, migrations/004_links.sql, migrations/006_fts.sql, .planning/phases/18-publication-gate/18-CONTEXT.md, .planning/phases/18-publication-gate/18-VALIDATION.md</read_first>
  <action>在 server-only `src/lib/publication.ts` 定义 canonical payload 与 `sha256:v1:` hash：固定 keys、显式 null、Unicode NFC、CRLF/CR 归一为 LF、数值/布尔类型固定、每个数组使用稳定复合 sort key、完全重复项确定性去重。输入覆盖 entity type/slug/name/summary、正确 published brand/model story、model_specs、model_variants、claims/citations、source_items 的 review/allowed-use、source_registry 的 reliability/license/independence、entity_references、timeline_events、approved primary media、`made_by` link。030 对 `entities`、`stories`、`model_specs`、`model_variants`、`claims`、`citations`、`source_items`、`source_registry`、`entity_references`、`timeline_events`、`media_assets`、`entity_links` 的相关 INSERT/UPDATE/DELETE 建立 fan-out triggers：原子递增受影响实体 `content_revision`，并将 published 改为 in_review；保留 approved hash 作审计证据。source/source_registry 变化必须沿 citation/reference/timeline 反向影响全部 owner，entity_links 仅在 publication-relevant owner/`made_by` 变化时触发。测试每张表三种 mutation、乱序查询 hash 稳定、每个 critical field 单独改变 hash、revision 单次递增且 visibility 立即消失。</action>
  <verify>
    <automated>pnpm check:publication-gate -- --invalidation</automated>
  </verify>
  <acceptance_criteria>
    - "canonical serializer 对相同语义的乱序输入产生相同 sha256:v1 hash。"
    - "12 类 publication-critical table 的 INSERT/UPDATE/DELETE 均有 fixture；source fan-out 不遗漏 owner。"
    - "critical edit 可以提交，但同一 transaction 后实体不再属于 public_entities。"
    - "诊断 blockers_json 的陈旧或伪造不能授予 public_entities membership。"
  </acceptance_criteria>
  <done>任一关键内容、证据、版本、来源许可、时间线、媒体或 made_by 变化都会可靠使旧审核失效，且 hash 可跨本地/libSQL 确定性复验。</done>
</task>

<task type="auto">
  <name>Task 3: 实现双重约束的原子 publish transition</name>
  <files>migrations/030_publication_gate.sql, src/lib/publication.ts, scripts/check-publication-gate.ts</files>
  <read_first>src/lib/publication.ts, src/lib/db.ts, migrations/030_publication_gate.sql, scripts/check-publication-gate.ts, .planning/phases/18-publication-gate/18-RESEARCH.md</read_first>
  <action>实现 bounded、无 retry 的 `publishEntity(db,{entityId,reviewer})` transaction：读取稳定输入并计算 hash；写 approved hash、reviewed revision、contract version、reviewer/reviewed timestamp；重查 computed readiness；只有 blocker_count=0 才切换 published/published_at；再断言 entity 已进入 public_entities；任何一步失败 rollback。`setEntityPublicationStatus()` 只允许 server 侧进入 draft/in_review/retired，不能绕过 publishEntity 进入 published。030 增加 BEFORE INSERT/UPDATE transition guard，任何直接 SQL 进入 published 都必须同时满足 status transition、readiness 零 blocker、current/reviewed revision 相等、contract version=1、`sha256:v1:` 小写 hex 格式、reviewer/reviewed_at/published_at 完整；非法 JSON/status/revision/hash 写入也由 CHECK/trigger 拒绝。fixture 覆盖 missing/draft/in_review/retired/stale revision/stale contract/malformed hash/blocker/direct SQL 与完整合格 brand/pen，证明失败不留下半审核状态、marker 或 visibility。</action>
  <verify>
    <automated>pnpm check:publication-gate -- --publish</automated>
  </verify>
  <acceptance_criteria>
    - "direct SQL 不能仅更新 status 绕过 readiness、revision、contract、hash format 与 reviewer/timestamps。"
    - "publishEntity 失败时所有 review/publication write 回滚且不自动 retry。"
    - "合格 fixture transaction 完成后恰好进入 public_entities；critical edit 后立即退出。"
    - "published、retired、重新审核再 published 的合法 transition 均由测试固定。"
  </acceptance_criteria>
  <done>publication 只能经 server transaction 与 DB guard 双重授权原子进入，缺失、陈旧或篡改状态全部 fail closed。</done>
</task>

</tasks>

## Artifacts This Plan Produces

- `migrations/030_publication_gate.sql`
- table `entity_publications`
- views `publication_blockers`, `public_entity_readiness`, `public_entities`
- `src/lib/publication.ts`
- constant `PUBLICATION_CONTRACT_VERSION = 1`
- functions `computePublicationContentHash()`, `publishEntity()`, `setEntityPublicationStatus()`
- CLI flags `--migration`, `--backfill`, `--invalidation`, `--publish`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| application/CLI → publication tables | 有 DB 写权限的代码不能仅靠改 status 获得公开资格 |
| critical content tables → approval state | 上游证据或内容改变必须原子失效旧审核 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-04 | Elevation of Privilege | direct SQL publish | high | mitigate | BEFORE transition guard + negative fixtures |
| T-18-05 | Tampering | stale hash/revision/contract | high | mitigate | computed readiness、revision triggers、canonical hash 与 transaction recheck |
| T-18-06 | Tampering | stale diagnostic blocker snapshot | medium | mitigate | snapshot 永不参与 authorization，public view 只读 computed result |
</threat_model>

<verification>
- `pnpm check:publication-gate -- --migration`
- `pnpm check:publication-gate -- --backfill`
- `pnpm check:publication-gate -- --invalidation`
- `pnpm check:publication-gate -- --publish`
- `pnpm check:migrations && pnpm check:data-contract && pnpm build`
- 只操作 disposable local DB；不得运行 remote migration、push 或 deploy。
</verification>

<success_criteria>
- PUB-01：brand/pen publication lifecycle 显式且默认 draft。
- PUB-02：contract version 1 readiness 零 blocker 才能发布，计算失败一律隐藏。
- PUB-05：全部关键输入变化都会使 revision/hash review 失效。
- PUB-06：backfill 只建 draft，不修改或复活 deprecated story。
- fresh replay、upgrade、idempotency、quick_check、foreign_key_check、schema object、constraint 与 transition fixtures 全绿。
</success_criteria>

<output>
Create `.planning/phases/18-publication-gate/18-02-SUMMARY.md` when done.
</output>
