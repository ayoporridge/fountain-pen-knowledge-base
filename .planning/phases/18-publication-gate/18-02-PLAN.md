---
phase: 18-publication-gate
plan: "02"
type: execute
wave: 2
depends_on:
  - "18-01"
files_modified:
  - scripts/seed.ts
  - scripts/seed-tags.ts
  - scripts/seed-concepts.ts
  - scripts/seed-library-samples.ts
  - scripts/import-csv.ts
  - scripts/import-markdown.ts
  - src/lib/db.ts
  - scripts/check-publication-gate.ts
  - playwright.config.ts
  - package.json
autonomous: true
requirements:
  - PUB-01
  - PUB-06
must_haves:
  truths:
    - "四个 seed 入口只调用 canonical migrateDatabase；CSV/Markdown importer 只调用 assertDatabaseReady。"
    - "contract 与 Playwright 使用 disposable file database，fixture 模式拒绝 data/fpkg.db。"
    - "后续 030、publication 与 browser tests 共用同一可清理 harness。"
  artifacts:
    - path: "scripts/check-publication-gate.ts"
      provides: "isolated DB and Next/Playwright fixture lifecycle"
    - path: "src/lib/db.ts"
      provides: "server-only injectable file database URL"
  key_links:
    - from: "playwright.config.ts"
      to: "scripts/check-publication-gate.ts"
      via: "isolated webServer command"
      pattern: "check:publication-gate"
---

<objective>
收敛剩余 seed/CSV/Markdown migration runner，并提前建立 publication contract 与 Playwright 共用的隔离数据库路径。

Purpose: 后续正向 publish/E2E fixture 必须不污染真实目录库。
Output: 六个 runner 收敛、injectable DB、check-publication harness、Playwright isolated webServer。
</objective>

<execution_context>
@/Users/xz/.codex/gsd-core/workflows/execute-plan.md
@/Users/xz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-publication-gate/18-CONTEXT.md
@.planning/phases/18-publication-gate/18-RESEARCH.md
@.planning/phases/18-publication-gate/18-PATTERNS.md
@.planning/phases/18-publication-gate/18-VALIDATION.md
@.planning/phases/18-publication-gate/18-01-SUMMARY.md
@src/lib/db.ts
@scripts/migrate.ts
@playwright.config.ts
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: 收敛 seed、CSV 与 Markdown runner</name>
  <files>scripts/seed.ts, scripts/seed-tags.ts, scripts/seed-concepts.ts, scripts/seed-library-samples.ts, scripts/import-csv.ts, scripts/import-markdown.ts</files>
  <read_first>scripts/seed.ts, scripts/seed-tags.ts, scripts/seed-concepts.ts, scripts/seed-library-samples.ts, scripts/import-csv.ts, scripts/import-markdown.ts, src/lib/db.ts, scripts/migrate.ts</read_first>
  <action>让 `seed.ts`、`seed-tags.ts`、`seed-concepts.ts`、`seed-library-samples.ts` 只调用 `migrateDatabase(client)` 后再 seed；删除 checksum-free runner、migration SQL loop 和直接 marker 写入。让 `import-csv.ts`、`import-markdown.ts` 删除 migration 文件读取/拆分/执行，在首个业务 write 前调用 `assertDatabaseReady(client)`。不改变导入格式或 seed 内容，不自动 retry writes。</action>
  <verify>
    <automated>pnpm check:migrations</automated>
  </verify>
  <acceptance_criteria>
    - "四个 seed 均调用 migrateDatabase(client) 且不直接写 migrations。"
    - "两个 importer 均调用 assertDatabaseReady(client) 且不执行 migration SQL。"
    - "pnpm check:migrations 退出码为 0。"
  </acceptance_criteria>
  <done>seed/import runner ownership 与 Plan 01 的 canonical 边界一致。</done>
</task>

<task type="auto">
  <name>Task 2: 建立 injectable DB 与 isolated Playwright/publication harness</name>
  <files>src/lib/db.ts, scripts/check-publication-gate.ts, playwright.config.ts, package.json</files>
  <read_first>src/lib/db.ts, scripts/check-publication-gate.ts, playwright.config.ts, package.json, scripts/check-migration-safety.ts</read_first>
  <action>在 `getDb()` 增加 server-only `FPKG_DATABASE_URL` file URL override；与 `TURSO_DATABASE_URL` 同时存在时失败，`PUBLICATION_GATE_FIXTURE=1` 时只接受 file URL 且拒绝解析到 `data/fpkg.db`。新增 `scripts/check-publication-gate.ts` 的 mkdtemp/client/child-process/finally+signal cleanup 生命周期以及 `--fixture-isolation`、`--serve-e2e --port 3107` 入口；harness 使用 canonical migrations、清空 Turso 凭据并把临时 URL 传给 Next。Playwright 本地 webServer 走该 harness，外部 E2E_BASE_URL 不变。package.json 新增 `check:publication-gate`，不安装依赖。fixture-isolation 同时比较真实 DB 前后 stat/hash。</action>
  <verify>
    <automated>pnpm check:publication-gate -- --fixture-isolation</automated>
  </verify>
  <acceptance_criteria>
    - "fixture 模式拒绝 data/fpkg.db 和 Turso/file URL 混用。"
    - "成功、失败与 SIGTERM 均关闭 client/server 并删除 temp root。"
    - "Playwright 本地 webServer 不再直接使用真实目录库。"
    - "pnpm check:publication-gate -- --fixture-isolation 退出码为 0。"
  </acceptance_criteria>
  <done>Plan 03–07 获得同一条 disposable database/browser harness。</done>
</task>

</tasks>

## Artifacts This Plan Produces

- package command `check:publication-gate`
- env contracts `FPKG_DATABASE_URL`, `PUBLICATION_GATE_FIXTURE`
- CLI flags `--fixture-isolation`, `--serve-e2e`, `--port`

<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation |
|---|---|---|---|---|---|
| T-18-02 | Tampering | fixture DB path | high | mitigate | real-path rejection + pre/post hash |
| T-18-03 | Denial of Service | leaked fixture process | medium | mitigate | signal/finally cleanup |
</threat_model>

<verification>
- `pnpm check:migrations`
- `pnpm check:publication-gate -- --fixture-isolation`
- `pnpm check:data-contract && pnpm build`
- 不运行 remote migration、push 或 deploy。
</verification>

<success_criteria>
- 六个剩余 runner 收敛。
- disposable DB 与 Playwright harness 可复用且不触碰 data/fpkg.db。
- 本计划只修改 10 个文件；每个 task 少于 15 个 files。
</success_criteria>

<output>
Create `.planning/phases/18-publication-gate/18-02-SUMMARY.md` when done.
</output>
