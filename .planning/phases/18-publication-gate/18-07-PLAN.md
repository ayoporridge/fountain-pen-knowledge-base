---
phase: 18-publication-gate
plan: "07"
type: execute
wave: 7
depends_on:
  - "18-06"
files_modified:
  - scripts/check-publication-gate.ts
  - scripts/check-public-boundary.ts
  - tests/e2e/publication-gate.spec.ts
  - tests/e2e/site-quality.spec.ts
  - tests/e2e/library.spec.ts
  - src/middleware.ts
autonomous: true
requirements:
  - PUB-01
  - PUB-02
  - PUB-03
  - PUB-04
  - PUB-05
  - PUB-06
  - PUB-07
must_haves:
  truths:
    - "fresh/upgrade/idempotent migration 与 publication state-machine 由 independent disposable fixtures 复验。"
    - "完整列表双向 equality、detail/metadata/API 逐 ID equivalence、facets/statistics aggregate equality、context results public subset 分开验证。"
    - "expected oracle 不导入 runtime authorization helper。"
    - "Montblanc 149 draft shell 全面缺席，synthetic valid fixtures 在适用 surfaces 同现。"
    - "Majohn A1 draft shell 全面缺席，synthetic brand 的全部 public models 与 reverse made_by 集合双向相等。"
    - "旧 >500/>=550、真实目录天然公开、允许 shared cache 等断言已替换。"
  artifacts:
    - path: "tests/e2e/publication-gate.spec.ts"
      provides: "isolated fail-closed and valid publication regression"
    - path: "scripts/check-public-boundary.ts"
      provides: "independent four-semantics parity oracle"
  key_links:
    - from: "independent fixture SQL"
      to: "all public surfaces"
      via: "equality/per-ID/aggregate/subset comparisons"
      pattern: "EXCEPT"
---

<objective>
完成 independent parity、legacy E2E rewrite 与 Phase 18 全量本地验收。

Purpose: 防止同源 helper 自证、数量 KPI 与真实目录 fixture 掩盖 public boundary 缺陷。
Output: migration/compatibility matrix、独立四语义 oracle、publication E2E、改写后的旧 regression。
</objective>

<execution_context>
@/Users/xz/.codex/gsd-core/workflows/execute-plan.md
@/Users/xz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/18-publication-gate/18-CONTEXT.md
@.planning/phases/18-publication-gate/18-RESEARCH.md
@.planning/phases/18-publication-gate/18-PATTERNS.md
@.planning/phases/18-publication-gate/18-VALIDATION.md
@.planning/phases/18-publication-gate/18-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 完成 migration/publication matrix 与 non-brand compatibility</name>
  <files>scripts/check-publication-gate.ts</files>
  <read_first>scripts/check-publication-gate.ts, migrations/030_publication_gate.sql, src/lib/publication.ts, src/lib/db.ts, migrations/023_retire_template_stories.sql</read_first>
  <action>补全 empty fresh replay、当前本地库临时副本 upgrade、second-run idempotency/checksum、quick_check、foreign_key_check、critical schema objects 与 illegal writes。migration 前后逐行 diff deprecated story id/type/status/body hash；断言全部 brand/pen draft、published=0、entity content 不变。覆盖 create/type transitions、critical I/U/D、direct SQL、rollback。compatibility expected side 在 upgrade 前保存 non-brand/pen legacy public set，升级后双向 EXCEPT 零 diff；显式 publication row 切换 strict gate。聚合全部 failures 后一次退出。</action>
  <verify>
    <automated>pnpm check:publication-gate -- --migration-full && pnpm check:publication-gate -- --compatibility</automated>
  </verify>
  <acceptance_criteria>
    - "fresh/upgrade/idempotent/constraints matrix 全绿。"
    - "deprecated story diff=0，brand/pen published=0。"
    - "non-brand/pen compatibility 两个 EXCEPT 方向为空。"
  </acceptance_criteria>
  <done>PUB-01/PUB-02/PUB-05/PUB-06 有独立 DB evidence。</done>
</task>

<task type="auto">
  <name>Task 2: 建立不复用 runtime predicate 的四语义 parity oracle</name>
  <files>scripts/check-public-boundary.ts</files>
  <read_first>scripts/check-public-boundary.ts, src/lib/public-visibility.ts, src/lib/browse-data.ts, src/lib/recommend.ts, src/lib/library.ts, src/app/sitemap.ts</read_first>
  <action>expected side 直接读取 fixture/publication contract SQL 与已知 ids，禁止导入 runtime public visibility/browse/recommend/library authorization helpers。browse/sitemap/entities list 做双向 equality；detail/metadata/detail API/preview 逐 governed id equivalence；facets/type/home/by aggregates 按 key equality；graph/links/recommend/wiki/concept/exhibit/timeline/source/media 做 public subset；每个 brand 的完整 model list 与 reverse approved made_by public pens 做双向 equality 和 count equality。输出 actual-only/expected-only ids、aggregate diffs、illegal context target 与 API field leak；删除页面数量 KPI。</action>
  <verify>
    <automated>pnpm check:public-boundary -- --all</automated>
  </verify>
  <acceptance_criteria>
    - "四类 surface 语义分别验证，未混成全集 equality。"
    - "expected oracle 不导入 runtime authorization helper。"
    - "全部品牌型号列表无遗漏、无额外项、无 12 条上限。"
    - "pnpm check:public-boundary -- --all 退出码为 0。"
  </acceptance_criteria>
  <done>PUB-03/PUB-04 由独立、语义正确的 parity oracle 覆盖。</done>
</task>

<task type="auto">
  <name>Task 3: 新增 publication E2E 并重写 legacy assumptions</name>
  <files>tests/e2e/publication-gate.spec.ts, tests/e2e/site-quality.spec.ts, tests/e2e/library.spec.ts, src/middleware.ts</files>
  <read_first>tests/e2e/publication-gate.spec.ts, tests/e2e/site-quality.spec.ts, tests/e2e/library.spec.ts, src/middleware.ts, src/lib/public-visibility.ts, playwright.config.ts, scripts/check-publication-gate.ts, scripts/check-public-boundary.ts</read_first>
  <action>新增 isolated synthetic fixture E2E：Montblanc 149 与 Majohn A1 deliberate draft/no-content 在 detail/metadata/browse/sitemap/entities/graph/links/recommend/library/wiki/media 全缺席；valid brand 及超过 12 个 valid pens 经真实 publish transaction 后在适用 surfaces 同现，brand 页面逐一显示全部型号链接和准确数量，pen 页面链接唯一 brand。覆盖 draft 404→published 200、published→critical edit/retire 404、re-review→200 且下一请求即时变化。Next App Router 对 streamed `notFound()` 会返回 soft 200，因此 middleware 使用 Next 15.5 stable Node.js runtime，在已知 canonical redirect 之后直接查询 server-only public helper，对 entity namespace 的未发布 exact type/slug 在渲染前返回真实 HTTP 404；public target 才放行到页面。site-quality/library 删除 >500/>=550、Pilot/LAMY/真实目录天然 200、无规格仍公开、browse shared cache 等断言，替换为 exact set/no-store/full sitemap traversal。targeted desktop test 不 retry、不 skip、不修改真实目录库。</action>
  <verify>
    <automated>pnpm exec playwright test tests/e2e/publication-gate.spec.ts --project=desktop</automated>
  </verify>
  <acceptance_criteria>
    - "Montblanc 与 Majohn A1 draft fixtures 全面缺席且 entity detail 返回真实 HTTP 404（不是含 noindex 的 soft 200），valid fixtures 在所有适用 surface 同现。"
    - "超过 12 个 synthetic pens 全部出现在 brand 页面且各自链接可达。"
    - "旧数量、真实目录与 cache 假设全部被精确语义断言替换。"
    - "targeted desktop Playwright 退出码为 0，data/fpkg.db 前后不变。"
  </acceptance_criteria>
  <done>publication browser regression 与 legacy suite 使用同一 fail-closed contract。</done>
</task>

</tasks>

## Artifacts This Plan Produces

- `tests/e2e/publication-gate.spec.ts`
- checker flags `--migration-full`, `--compatibility`, `--all`
- independent four-semantics parity oracle

<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation |
|---|---|---|---|---|---|
| T-18-09 | Repudiation | tautological parity | high | mitigate | independent expected SQL |
| T-18-10 | Tampering | real catalog fixture | high | mitigate | disposable DB + pre/post hash |
| T-18-11 | Information Disclosure | streamed entity notFound | medium | mitigate | Node middleware pre-render public lookup returns hard 404 |
</threat_model>

<verification>
- `pnpm check:publication-gate -- --migration-full`
- `pnpm check:publication-gate -- --compatibility`
- `pnpm check:public-boundary -- --all`
- `pnpm exec playwright test tests/e2e/publication-gate.spec.ts --project=desktop`
- Phase gate（4–8 分钟，仅在本计划结束运行）：`pnpm check:migrations && pnpm check:data-contract && pnpm check:publication-gate && pnpm check:public-boundary && pnpm check:library && pnpm lint && pnpm build && pnpm test:e2e:desktop && pnpm test:e2e:mobile`
- 不运行 remote migration、push 或 deploy。
</verification>

<success_criteria>
- PUB-01..PUB-07 均有独立 automated evidence。
- 四语义 parity、Montblanc fail-closed、valid publish/invalidate/republish 与完整 local regression 全绿。
- 本计划只修改 5 个文件；每个 task 少于 15 个 files。
</success_criteria>

<output>
Create `.planning/phases/18-publication-gate/18-07-SUMMARY.md` when done.
</output>
