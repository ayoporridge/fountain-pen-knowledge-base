---
phase: 19-real-audit-evidence
plan: "02"
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - migrations/031_evidence_readiness_v2.sql
  - src/lib/db.ts
  - scripts/check-evidence-contract.ts
autonomous: true
requirements: [EVID-01, EVID-02, EVID-03, EVID-04, EVID-05, EVID-06, QA-01]
must_haves:
  truths:
    - "D-03/D-04: 规格字段和 approved core claim 都有 claim-level citation/locator/scope/source provenance、item-level 来源层级与独立组、冲突、四类 hash review 的 normalized fail-closed schema。"
    - "D-03: 每个 approved core claim 缺 citation、非空 locator、显式 fact scope 或 qualifying source-item provenance/tier/group/allowed-use 任一项时，精确产生 approved_claim_missing_evidence 并阻止 readiness/publication。"
    - "D-05: contract version 2 不 grandfather 旧 review；任何旧 published fixture 升级后不可公开。"
    - "D-05: public_entities 仍是唯一授权集合，blocker 普通行是 truth，JSON 只作诊断。"
    - "D-06: migration 031 只在 disposable DB 演练，030 checksum/content 永远不改。"
  artifacts:
    - path: "migrations/031_evidence_readiness_v2.sql"
      provides: "spec/claim evidence、core-vs-editorial classification、scope/conflict/review schema、readiness blockers、guards、triggers"
    - path: "src/lib/db.ts"
      provides: "031 schema manifest readiness guard"
    - path: "scripts/check-evidence-contract.ts"
      provides: "schema/rebuild/upgrade/idempotency/integrity contract"
  key_links:
    - from: "spec_field_evidence/claim_evidence/citations/fact_scopes/source_items"
      to: "publication_blockers"
      via: "规格字段与 approved core claim 逐项检查 citation/locator/scope，并只按 source item 持久化 tier/group/allowed-use 去重的 qualified evidence/source-group views；registry 只可提供不计数的 defaults"
      pattern: "publication_v2_"
    - from: "entity_publications"
      to: "public_entities"
      via: "contract=2 zero-blocker strict branch"
      pattern: "reviewed_contract_version"
---

<objective>
用 migration 031 落地 Phase 19 的不可逆数据契约（D-03、D-04、D-05），并在 disposable canonical migration 中证明 table rebuild、旧 review 失效与唯一公开 predicate 都 fail closed。

Purpose: 后续 hash、publish 与 audit 只能建立在可表达逐字段证据、版本范围、来源独立性、冲突和分层审核的数据库真相上。
Output: 完整 031 migration、版本感知 schema manifest、schema/migration contract fixtures。
</objective>

<execution_context>
@/Users/xz/.codex/gsd-core/workflows/execute-plan.md
@/Users/xz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/19-real-audit-evidence/19-CONTEXT.md
@.planning/phases/19-real-audit-evidence/19-RESEARCH.md
@.planning/phases/19-real-audit-evidence/19-PATTERNS.md
@.planning/phases/19-real-audit-evidence/19-VALIDATION.md
@.planning/phases/19-real-audit-evidence/19-01-SUMMARY.md
@migrations/011_library_schema.sql
@migrations/030_publication_gate.sql
@src/lib/db.ts
@scripts/check-evidence-contract.ts
@scripts/check-publication-gate.ts

<interfaces>
- Preserve existing `migrateDatabase(client,{migrationsDir})`, migration checksums and `assertDatabaseReady()` semantics.
- Existing `entity_publications` v1 hash CHECK requires a generalized create/copy/drop/rename rebuild for `sha256:v2:`.
- Existing `publication_blockers`, `public_entity_readiness`, `public_entities` names remain runtime contracts; 031 replaces their definitions, not their consumers.
</interfaces>
</context>

<scope_constraints>
- Do not modify migration 030. After every 031 schema edit, the first blocking command is `pnpm check:evidence-contract -- --migration`, whose fixture must spawn canonical `pnpm migrate` against its own temporary DB.
- Never schema-push/migrate real catalog or remote; all writes/migrations disposable and real main/WAL/SHM snapshot-protected.
- No Turso, push, deploy, network research, content writing, search/LLM, renderer, taxonomy repair, package or test runner.
</scope_constraints>

<tasks>

<task type="auto">
  <name>Task 1: 创建 normalized evidence、scope、conflict 与 review schema</name>
  <files>migrations/031_evidence_readiness_v2.sql, src/lib/db.ts, scripts/check-evidence-contract.ts</files>
  <read_first>migrations/011_library_schema.sql, migrations/030_publication_gate.sql, src/lib/db.ts, scripts/check-evidence-contract.ts, scripts/check-publication-gate.ts, .planning/phases/19-real-audit-evidence/19-RESEARCH.md, .planning/phases/19-real-audit-evidence/19-PATTERNS.md</read_first>
  <action>先写 `--schema` 失败 fixtures，再新增 031：`fact_scopes` 表表达 model/variant、market、valid_from/to、production_state、nib/material/edition scope；`spec_field_evidence` 用受控 field_key 把 approved citation 与 scope、非空 evidence_locator 绑定到单一规格字段。同时为 `claims` 新增显式 fail-closed fact classification（`core|editorial|unclassified`，旧行为 unclassified），并新增 normalized `claim_evidence` mapping，使每个 `status='approved'` 且 classification=`core` 的 claim 至少链接一条 approved citation、非空 evidence locator、显式 `fact_scopes` row，且 citation 链到的 `source_items` 具有显式 provenance/source_tier/independence_group/archive locator，其 registry/item allowed-use 对 core-fact publication 合法。任一链路节点缺失都必须以稳定低基数 code `approved_claim_missing_evidence` 输出，`detail_key` 记录 claim id 与 missing component；显式 editorial claim 不可计入 core-fact completeness/source threshold，approved unclassified claim 仍 fail closed，不得通过改名 editorial 来代替 core fact 的证据。给 citations 增 pending-default review_status、scope/evidence locator；`fact_conflicts` + `fact_conflict_members` 表达 field 与 identity/made_by conflict、resolved note；`entity_content_reviews` 约束 fact/language/media/publication 四类、content_hash、status、reviewer/time 且 unique entity+kind+hash。保留 registry 的 `source_type`/`allowed_use` 作为来源目录属性；registry 可新增 nullable `default_source_tier`/`default_independence_group` 供录入提示，但资格计算绝不能直接计 registry default。真正参与 publication 的 `source_tier`、`independence_group` 与 archive locator 必须持久化在每条 `source_items` provenance 上，旧行保持 NULL/fail-closed；镜像/转载按 item-level group DISTINCT 去重。fixtures 必须证明“不同 registry、相同 group”只计 1，以及“相同 registry、不同 group/tier 的两个 item”按各自 provenance 正确计数，禁止一条 registry 属性替全部 items 赋予独立性。所有新增审核状态默认 pending/draft，旧行不得推断 approved。将全部 table/index 加入版本感知 manifest。字段与 CHECK 的确切命名按 agent discretion，但必须覆盖 EVID-01..06。</action>
  <verify>
    <automated>pnpm check:evidence-contract -- --migration &amp;&amp; pnpm check:evidence-contract -- --schema</automated>
  </verify>
  <acceptance_criteria>
    - "每个 EVID-01..06 必须字段都有 NOT NULL/CHECK/FK/unique 或明确 fail-closed null 语义。"
    - "qualification 只读取 source_items 上显式 tier/group；registry defaults 单独不计数，旧 item null provenance 默认不合格。"
    - "跨 registry 同 group 的 items 只计 1；同 registry 不同 group/tier 的 items 可被分别判断；retailer/community/search tier 只能辅助。"
    - "每个 approved core claim 必须有至少一条完整 claim_evidence→approved citation→explicit scope→qualifying source item 链；缺 citation/locator/scope/provenance 任一项都精确产生 approved_claim_missing_evidence 并不可公开。"
    - "editorial/core/unclassified 分类显式且入 hash；editorial 不能计作 core 完成度，approved unclassified 不能绕过 core-fact gate。"
    - "四 review kinds 独立、hash-bound；review rows 不属于 content payload FK 循环。"
    - "030 文件 hash 不变，真实 catalog snapshot 不变。"
  </acceptance_criteria>
  <done>逐字段证据、来源、scope、冲突与审核均有可迁移、可查询、默认不合格的 schema。</done>
</task>

<task type="auto">
  <name>Task 2: 重建 publication contract 2、readiness blockers 与 guards</name>
  <files>migrations/031_evidence_readiness_v2.sql, src/lib/db.ts, scripts/check-evidence-contract.ts</files>
  <read_first>migrations/031_evidence_readiness_v2.sql, migrations/030_publication_gate.sql, src/lib/db.ts, scripts/check-evidence-contract.ts, .planning/phases/18-publication-gate/18-VERIFICATION.md</read_first>
  <action>在 031 内按 SQLite generalized rebuild 顺序先保存/drop dependent publication views/triggers，create new `entity_publications` 接受 `sha256:v2:`，按列复制 lifecycle；retired 保持 retired，其余旧 review/hash 清空并回到 draft，绝不自动 published。重建 centralized qualifying views 与 blocker rows：correct published story、deprecated/pending/needs_source、逐字段 citation+locator+scope、approved core claim 的 claim-level citation+locator+scope+qualifying source-item provenance/tier/group/allowed-use、primary/archive 与独立 professional-secondary groups、unresolved field/identity conflicts、approved primary reusable media、四类 current-hash reviews，以及 Phase 18 summary/hash/revision/reviewer/made_by blockers。`publication_v2_qualified_core_claims` 只能收录证据链完整的 approved core claim；对每个不完整的 approved core claim 输出 exact `approved_claim_missing_evidence`，任一行存在均使 entity readiness false 并让 direct SQL/publication transaction 失败。blocker 以稳定 `(code,subject_type,subject_id/detail_key)` 行为 truth，聚合显式排序。重建 invalidation triggers：claim classification/claim evidence/scope/evidence/citation/source/conflict/media 等 payload mutation 增 revision并隐藏旧发布；review mutation 不增 revision、不参与 hash，但 revoked/deleted review 立即使 readiness false。重建 direct publish guards 与原名 `public_entity_readiness`/`public_entities`，strict contract 2 且保留 legacy non-brand compatibility。每次 031 schema 修改后都必须先运行 `pnpm check:evidence-contract -- --migration`，然后运行 `pnpm check:evidence-contract -- --schema`，不得单独跳到 schema fixture。</action>
  <verify>
    <automated>pnpm check:evidence-contract -- --migration &amp;&amp; pnpm check:evidence-contract -- --schema</automated>
  </verify>
  <acceptance_criteria>
    - "fresh、pre-031 upgrade、第二次 replay、quick_check、foreign_key_check、manifest/checksum 全通过。"
    - "旧 reviewed/published fixture 升级后 0 个自动 published，旧 review 不满足 contract 2。"
    - "031 table rebuild 行数/identity/lifecycle disposition 守恒且 indexes/views/triggers 齐全。"
    - "直接 SQL 无法凭 status/hash snapshot 绕过 blockers；public_entities 仍是唯一授权 view。"
    - "approved core claim 缺任一 evidence-chain 组件都有 exact approved_claim_missing_evidence/detail，该 claim 不进 qualified view且实体无法 readiness/publication。"
  </acceptance_criteria>
  <done>migration 031 在 disposable fresh/upgrade/replay 中完整建立 fail-closed contract 2，无 schema 或授权旁路。</done>
</task>

</tasks>

## Artifacts this phase produces

DB objects：`fact_scopes`, `spec_field_evidence`, `claim_evidence`, `claims.fact_class`, `fact_conflicts`, `fact_conflict_members`, `entity_content_reviews`，`source_items.source_tier/independence_group/archive*` item-level provenance columns、可选 registry defaults、citation contract columns，`publication_v2_qualified_core_claims` 及其他 `publication_v2_*` qualifying views，contract-2 blockers/guards/invalidation triggers，以及重建后的 `entity_publications`, `public_entity_readiness`, `public_entities`。  
CLI flags：`check:evidence-contract -- --schema|--migration`。  
Symbols/paths：`PUBLICATION_SCHEMA_MANIFEST` 扩展、`migrations/031_evidence_readiness_v2.sql`；不创建内容或 inventory artifacts。

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Migration SQL → existing schema/data | table rebuild 可能丢 row、FK、index、trigger 或误授权。 |
| Raw evidence rows → readiness | pending/mirror/retailer rows或缺引用、locator、scope、source provenance 的 approved core claim 可能被误算为合格。 |
| Direct SQL → publication lifecycle | caller 可能跳过 server transaction 直接改 status。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-19-03 | Tampering | migration 031 table rebuild | high | mitigate | disposable canonical migrate first、row/identity parity、quick/FK check、manifest/checksum、fresh/upgrade/replay；任一失败阻断。 |
| T-19-04 | Elevation of Privilege | direct SQL publish | high | mitigate | DB transition guards读取当前 contract-2 blockers/revision/hash/reviews，negative direct-SQL fixtures。 |
| T-19-05 | Tampering | evidence/source qualification | high | mitigate | pending defaults、item-level tier allowlist/group DISTINCT、registry defaults不计数、跨registry同组与同registry异组fixtures、spec/approved-core-claim 的 citation/locator/scope/provenance joins、approved_claim_missing_evidence exact blocker fixtures。 |
| T-19-06 | Tampering | duplicated public predicate | high | mitigate | 保留一个 `public_entities` definition，qualifying/audit views只提供诊断；independent expected oracle不导入 runtime predicate。 |

**Blocking threshold:** 任一 unresolved high/critical threat 停止执行，不得进入 Plan 03。
</threat_model>

<verification>
- 每次 031 edit 后固定按顺序：`pnpm check:evidence-contract -- --migration &amp;&amp; pnpm check:evidence-contract -- --schema`
- `pnpm check:migrations`
- `pnpm exec tsc --noEmit`
- 真实 catalog 与 remote 均不得运行 migration。
</verification>

<success_criteria>
- EVID-01..06 均有 normalized、默认 fail-closed、可 fixture 验证的 schema/readiness 表达；approved core claim 不可在 citation/locator/scope/qualifying source provenance 不完整时获得资格。
- migration 031 rebuild 不改 030、不丢数据/对象、不继承旧资格，public_entities sole authorization 不变。
</success_criteria>

<output>
Create `.planning/phases/19-real-audit-evidence/19-02-SUMMARY.md` when done.
</output>
