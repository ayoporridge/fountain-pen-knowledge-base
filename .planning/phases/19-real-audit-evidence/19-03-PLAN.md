---
phase: 19-real-audit-evidence
plan: "03"
type: execute
wave: 3
depends_on: ["19-02"]
files_modified:
  - src/lib/publication.ts
  - scripts/lib/phase19-fixtures.ts
  - scripts/check-evidence-contract.ts
  - scripts/check-publication-gate.ts
autonomous: true
requirements: [EVID-01, EVID-02, EVID-03, EVID-04, EVID-05, EVID-06, AUD-02, AUD-03, QA-01]
must_haves:
  truths:
    - "D-03/D-04: canonical hash 覆盖 spec evidence、approved core claim classification/evidence/locator/scope/source provenance、conflict/media 内容，但不包含指向该 hash 的 review rows。"
    - "D-03: approved core claim 缺 citation、locator、scope 或 qualifying source-item provenance 任一项时，approved_claim_missing_evidence 会独立使 DB guard 和 server publication 失败。"
    - "D-05: fact/language/media reviews 必须已批准当前 hash，publication review 在同一 transaction 内写入；任一 blocker 都不能被分数抵消。"
    - "D-05: direct SQL 与 server transaction 独立 fail closed，失败不留半审核状态且不 retry。"
    - "完整 brand+pen fixture 可原子发布，pen 唯一 made_by public brand；任一 critical mutation 立即隐藏。"
  artifacts:
    - path: "src/lib/publication.ts"
      provides: "sha256:v2 canonical payload、review API、atomic publish"
    - path: "scripts/check-evidence-contract.ts"
      provides: "hash/readiness/publish fixture matrix"
    - path: "scripts/lib/phase19-fixtures.ts"
      provides: "完整资格 fixture builders"
  key_links:
    - from: "readPublicationContentPayload()"
      to: "entity_content_reviews.content_hash"
      via: "stable sha256:v2 without review self-reference"
      pattern: "PUBLICATION_CONTRACT_VERSION"
    - from: "publishEntity()"
      to: "public_entity_readiness/public_entities"
      via: "one write transaction: hash/first-3 reviews → in_review v2 publication snapshot → publication review → blockers → published/published_at → membership"
      pattern: "publishEntity"
---

<objective>
把 contract-2 schema 接入 canonical hash、四类审核和无 retry 原子发布（D-03、D-04、D-05），让 DB blocker 与 server transaction 对同一事实独立达成一致。

Purpose: schema 能表达证据还不够；只有 hash/invalidation/review/publish 全链闭合，旧审核才不会在内容变化后继续授权。
Output: publication module contract 2、qualification fixture builders、hash/readiness/direct-SQL/rollback checks。
</objective>

<execution_context>
@/Users/xz/.codex/gsd-core/workflows/execute-plan.md
@/Users/xz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/19-real-audit-evidence/19-CONTEXT.md
@.planning/phases/19-real-audit-evidence/19-RESEARCH.md
@.planning/phases/19-real-audit-evidence/19-PATTERNS.md
@.planning/phases/19-real-audit-evidence/19-VALIDATION.md
@.planning/phases/19-real-audit-evidence/19-02-SUMMARY.md
@migrations/031_evidence_readiness_v2.sql
@src/lib/publication.ts
@scripts/lib/phase19-fixtures.ts
@scripts/check-evidence-contract.ts
@scripts/check-publication-gate.ts

<interfaces>
- Preserve `PublicationDatabase`, `readPublicationContentPayload`, `computePublicationContentHash`, `publishEntity`, `setEntityPublicationStatus` exports.
- Upgrade `PUBLICATION_CONTRACT_VERSION` to 2 and hash prefix to `sha256:v2:`.
- Add `ReviewKind = fact|language|media|publication` and a server-only `recordEntityContentReview()` for non-publication reviews; `publishEntity()` owns final publication review.
</interfaces>
</context>

<scope_constraints>
- Real catalog remains explicit read-only/snapshot-protected; every mutation/publish fixture disposable with cleared remote env.
- Do not modify migration 030, do not migrate remote/real catalog, add package, deploy, search/research content, bulk write, render UI or repair taxonomy/content.
- `public_entities` remains sole authorization; checker expected sets stay independent of production readiness helpers.
</scope_constraints>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 扩展 canonical payload、hash 与 invalidation evidence</name>
  <files>src/lib/publication.ts, scripts/lib/phase19-fixtures.ts, scripts/check-evidence-contract.ts, scripts/check-publication-gate.ts</files>
  <read_first>src/lib/publication.ts, migrations/031_evidence_readiness_v2.sql, scripts/check-evidence-contract.ts, scripts/check-publication-gate.ts, scripts/lib/phase19-fixtures.ts, .planning/phases/19-real-audit-evidence/19-PATTERNS.md</read_first>
  <behavior>
    - 同义 rows 不同插入顺序、Unicode/newline 差异 -> 相同 sha256:v2 hash。
    - spec/claim evidence locator/scope/source tier/group/archive、claim fact classification、conflict/media 的 publication-critical mutation -> hash 或 revision变化并使旧 review失效。
    - Fully evidenced approved core claim 分别移除 citation、locator、scope 或 qualifying source provenance -> 均产生 approved_claim_missing_evidence 且无 public membership。
    - review row insert/update/delete -> 不改变 content hash/revision，但 current-hash review不足立即产生 blocker。
  </behavior>
  <action>先扩 `--hash-invalidation` fixture，再把 canonical payload 固定升级到 contract 2：明确 keys/nulls，稳定排序/去重，纳入 citations 的 locator/scope/status、spec_field_evidence、claims 的 core/editorial/unclassified fact classification、claim_evidence 及其 citation/locator/scope/source-item provenance、fact_scopes、source tier/group/archive/allowed-use/retrieved、conflicts 与 members、approved primary reusable media，以及现有 entity/story/spec/variant/claim/reference/timeline/made_by；不得纳入 entity_content_reviews，避免 hash-review 自引用。扩 mutation fan-out 检查至全部新 payload table/columns，特别覆盖 claim classification、claim evidence、locator、scope 和 source provenance I/U/D，并单独证明 review mutation不增 revision但立即改变 readiness。fixture builder 提供 primary/archive、独立专业二手、mirror same-group、scope、spec/claim evidence、conflict、media数据，所有 ID/时间固定以保持确定性。</action>
  <verify>
    <automated>pnpm check:evidence-contract -- --hash-invalidation &amp;&amp; pnpm check:publication-gate -- --invalidation</automated>
  </verify>
  <acceptance_criteria>
    - "hash format 精确为 sha256:v2 + 64 lowercase hex，canonical ordering/NFC/newline fixtures 稳定。"
    - "每个 contract-2 payload dependency 都有 I/U/D 或 column mutation evidence。"
    - "claim classification/evidence/locator/scope/provenance 进入 hash 和 invalidation；approved core claim 证据链不完整时 exact approved_claim_missing_evidence 阻止 public membership。"
    - "review records 不进入 payload；revoked/deleted review 在下一次 readiness query 隐藏实体。"
  </acceptance_criteria>
  <done>任何公开事实、证据、scope、冲突、来源或媒体变化都可确定性使旧审核失效且无 review 自引用。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现四类 current-hash review 与原子 publication</name>
  <files>src/lib/publication.ts, scripts/lib/phase19-fixtures.ts, scripts/check-evidence-contract.ts, scripts/check-publication-gate.ts</files>
  <read_first>src/lib/publication.ts, migrations/031_evidence_readiness_v2.sql, scripts/check-evidence-contract.ts, scripts/check-publication-gate.ts, scripts/lib/phase19-fixtures.ts</read_first>
  <behavior>
    - 缺 fact/language/media current-hash approval 或任一 evidence/source/scope/conflict blocker -> publish rollback、无 public membership。
    - 完整 brand fixture先发布，再完整 pen fixture发布 -> 两者进入 public_entities，pen恰好一个 public made_by。
    - 直接 SQL 设置 published、stale hash/revision/contract/reviewer/time -> DB guard拒绝。
    - blocker query 前 publication lifecycle row 已在同一 transaction 内写入 current v2 hash/revision/contract/reviewer snapshot；失败 rollback 后该 snapshot 与 publication review 均不存在。
  </behavior>
  <action>实现 `recordEntityContentReview(db,{entityId,reviewKind,reviewer,status,notes})`：在稳定 content hash 上只允许 fact/language/media，写 current-hash review；publication kind 由 `publishEntity` 在单次 bounded write transaction 中 upsert。publish 的固定顺序必须是：(1) 计算 current hash 与 current `content_revision`，校验 fact/language/media 三类 approved review 都精确绑定该 hash；(2) 将 `entity_publications` 写为 `status='in_review'`, `approved_content_hash=currentHash`, `reviewed_content_revision=currentRevision`, `reviewed_contract_version=2`, `reviewed_by/reviewed_at=current reviewer snapshot`, `published_at=NULL`；(3) upsert 同 hash 的 approved publication review；(4) 此后才查询 contract-2 blockers，其中每个 approved core claim 必须完整通过 claim_evidence→approved citation→nonempty locator→explicit scope→qualifying source item/tier/group/allowed-use，否则 exact `approved_claim_missing_evidence` 不可被其他分数/条件抵消；(5) zero blocker 时更新 `status='published'` 与 `published_at`；(6) assert `public_entities` membership；(7) commit。任何 blocker/guard/membership 失败都 rollback，恢复调用前 publication row并移除本次 publication review，不 retry。`setEntityPublicationStatus` 继续只允许 non-published states。扩 `--readiness-publish` 与既有 `--publish`：断言 blocker 查询可见 current v2 publication snapshot、每一个 hard blocker 单独失败、rollback 无半审核状态、完整 brand+pen成功、critical edit隐藏，重新前三审后由 publish transaction 刷新 final review 并可再发布；不得加入测试绕过开关或共享 expected public predicate。</action>
  <verify>
    <automated>pnpm check:evidence-contract -- --readiness-publish &amp;&amp; pnpm check:publication-gate -- --publish</automated>
  </verify>
  <acceptance_criteria>
    - "事实、中文、媒体、publication 四类 review 均独立绑定同一 current hash。"
    - "任一 blocker使 entity publishable=false，quality score/其他优点不能抵消。"
    - "approved core claim 的 citation/locator/scope/source provenance 任一缺失都以 approved_claim_missing_evidence 使 publishEntity rollback，且 direct SQL 不可绕过。"
    - "blocker query 前 entity_publications 已持有 current hash/revision/contract/reviewer snapshot；因此 hash/revision/reviewer blockers 只反映真实不一致，不是事务顺序造成的假 blocker。"
    - "direct SQL、stale first-three review、failed transaction 无法留下 published、in_review v2 snapshot或本次 publication review。"
    - "完整 brand+pen fixture 原子公开，critical mutation 后立即退出 public_entities。"
  </acceptance_criteria>
  <done>contract-2 publication 只能在四审、证据、scope、来源、冲突、媒体与 made_by 全部合格时原子成功。</done>
</task>

</tasks>

## Artifacts this phase produces

Symbols：`PUBLICATION_CONTRACT_VERSION = 2`, `ReviewKind`, `recordEntityContentReview()`, 扩展后的 `readPublicationContentPayload()`, `computePublicationContentHash()`, `publishEntity()`。  
CLI flags：`check:evidence-contract -- --hash-invalidation|--readiness-publish`，扩展 `check:publication-gate -- --invalidation|--publish`。  
DB objects：消费 Plan 02 的 contract-2 views/triggers，不新增旁路 view。  
Paths：publication service与qualification fixture builders；不触碰real inventory或UI。

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Raw content/evidence → canonical hash | 非确定排序、漏 claim classification/evidence/scope/provenance 会错误复用审核。 |
| Review caller → review ledger | caller 可能伪造 publication review 或复用 stale hash。 |
| Server transaction/direct SQL → public_entities | 两条写路径必须独立 fail closed。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-19-07 | Tampering | canonical payload/hash | high | mitigate | fixed keys/nulls、stable sort/NFC、spec/claim evidence/classification/locator/scope/provenance 全 dependency mutation matrix、review exclusion test。 |
| T-19-08 | Elevation of Privilege | direct SQL publication bypass | high | mitigate | DB guards + server blocker recheck + final membership assertion + negative direct-SQL matrix。 |
| T-19-09 | Repudiation | hash-bound reviews | high | mitigate | four distinct kinds、reviewer/time/status、unique current hash；blocker query 前先写 in_review v2 publication snapshot再写transaction-owned final review，失败整笔回滚。 |
| T-19-10 | Tampering | ambiguous write retry | high | mitigate | exactly one bounded write transaction、rollback original error、transaction-call count fixture。 |

**Blocking threshold:** 任一 unresolved high/critical threat 阻止 Plan 04。
</threat_model>

<verification>
- `pnpm check:evidence-contract -- --hash-invalidation`
- `pnpm check:evidence-contract -- --readiness-publish`
- `pnpm check:publication-gate -- --invalidation`
- `pnpm check:publication-gate -- --publish`
- `pnpm exec tsc --noEmit`
</verification>

<success_criteria>
- EVID-01..06、AUD-02/AUD-03、QA-01 的 runtime hash/review/publication链可由disposable fixtures重放；approved core claim 的逐 claim evidence gate 已进 hash/invalidation/blocker。
- 任一 hard blocker、stale review 或 direct SQL 都不能产生 public membership；完整 fixture 原子成功。
</success_criteria>

<output>
Create `.planning/phases/19-real-audit-evidence/19-03-SUMMARY.md` when done.
</output>
