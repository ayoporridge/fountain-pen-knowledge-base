---
phase: 19-real-audit-evidence
plan: "05"
type: execute
wave: 5
depends_on: ["19-04"]
files_modified:
  - scripts/lib/phase19-fixtures.ts
  - scripts/check-evidence-contract.ts
  - scripts/check-audit-readiness.ts
  - scripts/check-publication-gate.ts
  - scripts/check-public-boundary.ts
  - scripts/check-phase19-regression.ts
  - tests/e2e/publication-gate.spec.ts
  - .planning/phases/19-real-audit-evidence/artifacts/inventory-readiness-v2.ndjson
  - .planning/phases/19-real-audit-evidence/artifacts/inventory-readiness-v2.csv
  - .planning/phases/19-real-audit-evidence/artifacts/inventory-readiness-v2-summary.json
autonomous: true
requirements: [EVID-01, EVID-02, EVID-03, EVID-04, EVID-05, EVID-06, AUD-01, AUD-04, AUD-05, QA-01]
must_haves:
  truths:
    - "QA-01: 每个负例产生精确 blocker；approved core claim 缺 citation、locator、scope 或 qualifying source provenance 的四个单变量负例均精确命中 approved_claim_missing_evidence；完整 brand+pen fixture 经四审原子公开。"
    - "D-01/D-07: real read-only inventory 精确为69 brand+236 pen=305；legacy exclusion与made_by dispositions逐项等于locked live facts。"
    - "D-02: real source inventory snapshot 在pre-migration copy上锁定，readiness在同一copy canonical升至031后计算；最终artifacts 305/305且limit invariant。"
    - "D-05/AUD-05: published blocker=0且所有public surfaces遵循唯一public_entities的四种parity语义。"
    - "D-06: 全套fixture/browser/build都用disposable DB，real main/WAL/SHM不变，无remote/deploy。"
  artifacts:
    - path: "scripts/check-evidence-contract.ts"
      provides: "complete evidence negative/positive fixture oracle"
    - path: "scripts/check-audit-readiness.ts"
      provides: "real baseline and artifact invariant oracle"
    - path: "scripts/check-public-boundary.ts"
      provides: "contract-2 independent four-semantics public parity"
    - path: "scripts/check-phase19-regression.ts"
      provides: "build/Playwright 共用的 canonical-migrated disposable DB wrapper 与 real-catalog invariant guard"
    - path: ".planning/phases/19-real-audit-evidence/artifacts/inventory-readiness-v2.ndjson"
      provides: "verified 305-row canonical ledger"
  key_links:
    - from: "complete qualification fixture"
      to: "publishEntity/public_entities/brand reverse model list"
      via: "real contract data, no test bypass"
      pattern: "recordEntityContentReview|publishEntity"
    - from: "real audit artifacts"
      to: "raw entities/legacy baseline/made_by/contract-2 readiness"
      via: "explicit readonly source → SQLite online backup → pre-migration inventory snapshot → canonical migration031 copy → independent expected constants"
      pattern: "--real-artifacts|backupCatalogToDisposableCopy|--verify-baseline"
    - from: "scripts/check-phase19-regression.ts"
      to: "build and Playwright child processes"
      via: "one owned migrated temp DB with cleared Turso env and finally cleanup/snapshot comparison"
      pattern: "runPhase19RegressionWithDisposableDatabase"
---

<objective>
以完整负例/正例、真实305-row只读台账和独立public parity完成Phase 19验收（D-01..D-07），不修复任何被发现的内容或taxonomy问题。

Purpose: 证明契约不仅在单元式fixture成立，也能准确描述当前all-draft真实库存，同时保持Phase18唯一公开集合与所有surface不回退。
Output: QA-01全矩阵、locked live inventory evidence、更新后的Phase18 regression fixtures、最终deterministic artifacts与全套本地gate。
</objective>

<execution_context>
@/Users/xz/.codex/gsd-core/workflows/execute-plan.md
@/Users/xz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-real-audit-evidence/19-CONTEXT.md
@.planning/phases/19-real-audit-evidence/19-RESEARCH.md
@.planning/phases/19-real-audit-evidence/19-PATTERNS.md
@.planning/phases/19-real-audit-evidence/19-VALIDATION.md
@.planning/phases/19-real-audit-evidence/19-04-SUMMARY.md
@.planning/phases/18-publication-gate/18-VERIFICATION.md
@scripts/check-evidence-contract.ts
@scripts/check-audit-readiness.ts
@scripts/check-publication-gate.ts
@scripts/check-public-boundary.ts
@tests/e2e/publication-gate.spec.ts
</context>

<scope_constraints>
- Real catalog only via required explicit absolute readonly path and before/after main/WAL/SHM snapshots; never remove sidecars. Its SQLite online backup is task-owned and disposable; every migration/readiness write/browser/build DB is that copy or another owned disposable DB.
- No Turso/remote, push, deploy, network content research, bulk writing, search/LLM, UI renderer or taxonomy/content/made_by repair.
- Do not modify migration 030 or add package/test runner. Preserve public_entities sole authorization and independent expected oracles.
- `19-VALIDATION.md` status/evidence may only be updated by executor after commands actually pass; this plan does not pre-mark it green.
</scope_constraints>

<tasks>

<task type="auto">
  <name>Task 1: 完成 QA-01 evidence/readiness fixture matrix</name>
  <files>scripts/lib/phase19-fixtures.ts, scripts/check-evidence-contract.ts, scripts/check-publication-gate.ts</files>
  <read_first>scripts/lib/phase19-fixtures.ts, scripts/check-evidence-contract.ts, scripts/check-publication-gate.ts, migrations/031_evidence_readiness_v2.sql, src/lib/publication.ts, .planning/phases/19-real-audit-evidence/19-CONTEXT.md</read_first>
  <action>扩 `--all` 为互相独立的cases：deprecated story、pending claim、needs_source spec、mirror/repost同independence group、非空spec field无逐字段citation/locator、retailer-only、unresolved same-scope field conflict、identity/made_by conflict、stale fact/language/media/publication review hash，以及一组完整brand+pen。另从同一个完全合格的 approved core claim fixture 分别只移除：(1) claim citation mapping，(2) nonempty evidence locator，(3) explicit fact scope，(4) qualifying source-item provenance/tier/group/allowed-use；四个 case 必须互相隔离、其余条件均保持合格，每个都断言 exact blocker code `approved_claim_missing_evidence`、对应 missing-component detail、readiness=false、direct SQL/publication 失败与 public absence。每个其他负例也只改变一个变量并断言exact blocker code/detail和public absence。stale fact/language/media 各自必须让 `publishEntity` 以 `missing_fact_review`/`missing_language_review`/`missing_media_review` rollback，且不留in_review v2 snapshot或publication review；stale publication review 的语义不同：在调用publish前的readiness与direct-SQL published尝试中必须精确产生 `missing_publication_review` 并拒绝公开，但 `publishEntity` 正是final review owner，必须在同一transaction刷新current-hash publication review并在其余条件合格时成功，不得把旧publication review当成API应失败的负例。完整fixture必须有primary/archive+独立professional-secondary、scope、spec field citations、完整 approved core claim evidence chain、resolved/no conflict、approved reusable primary media、前三类review后由publish transaction写final review。更新Phase18 publication fixtures使用真实qualification builder而非绕过，保持direct SQL/rollback/no-retry检查。</action>
  <verify>
    <automated>pnpm check:evidence-contract -- --all &amp;&amp; pnpm check:publication-gate -- --migration-full</automated>
  </verify>
  <acceptance_criteria>
    - "context指定的10类case全部有exact blocker/result，不共享会掩盖原因的多重失败。"
    - "approved core claim 缺citation、locator、scope、qualifying source provenance 的四个单变量 case 均只命中 approved_claim_missing_evidence 及对应detail，并各自阻止readiness/publication。"
    - "mirror同组计1；retailer-only不能解锁；identity conflict阻止entity，field conflict阻止field/entity readiness。"
    - "stale publication review 在pre-publish readiness/direct SQL精确命中missing_publication_review；publishEntity刷新final review后成功，stale前三审仍各自rollback。"
    - "完整brand+pen无blocker并原子published；所有writes仅在owned fixture。"
  </acceptance_criteria>
  <done>QA-01与EVID-01..06有完整、独立、可重放的negative/positive contract matrix。</done>
</task>

<task type="auto">
  <name>Task 2: 对真实 catalog 生成并锁定305-row inventory evidence</name>
  <files>scripts/check-audit-readiness.ts, .planning/phases/19-real-audit-evidence/artifacts/inventory-readiness-v2.ndjson, .planning/phases/19-real-audit-evidence/artifacts/inventory-readiness-v2.csv, .planning/phases/19-real-audit-evidence/artifacts/inventory-readiness-v2-summary.json</files>
  <read_first>scripts/check-audit-readiness.ts, scripts/audit-readiness-v2.ts, src/lib/audit/readiness-audit.ts, src/lib/audit/read-only-catalog.ts, .planning/phases/19-real-audit-evidence/19-CONTEXT.md, .planning/phases/19-real-audit-evidence/19-VALIDATION.md</read_first>
  <action>扩 `check:audit-readiness -- --real-artifacts` 为唯一最终产物入口。它用 explicit absolute `--database-path "$PWD/data/fpkg.db"` readonly打开source并snapshot main/WAL/SHM，以 SQLite online backup 写入owned temp DB；在副本迁移前读取raw identities与 `migrations(name, applied_at, checksum)`，只从符合三位数前缀格式的 `name` 提取 max migration，锁定 `source_inventory_snapshot_id`、`source_schema_max_migration=030` 及对应name/checksum provenance，再只对副本调用canonical `migrateDatabase`升至 `audit_schema_max_migration=031` 并锁定031 name/checksum，readiness/qualifying views全部在该post-copy-migration DB上查询。fixture 必须精确断言 030→031 和两端 migration provenance，不得查询任何不存在的 migration table；`applied_at` 不进 deterministic artifact。checker 先分别用unlimited与`--limit=1`运行到两个owned temp out-dir，比较NDJSON/CSV/summary三件bytes+SHA、summary object、inventory/content/public verdict及exit semantics完全一致；只有比较、baseline与最终source snapshot guard全部通过，才把unlimited已验证bytes经目标目录内temp-file+rename写到显式 `--final-out-dir` 的固定三paths，失败不得留下半套文件。最终summary/每row provenance必须明确 source snapshot/schema 与 `audit_database_kind='owned_disposable_migrated_copy'`，不得暗示真实source已迁移。baseline必须精确断言：69 brands +236 pens=305；305 publication rows全为draft；legacy false只有brands `banju`,`saier`,`shanghai`,`yongxu`与pens `百乐-pilot-custom-823`,`百利金-pelikan-m800`,`派克-parker-51-经典-vintage`,`写乐-sailor-21k-pro-gear-大鱼雷`,`奥罗拉-aurora`。made_by必须为230 exactly-one、5 missing slugs `the-camel-pen`,`the-j-g-rider-fountain-pen`,`the-john-hancock-cartridge-pen`,`the-postal-reservoir-pen`,`the-security-pen`、1 multiple `英雄派迪-一体尖`且targets为Hero与Hero Paddy。将这些作为disposition evidence，不修改relation/entity/content。报告同时给出content-ready与published实际数；即使published=0且published blockers=0，也只能表示公开面干净，不能表示内容完成。运行前后逐项比较source main/WAL/SHM，发现外部变化立即失败且不清理sidecar。</action>
  <verify>
    <automated>pnpm check:audit-readiness -- --real-artifacts --database-path "$PWD/data/fpkg.db" --final-out-dir "$PWD/.planning/phases/19-real-audit-evidence/artifacts"</automated>
  </verify>
  <acceptance_criteria>
    - "NDJSON与CSV均305 identities，69/236 counts与双向raw equality通过。"
    - "legacy exact 9、all 305 draft、made_by 230/5/1及具体slugs/targets完全匹配。"
    - "unlimited与limit=1各自先写owned temp dir，三artifact bytes/hashes、summary、verdict、exit完全相等后才原子更新fixed paths。"
    - "final artifacts标明source snapshot/schema=030与audit copy schema=031；readiness从未直接查询030 source，main/WAL/SHM before/after相等。"
    - "source/audit schema max 仅从 migrations.name 三位前缀得出，对应 030/031 name+checksum provenance 有 exact fixture assertion。"
    - "所有内容/taxonomy/made_by问题仅记录blocker/disposition，无silent repair。"
  </acceptance_criteria>
  <done>2026-07-15真实库存以可复核、零抽样、只读且确定的305-row evidence冻结。</done>
</task>

<task type="auto">
  <name>Task 3: 复验唯一public universe与完整brand-model parity</name>
  <files>scripts/lib/phase19-fixtures.ts, scripts/check-public-boundary.ts, scripts/check-publication-gate.ts, scripts/check-phase19-regression.ts, tests/e2e/publication-gate.spec.ts</files>
  <read_first>scripts/check-public-boundary.ts, scripts/check-publication-gate.ts, tests/e2e/publication-gate.spec.ts, scripts/lib/phase19-fixtures.ts, src/lib/publication.ts, migrations/031_evidence_readiness_v2.sql, .planning/phases/18-publication-gate/18-VERIFICATION.md</read_first>
  <action>把Phase18 independent known-ID fixtures升级为完整contract-2 evidence/review数据，expected side继续只用已知IDs/raw SQL，禁止import runtime visibility/readiness/audit helper。`--all`分别验证完整lists双向等价、governed per-ID detail/API等价、facets/stat keyed equality、graph/recommend/wiki contextual subset、每brand reverse made_by public set双向等价；完整pen publish后自动出现在brand全型号list，critical edit/retire后next read消失。Playwright fixture验证hard404→qualified publish→brand link→invalidate→retire lifecycle。新增 `scripts/check-phase19-regression.ts` 的 `runPhase19RegressionWithDisposableDatabase()`：先snapshot真实catalog main/WAL/SHM，创建并登记一个mkdtemp DB，清空Turso URL/token，设置 `FPKG_DATABASE_URL=file:<owned-db>` 与 `PUBLICATION_GATE_FIXTURE=1`，只用canonical `migrateDatabase`升至031，然后把同一显式env传给`pnpm build`、`pnpm test:e2e:desktop`、`pnpm test:e2e:mobile` child；success、child failure、SIGINT/SIGTERM均在finally停止children、close client、删除owned root并断言真实main/WAL/SHM不变。最终build/Playwright不得再裸跑或隐式解析真实/remote DB。</action>
  <verify>
    <automated>pnpm check:publication-gate &amp;&amp; pnpm check:public-boundary -- --all &amp;&amp; pnpm exec tsx scripts/check-phase19-regression.ts --build-e2e</automated>
  </verify>
  <acceptance_criteria>
    - "published blocker count=0；list/per-ID/aggregate/context/reverse-model五类diff全部0。"
    - "expected oracle不复用public/readiness/audit predicate，public_entities仍是唯一actual授权集合。"
    - "完整pen自动进入brand反向全型号集合，invalidate/retire后立即退出。"
    - "build与desktop/mobile Playwright共享同一canonical-migrated disposable DB；成功/失败/signal均cleanup且real catalog snapshot不变。"
  </acceptance_criteria>
  <done>AUD-05与Phase18全部公开边界在contract 2下保持独立、精确、fail-closed。</done>
</task>

</tasks>

## Artifacts this phase produces

Final CLI flags：`check:evidence-contract -- --all`、`check:audit-readiness`全flags、`audit:readiness-v2 -- --database-path|--inventory-only|--verify-baseline|--limit`、扩展后的publication/public-boundary commands。  
Final DB objects：migration031全部evidence/scope/conflict/review/qualifying/readiness/guard objects；`public_entities`仍唯一。  
Final symbols：qualification fixture builders、contract-2 review/hash/publish、canonical audit serializers、`runPhase19RegressionWithDisposableDatabase()`。  
Final paths：三件已验证305-row artifacts、更新的contract/parity/browser checks、`scripts/check-phase19-regression.ts`；没有remote或content产物。

## Multi-Source Coverage Audit

| SOURCE | ID | Feature / requirement | Plan | Status |
|---|---|---|---|---|
| GOAL | — | 真实可见状态全库审计与逐事实可验证 | 02-05 | COVERED |
| REQ | EVID-01..06 | field evidence、source groups、scope、conflict、reviews、source mix | 02,03,05 | COVERED |
| REQ | AUD-01..04 | 305 ledger、status truth、single blocker、limit-only display | 01,04,05 | COVERED |
| REQ | AUD-05 | published blockers zero + all-surface parity | 05 | COVERED |
| REQ | QA-01 | required negative and complete positive fixtures | 01-03,05 | COVERED |
| CONTEXT | D-01 | 69+236/305、legacy separate、backlog separate | 04,05 | COVERED |
| CONTEXT | D-02 | zero sampling、deterministic、limit console-only | 01,04,05 | COVERED |
| CONTEXT | D-03 | field evidence/source thresholds、no collection | 02,03,05 | COVERED |
| CONTEXT | D-04 | scope/conflict/four reviews | 02,03,05 | COVERED |
| CONTEXT | D-05 | contract 2 fail-closed、sole public view | 02,03,05 | COVERED |
| CONTEXT | D-06 | readonly real/disposable writes/no remote | 01-05 | COVERED |
| CONTEXT | D-07 | all made_by and reverse sets, report-only | 04,05 | COVERED |
| RESEARCH | — | table rebuild、canonical hash、central views、audit serializers、安全与validation | 01-05 | COVERED |

Deferred Phase20-26 renderer、taxonomy、Montblanc research、bulk content、expansion与production rollout全部排除，不构成gap。

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Fixture expected oracle → runtime actual | 共用predicate会产生自证。 |
| Real catalog → final artifacts | 读取可能误写或漏/改行。 |
| Contract-2 publish → all public surfaces | surface可能复制旧predicate或缓存stale结果。 |
| Build/Playwright child → database resolver | 裸命令可能继承真实或Turso配置，且失败/signal可能泄漏临时资源。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-19-15 | Tampering | real catalog mutation/remote selection | high | mitigate | explicit readonly path、remote reject、main/WAL/SHM snapshots、writes disposable；failure blocks phase。 |
| T-19-16 | Tampering | incomplete ledger/limit verdict | high | mitigate | exact 305 identities、locked extra-nine/made_by facts、artifact hash+exit invariance。 |
| T-19-17 | Elevation of Privilege | direct SQL/public predicate bypass | high | mitigate | DB guards、transaction、independent oracle、all-surface parity diffs。 |
| T-19-18 | Injection | CSV artifact | high | mitigate | Plan04 serializer attack fixtures rerun before accepting real artifacts。 |
| T-19-19 | Tampering | migration031 rebuild/provenance regression | high | mitigate | full migration matrix precedes parity/E2E；从 migrations.name 三位前缀精确断言030→031与name/checksum provenance；quick/FK/checksum/manifest must stay green。 |
| T-19-20 | Tampering / Resource leak | final build/Playwright DB selection | high | mitigate | 单一wrapper创建并canonical migrate owned DB、清空Turso、向所有child传同一FPKG_DATABASE_URL，finally处理success/failure/signals并比较真实main/WAL/SHM。 |

**Blocking threshold:** 任一 unresolved high/critical threat 阻止Phase 19完成；不得以0 public或部分绿色替代。
</threat_model>

<verification>
- `pnpm check:evidence-contract -- --all`
- `pnpm check:audit-readiness -- --fixture &amp;&amp; pnpm check:audit-readiness -- --artifacts-limit`
- `pnpm check:audit-readiness -- --real-artifacts --database-path "$PWD/data/fpkg.db" --final-out-dir "$PWD/.planning/phases/19-real-audit-evidence/artifacts"`
- `pnpm check:migrations &amp;&amp; pnpm check:publication-gate &amp;&amp; pnpm check:public-boundary -- --all &amp;&amp; pnpm check:library`
- `pnpm lint`
- Disposable DB wrapper only: `pnpm exec tsx scripts/check-phase19-regression.ts --build-e2e`
</verification>

<success_criteria>
- 全12 requirement IDs与D-01..D-07均有执行证据，source audit无missing项。
- 真实inventory 305/305、legacy/made_by/live publication事实精确，artifacts deterministic且主库不变。
- QA-01、migration、direct-SQL、publication、public parity、browser与build全绿；无remote/deploy/content/taxonomy动作。
</success_criteria>

<output>
Create `.planning/phases/19-real-audit-evidence/19-05-SUMMARY.md` when done.
</output>
