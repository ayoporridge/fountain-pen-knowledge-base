---
phase: 20-renderer
plan: "02"
type: execute
wave: 2
depends_on:
  - 20-01
files_modified:
  - src/lib/markdown.ts
  - src/components/library/EncyclopediaShell.tsx
  - src/components/library/BrandMuseum.tsx
  - src/components/library/ModelArchive.tsx
  - src/app/[type]/[slug]/page.tsx
  - tests/renderer/components.test.tsx
autonomous: true
requirements:
  - PAGE-01
  - PAGE-04
  - PAGE-06
must_haves:
  truths:
    - "Brand and model pages server-render the complete current published story plus a distinct 60–160-character summary."
    - "Brand pages render an exact image, at least two sourced timeline nodes, all published models, and qualified sources."
    - "Model pages render the complete required story topics, evidence-backed specs, optional qualified variants, qualified sources, and one canonical brand."
    - "Navigation IDs and links are derived from the same sanitized Markdown AST and optional modules produce no empty UI."
  artifacts:
    - path: "src/lib/markdown.ts"
      provides: "One-pass sanitized Markdown document with stable heading IDs"
      exports: ["StoryHeading", "RenderedMarkdownDocument", "renderMarkdownDocument"]
    - path: "src/components/library/EncyclopediaShell.tsx"
      provides: "Pure server-rendered shared encyclopedia composition"
      exports: ["EncyclopediaShell"]
    - path: "src/components/library/BrandMuseum.tsx"
      provides: "Pure-props brand facts renderer"
      exports: ["BrandMuseum"]
    - path: "src/components/library/ModelArchive.tsx"
      provides: "Pure-props model facts renderer"
      exports: ["ModelArchive"]
  key_links:
    - from: "src/app/[type]/[slug]/page.tsx"
      to: "src/lib/entity-page.ts"
      via: "request-cached getPublishedEntityPage for brand/pen metadata and page rendering"
      pattern: "getPublishedEntityPage"
    - from: "src/components/library/EncyclopediaShell.tsx"
      to: "src/lib/markdown.ts"
      via: "pre-sanitized HTML and headings from one renderMarkdownDocument call"
      pattern: "renderMarkdownDocument"
    - from: "src/components/library/EncyclopediaShell.tsx"
      to: "src/components/MarkdownHtml.tsx"
      via: "existing sanitized prose-body boundary"
      pattern: "MarkdownHtml"
---

<objective>
Replace the brand/model archive branches with one pure server encyclopedia renderer whose summary, story, evidence modules, exact media, canonical relations, metadata, and JSON-LD all consume the Plan 20-01 view model.

Purpose: Make PAGE-01, PAGE-04, and PAGE-06 visible without reintroducing component SQL, legacy content, generic media, or empty-state UI.
Output: Same-AST Markdown headings, shared encyclopedia shell, pure brand/model facts, and a thin fail-closed detail route.
</objective>

<execution_context>
@/Users/xz/.codex/gsd-core/workflows/execute-plan.md
@/Users/xz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/20-renderer/20-RESEARCH.md
@.planning/phases/20-renderer/20-PATTERNS.md
@.planning/phases/20-renderer/20-UI-SPEC.md
@.planning/phases/20-renderer/20-VALIDATION.md
@.planning/phases/20-renderer/20-01-SUMMARY.md
@src/lib/entity-page.ts
@src/lib/markdown.ts
@src/components/MarkdownHtml.tsx
@src/components/library/BrandMuseum.tsx
@src/components/library/ModelArchive.tsx
@src/app/[type]/[slug]/page.tsx

<interfaces>
From Plan 20-01: `getPublishedEntityPage(type: "brand" | "pen", slug: string): Promise&lt;PublishedPageData | null&gt;`; `PublishedPageData` discriminates `BrandPageData` and `ModelPageData`; required invariants have already failed before rendering.

Existing Markdown seam: `renderMarkdown(md: string, resolveHref?: (slug: string) =&gt; Promise&lt;string | null&gt; | string | null): Promise&lt;string&gt;` in `src/lib/markdown.ts`, with `rehypeNormalizeHeadings` before URL sanitation/stringify. Preserve this exact API for existing consumers while adding the document-returning API.

Existing HTML seam: `MarkdownHtml` in `src/components/MarkdownHtml.tsx` renders already-sanitized HTML inside `.prose-body`.

Existing route seams: canonical redirect then `notFound()` in `EntityPage`; safe JSON-LD serialization; semantic breadcrumb. Preserve non-brand/non-pen behavior.
</interfaces>
</context>

<execution_preflight>
Before any task, read `.planning/phases/19-real-audit-evidence/19-VERIFICATION.md`. Do not rerun the Phase 19 wrapper. If status is `gaps_found`, continue only under the accepted user-directed content-track exception, record that exception in `20-02-SUMMARY.md`, and do not imply the Phase 19 debt was resolved. Any other blocking verdict stops execution for user direction.
</execution_preflight>

## Artifacts this phase produces

Plan 20-02 owns these new symbols and files:

- `src/lib/markdown.ts`: exported `StoryHeading`, `RenderedMarkdownDocument`, and `renderMarkdownDocument`; existing `renderMarkdown` remains compatible.
- `src/components/library/EncyclopediaShell.tsx`: exported `EncyclopediaShell`; internal pure components `EntityHeader`, `PrimaryMedia`, `StoryArticle`, `SectionNav`, `QualifiedSources`, `CanonicalRelations`, and `ExploreMore`.
- `tests/renderer/components.test.tsx`: focused same-AST and pure server-markup assertions using `react-dom/server`.
- `src/app/[type]/[slug]/page.tsx`: request-cached local loader symbol `loadPublishedEntityPage`; no new route.
- Existing `BrandMuseum` and `ModelArchive` exports become pure props; no parallel renderer, client data fetch, schema, flag, or package dependency is introduced.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add same-AST heading IDs and the shared pure server shell</name>
  <files>src/lib/markdown.ts, src/components/library/EncyclopediaShell.tsx, tests/renderer/components.test.tsx</files>
  <read_first>
    - src/lib/markdown.ts
    - src/components/MarkdownHtml.tsx
    - src/lib/entity-page.ts
    - .planning/phases/20-renderer/20-PATTERNS.md
    - .planning/phases/20-renderer/20-UI-SPEC.md
    - .planning/phases/19-real-audit-evidence/19-VERIFICATION.md
  </read_first>
  <behavior>
    - Duplicate Chinese H2/H3 labels -> deterministic unique IDs with stable `-2`, `-3` suffixes.
    - Sanitized story HTML heading IDs -> exact one-to-one match with `StoryHeading[]`; no regex or second parse.
    - Optional module absent -> both module and navigation item absent.
    - Summary, story, sources, media attribution, and canonical link -> present in server response HTML before hydration.
    - Raw HTML and `javascript:` link payloads -> sanitized and never emitted as executable markup or URL.
  </behavior>
  <action>
Add `renderMarkdownDocument` to the existing pipeline. Insert one heading-ID/collection transform after `rehypeNormalizeHeadings` and before URL sanitation/stringify; extract readable text from H2/H3 nodes, retain usable Chinese slugs, deterministically suffix duplicates, assign the same IDs into the AST, and return `{ html, headings }`. Keep `renderMarkdown` as a delegating compatibility API and preserve every existing raw-HTML and URL/image sanitation stage; do not add a package or another parser.

Create pure server component `EncyclopediaShell({ data, document })`. Its DOM order is breadcrumb/header/summary/canonical brand, exact primary figure, heading-derived navigation, complete story, type-specific facts, qualified sources, related facts, then optional explore. Render story HTML only through `MarkdownHtml`. Implement semantic `<figure>` media with entity-specific alt, visible attribution/license/source, and no fallback. Build SectionNav from `document.headings` plus modules that actually exist; omit it when fewer than two targets exist. Keep all required content server-rendered and expose class hooks for Plan 20-03 without adding client fetch, reveal, fixed-height, clamp, or accordion behavior. Create `tests/renderer/components.test.tsx` with Node test + `react-dom/server` assertions for same-AST IDs, one H1, H2/H3 structure, server markup, summary/story separation, optional-module omission, hostile raw HTML, and `javascript:` URLs. Assert hostile inputs are removed or rendered inert by the existing sanitation pipeline.
  </action>
  <acceptance_criteria>
    - One AST traversal owns both sanitized heading IDs and navigation data, including duplicate Chinese headings.
    - `renderMarkdown` callers outside Phase 20 keep their current return type and sanitation behavior.
    - Shell children receive props only; none accepts an entity ID or imports DB helpers.
    - Required content and canonical links are present in the initial response HTML.
    - Raw HTML/script payloads and JavaScript-scheme URLs cannot survive as executable output.
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec tsx --test --test-name-pattern "heading|server markup|raw HTML|javascript URL" tests/renderer/components.test.tsx</automated>
  </verify>
  <done>The shared server shell renders one sanitized story document whose emitted IDs exactly power its present-module navigation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Convert BrandMuseum and ModelArchive into evidence-aware pure facts</name>
  <files>src/components/library/BrandMuseum.tsx, src/components/library/ModelArchive.tsx, tests/renderer/components.test.tsx</files>
  <read_first>
    - src/components/library/BrandMuseum.tsx
    - src/components/library/ModelArchive.tsx
    - src/lib/entity-page.ts
    - .planning/phases/20-renderer/20-UI-SPEC.md
    - tests/renderer/components.test.tsx
  </read_first>
  <behavior>
    - Brand facts -> semantic sourced timeline plus `全部型号（15）` and exactly 15 unique model links.
    - Model facts -> every visible spec has Chinese label/value/source and optional locator; numeric zero remains visible.
    - Qualified variants -> complete vertical list when present; no heading/panel/nav target when absent.
    - Empty/unqualified collections -> no public empty card, dash, internal status, or diagnostic text.
  </behavior>
  <action>
Convert `BrandMuseum` from an async entity-ID query component to a synchronous pure-props component receiving the `BrandPageData` timeline/models subset. Preserve the useful model-list and timeline presentation shapes, but use semantic list markup, visible per-event source links, complete stable model links, natural wrapping, minimum 44px targets, and the exact `全部型号（{models.length}）` heading. Remove all DB imports, source/identifier ownership, truncation controls, and empty branches.

Convert `ModelArchive` to a synchronous pure-props component receiving the `ModelPageData` specs/variants subset. Remove `prepareSourceMaterial`, `sourceBody`, all DB helpers, canonical-brand duplication, and empty branches. Render evidence specs as semantic definition cards containing the user-facing label, value, `来源：{source title}`, and locator only when present; keep zero values. Render qualified variants as a complete vertical list with source affordance and omit the section when empty. Extend `evidence modules` tests to prove invalid sentinels and forbidden public empty-state copy are absent, while all qualified facts and 15 exact relation links are present.
  </action>
  <acceptance_criteria>
    - Neither component is async or imports `src/lib/library.ts`, `src/lib/db.ts`, or legacy story/body data.
    - Brand count equals rendered unique model-link count and is not a recommendation/pagination count.
    - Every visible model fact has evidence text; optional variants disappear cleanly.
    - No internal field/status identifier or public empty-state panel is rendered.
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec tsx --test --test-name-pattern "evidence modules" tests/renderer/components.test.tsx</automated>
  </verify>
  <done>Brand and model fact modules are pure projections of the single page model with exact relations and visible evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Make the detail route a thin cached loader and renderer controller</name>
  <files>src/app/[type]/[slug]/page.tsx, tests/renderer/components.test.tsx</files>
  <read_first>
    - src/app/[type]/[slug]/page.tsx
    - src/lib/entity-page.ts
    - src/components/library/EncyclopediaShell.tsx
    - src/components/library/BrandMuseum.tsx
    - src/components/library/ModelArchive.tsx
    - .planning/phases/20-renderer/20-PATTERNS.md
  </read_first>
  <behavior>
    - Canonical alias -> redirect before loader; non-public or renderer-invariant brand/pen -> existing fail-closed public response without diagnostic leakage.
    - Brand/pen metadata, JSON-LD, and visible page -> same request-cached `PublishedPageData` summary and exact primary image.
    - No qualified primary image -> no generic social image or visible hero fallback.
    - Non-brand/non-pen entity types -> preserve current route behavior.
    - Complete model story -> all eight independent PAGE-06 topic sentinels survive server rendering without clamp, truncation, or summary substitution.
  </behavior>
  <action>
In `page.tsx`, define a request-cached `loadPublishedEntityPage` around the Plan 20-01 loader and use it for brand/pen `generateMetadata` and `EntityPage`; preserve canonical redirect before lookup. Convert `PublishedPageInvariantError` to the existing fail-closed public behavior while logging only server-side diagnostic context. Feed the same data to metadata description, optional exact image, safe JSON-LD, breadcrumb, and `EncyclopediaShell`. Remove brand/pen ownership of `preparePublicBody`, `penSourceBody`, weak library queries, hard-coded section arrays, generic hero/social fallback, placeholder hero, and `LIMIT 1` canonical-brand selection. Preserve other entity types surgically. Extend server-markup cases so the expected brand/model story, all eight independent model-topic sentinels, and all required PAGE modules are visible, while legacy/deprecated/generic sentinels are absent. Assert identity/product line, history, design/dimensions/materials/ergonomics, nib, attributed writing experience, filling/maintenance, variant boundaries, and purchase checks separately so tail truncation cannot pass.
  </action>
  <acceptance_criteria>
    - Brand/pen route and metadata use the same request-cached loader contract after canonical redirect.
    - Metadata description equals the complete reviewed summary; image is exact or omitted.
    - Page contains one H1, complete story, required facts/sources/media, and canonical relation in server HTML.
    - All eight PAGE-06 topic sentinels appear independently in the server-rendered model page.
    - Other entity types retain their prior rendering path.
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec tsx --test --test-name-pattern "route markup|PAGE-06 topics" tests/renderer/components.test.tsx &amp;&amp; pnpm exec tsc --noEmit</automated>
  </verify>
  <done>Brand and model URLs are thin fail-closed controllers over one pure server renderer and one page snapshot.</done>
</task>

</tasks>

<prohibitions>
- Do not retain new and old brand/model renderers in parallel or allow any component to query by entity ID.
- Do not use legacy `entities.body_md`, `prepareSourceMaterial`, deprecated/reviewed story, raw HTML bypass, generic social/hero media, or public empty-state copy.
- Do not hard-code story headings, parse headings with regex/a second Markdown pass, or make required content hydration-dependent.
- Do not edit schema/migrations, Phase 19 files, real inventory, Header/Footer/browse/home, or deployment configuration.
</prohibitions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| approved Markdown -> server HTML | Stored content crosses into executable browser markup. |
| page view model -> public DOM/metadata | Internal database facts become user-visible structured content. |
| external source/media URL -> browser link/image | Qualified URLs leave the application trust boundary. |

## STRIDE Threat Register (ASVS L1; block high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-20-05 | Tampering | `renderMarkdownDocument` | high | mitigate | Preserve existing raw HTML and URL sanitation; add IDs inside the same sanitized AST pipeline only. |
| T-20-06 | Information Disclosure | pure renderer props/DOM | medium | mitigate | Render allowlisted view-model fields and human-readable evidence only; omit internal IDs/status/enums. |
| T-20-07 | Spoofing | primary media and canonical relations | high | mitigate | Render only the loader-qualified exact image and authoritative relation zones; omit generic/recommended substitutes. |
| T-20-08 | Information Disclosure | invariant errors | medium | mitigate | Convert mismatch diagnostics to existing fail-closed public behavior and keep details server-side. |
</threat_model>

<verification>
- `pnpm exec tsx --test tests/renderer/components.test.tsx`
- `pnpm exec tsc --noEmit`
- `pnpm biome check src/lib/markdown.ts src/components/library/EncyclopediaShell.tsx src/components/library/BrandMuseum.tsx src/components/library/ModelArchive.tsx 'src/app/[type]/[slug]/page.tsx' tests/renderer/components.test.tsx`
</verification>

<success_criteria>
- PAGE-01 summary is complete, 60–160 characters, distinct from the full story, and server rendered for brand and model.
- PAGE-04 brand fixture shows exact image, complete story, at least two sourced timeline nodes, all models, and qualified sources.
- PAGE-06 model fixture exposes the complete published story with every fixture topic/attribution sentinel and no truncation.
- The renderer has no legacy/deprecated/unqualified/generic-media or empty-state escape hatch.
</success_criteria>

<output>
Create `.planning/phases/20-renderer/20-02-SUMMARY.md` when done.
</output>
