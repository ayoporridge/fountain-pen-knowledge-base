---
phase: 20-renderer
plan: "04"
type: execute
wave: 4
depends_on: [20-03]
files_modified:
  - scripts/check-renderer.ts
  - tests/e2e/renderer.spec.ts
  - playwright.config.ts
  - .planning/phases/20-renderer/20-VERIFICATION-EVIDENCE.md
  - .planning/phases/20-renderer/artifacts/brand-desktop.png
  - .planning/phases/20-renderer/artifacts/brand-mobile.png
  - .planning/phases/20-renderer/artifacts/model-desktop.png
  - .planning/phases/20-renderer/artifacts/model-mobile.png
autonomous: false
requirements: [PAGE-08]
must_haves:
  truths:
    - "A real desktop and mobile browser renders complete brand/model fixture pages with no page-level overflow."
    - "The fixture brand shows exactly 15 unique canonical model links/count, and the fixture model shows exactly one canonical brand link."
    - "Focused publication-boundary regression remains green without running the Phase 19 monolithic wrapper or broad E2E suite."
    - "Evidence explicitly says the fixture proves renderer behavior, not publication of the real 305-row inventory."
    - "A human approves each fixture hero's entity-to-asset-to-rights-to-attribution mapping from the saved desktop/mobile evidence."
  artifacts:
    - path: "scripts/check-renderer.ts"
      provides: "Narrow renderer DB/seed/command/cleanup runner over the existing serve-e2e seam"
      exports: ["runRendererCheck"]
    - path: "tests/e2e/renderer.spec.ts"
      provides: "Independent desktop/mobile PAGE-01..PAGE-08 browser matrix"
    - path: ".planning/phases/20-renderer/20-VERIFICATION-EVIDENCE.md"
      provides: "Commands, results, exact relation counts, hero asset approval matrix, scope proof, and fixture boundary"
  key_links:
    - from: "scripts/check-renderer.ts"
      to: "scripts/check-publication-gate.ts --serve-e2e"
      via: "borrowed explicit disposable DB server seam; no lifecycle implementation changes"
      pattern: "--serve-e2e"
    - from: "tests/e2e/renderer.spec.ts"
      to: "src/app/[type]/[slug]/page.tsx"
      via: "real loopback HTTP desktop/mobile assertions"
      pattern: "renderer-"
---

<objective>
Prove the complete Phase 20 renderer in independent desktop/mobile browsers, lock exact canonical relations, run one focused publication regression, and hand off durable evidence with the real-inventory boundary stated correctly.

Purpose: Close PAGE-08 and phase-wide browser confidence without reopening the Phase 19 monolithic verification loop.
Output: Minimal runner, independent E2E spec, four screenshots, focused regression results, and verification evidence.
</objective>

<execution_context>
@/Users/xz/.codex/gsd-core/workflows/execute-plan.md
@/Users/xz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/20-renderer/20-RESEARCH.md
@.planning/phases/20-renderer/20-PATTERNS.md
@.planning/phases/20-renderer/20-UI-SPEC.md
@.planning/phases/20-renderer/20-VALIDATION.md
@.planning/phases/20-renderer/20-03-SUMMARY.md
@.planning/phases/19-real-audit-evidence/19-05-SUMMARY.md
@scripts/lib/renderer-fixture.ts
@scripts/check-publication-gate.ts
@playwright.config.ts
@.planning/phases/19-real-audit-evidence/19-VERIFICATION.md

<interfaces>
Existing server seam: `pnpm check:publication-gate -- --serve-e2e --port PORT` borrows an explicit disposable `FPKG_DATABASE_URL` when `PUBLICATION_GATE_FIXTURE=1`; Phase 20 must call it without editing it.

Plan 20-01 fixture exports `createRendererFixture`, `seedRendererFixture`, and `cleanupRendererFixture`. The runner owns only this disposable DB/seed/command cleanup and ordinary child try/finally; it does not duplicate Phase 19 signal/process-group/descriptor machinery.
</interfaces>
</context>

<execution_preflight>
Read `.planning/phases/19-real-audit-evidence/19-VERIFICATION.md` before creating the browser fixture. Do not rerun the Phase 19 wrapper. If status is `gaps_found`, continue under the accepted user-directed content-track exception, quote that exception in `20-VERIFICATION-EVIDENCE.md` and `20-04-SUMMARY.md`, and do not imply the debt vanished. Any other blocking verdict stops execution for user direction.
</execution_preflight>

## Artifacts this phase produces

- `scripts/check-renderer.ts`: `runRendererCheck`; CLI flags `--spec=renderer|boundary`, `--project=desktop|mobile|desktop,mobile`, `--grep`, `--evidence-dir`.
- Child-only `RENDERER_FIXTURE=1`; existing `PUBLICATION_GATE_FIXTURE`, `FPKG_DATABASE_URL`, and generated loopback `E2E_BASE_URL` are reused.
- `tests/e2e/renderer.spec.ts`: `expectNoHorizontalOverflow`, four PAGE browser groups, exact relation assertions, and screenshot capture.
- Four PNG files and `20-VERIFICATION-EVIDENCE.md` at the paths in frontmatter.
- No schema, migration, Phase 19 fixture/lifecycle, real-catalog, package, route, or deployment artifact.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Run the independent renderer fixture in desktop and mobile browsers</name>
  <files>scripts/check-renderer.ts, tests/e2e/renderer.spec.ts, playwright.config.ts, .planning/phases/20-renderer/20-VERIFICATION-EVIDENCE.md, .planning/phases/20-renderer/artifacts/brand-desktop.png, .planning/phases/20-renderer/artifacts/brand-mobile.png, .planning/phases/20-renderer/artifacts/model-desktop.png, .planning/phases/20-renderer/artifacts/model-mobile.png</files>
  <read_first>
    - scripts/lib/renderer-fixture.ts
    - scripts/check-publication-gate.ts
    - playwright.config.ts
    - .planning/phases/20-renderer/20-UI-SPEC.md
    - .planning/phases/19-real-audit-evidence/19-VERIFICATION.md
  </read_first>
  <behavior>
    - Brand/model desktop and mobile -> summary, exact hero/caption, full story, facts, sources, and canonical relation visible.
    - Initial response HTML -> summary/story/sources/canonical link present without hydration.
    - Mobile -> document scrollWidth no greater than clientWidth; table/pre/nav scroll only locally; 44px targets/focus/reduced motion pass.
    - Brand -> `全部型号（15）` plus exactly 15 unique `/pen/` links; model -> exactly one canonical brand relation.
    - Model story -> all eight independent PAGE-06 topic sentinels appear in initial HTML, desktop, and mobile with no truncation.
    - Fixture hero -> asset path, entity identity, rights/license, and visible attribution/source are recorded for human visual approval; this is not production-image evidence.
    - Missing fixture flag or unsafe remote/external/protected-catalog environment -> runner/spec fail before build, seed, or server start.
  </behavior>
  <action>Create minimal `scripts/check-renderer.ts`: call the tested Plan 20-01 environment guard before resource creation; reject inherited external `E2E_BASE_URL`, remote credentials, protected-catalog paths/aliases, or missing runner intent; create and seed the owned fixture; then run `pnpm build` inside the runner with sanitized explicit `FPKG_DATABASE_URL`, `PUBLICATION_GATE_FIXTURE=1`, `RENDERER_FIXTURE=1`, empty Turso credentials, and the owned working context. Only after that build succeeds, start the existing `--serve-e2e` command with the same explicit DB and a loopback port, readiness-probe it, invoke Playwright with one worker and the requested spec/project/grep/evidence directory, then stop that ordinary child and call fixture cleanup in `finally`. Use ordinary child try/finally only; do not copy or modify Phase 19 signal/process-group/FD lifecycle and do not turn this into a broad wrapper.

Create `renderer.spec.ts` with independent brand/model desktop/mobile cases and the behavior assertions above. Its setup must require both fixture flags and the owned local DB/base URL. Assert initial response text, same-AST TOC href/IDs, optional-module absence, forbidden sentinel/empty-state absence, exact media URL/alt/attribution/license/source, safe source links, page/local overflow, focus/touch, and screenshot output. Assert each PAGE-06 sentinel separately in response HTML and both projects. Write an initial hero mapping table into `20-VERIFICATION-EVIDENCE.md` containing fixture entity ID/name, exact asset path/hash, rights/license, attribution, source, and screenshot paths for the checkpoint. Extend mobile `testMatch` only to include `renderer.spec.ts`, then run the narrow runner for both projects.
  </action>
  <acceptance_criteria>
    - The runner uses the existing serve-e2e seam and one owned DB; it neither imports nor edits Phase 19 lifecycle.
    - Desktop/mobile both execute real brand and model pages with one worker.
    - Four non-empty screenshots are written at the declared paths.
    - Exact 15-model and exactly-one-brand assertions pass independently of recommendations/graph.
    - The build only runs after owned fixture creation and receives the same sanitized explicit database environment as the server.
    - Every PAGE-06 topic sentinel passes in response HTML, desktop, and mobile; the hero mapping table is complete but not yet human-approved.
  </acceptance_criteria>
  <verify><automated>pnpm exec tsx scripts/check-renderer.ts --spec=renderer --project=desktop,mobile --evidence-dir=.planning/phases/20-renderer/artifacts</automated></verify>
  <done>The independent fixture proves PAGE-01..PAGE-08 in four real-browser surfaces and persists its renderer-only evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Run the Phase 20-owned public-boundary regression and record scope</name>
  <files>.planning/phases/20-renderer/20-VERIFICATION-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/19-real-audit-evidence/19-05-SUMMARY.md
    - tests/e2e/renderer.spec.ts
    - .planning/phases/20-renderer/20-VALIDATION.md
    - .planning/phases/20-renderer/20-VERIFICATION-EVIDENCE.md
    - .planning/phases/20-renderer/artifacts/brand-desktop.png
    - .planning/phases/20-renderer/artifacts/brand-mobile.png
    - .planning/phases/20-renderer/artifacts/model-desktop.png
    - .planning/phases/20-renderer/artifacts/model-mobile.png
  </read_first>
  <action>Use the same narrow runner with `--spec=boundary --project=desktop` to execute Phase 20-owned cases in `renderer.spec.ts`: an unqualified/nonpublic slug is 404, a complete public fixture is readable, a publication-critical story mutation removes it on the next request, and a lawful fixture-only current-hash review/republish restores it. The boundary mode may seed only its owned Phase 20 rows and must not call or edit `publication-gate.spec.ts`, Phase 19 helpers, `check-phase19-regression`, or broad E2E. Run exact TypeScript and changed-file Biome checks. Complete `20-VERIFICATION-EVIDENCE.md` with commands/results, screenshot paths, server-HTML/overflow evidence, PAGE-06 sentinel matrix, 15-model count/link equality, exactly-one-brand assertion, guard-probe results, and a scope audit showing no Phase 19 fixture/lifecycle/migration/readiness/real-catalog/deployment changes. Explicitly state that fixture success proves renderer/lifecycle behavior only, real 305 image review is deferred to Phase 23, and no Phase 19 debt or production publication is claimed.</action>
  <acceptance_criteria>
    - Phase 20-owned 404/mutation/republish boundary cases, renderer projects, typecheck, and exact changed-file Biome checks are green.
    - Evidence records 15 links/count and one canonical model-brand link.
    - Evidence makes no real-305 publication or deployment claim and lists no protected-path diff.
  </acceptance_criteria>
  <verify><automated>pnpm exec tsx scripts/check-renderer.ts --spec=boundary --project=desktop &amp;&amp; pnpm exec tsc --noEmit &amp;&amp; pnpm biome check scripts/lib/renderer-fixture.ts scripts/check-renderer.ts src/lib/entity-page.ts src/lib/markdown.ts src/components/library/EncyclopediaShell.tsx src/components/library/BrandMuseum.tsx src/components/library/ModelArchive.tsx 'src/app/[type]/[slug]/page.tsx' src/app/globals.css tests/renderer/entity-page.test.ts tests/renderer/components.test.tsx tests/e2e/renderer.spec.ts playwright.config.ts</automated></verify>
  <done>Focused regression and durable evidence close PAGE-08 while preserving the Phase 19/real-catalog boundary.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Approve fixture hero identity and rights mappings</name>
  <files>.planning/phases/20-renderer/20-VERIFICATION-EVIDENCE.md, .planning/phases/20-renderer/artifacts/brand-desktop.png, .planning/phases/20-renderer/artifacts/brand-mobile.png, .planning/phases/20-renderer/artifacts/model-desktop.png, .planning/phases/20-renderer/artifacts/model-mobile.png</files>
  <read_first>
    - .planning/phases/20-renderer/20-VERIFICATION-EVIDENCE.md
    - .planning/phases/20-renderer/artifacts/brand-desktop.png
    - .planning/phases/20-renderer/artifacts/brand-mobile.png
    - .planning/phases/20-renderer/artifacts/model-desktop.png
    - .planning/phases/20-renderer/artifacts/model-mobile.png
  </read_first>
  <behavior>
    - Brand hero -> visible asset depicts the named fixture brand and agrees with recorded asset path/hash, rights/license, attribution, and source.
    - Model hero -> visible asset depicts the named fixture model and agrees with the same recorded evidence chain.
    - Desktop/mobile -> same approved asset identity and uncropped identifying details.
  </behavior>
  <action>Review the two fixture hero mappings at asset level using all four screenshots and the evidence table. Approve only when entity identity, visual subject, exact asset path/hash, rights/license, attribution, and source form one consistent chain for both brand and model. This checkpoint approves synthetic fixture assets only; record that visual review for every real inventory image remains Phase 23 work.</action>
  <what-built>Two entity-exact fixture hero mappings rendered on desktop/mobile with loader qualification and recorded rights/attribution evidence.</what-built>
  <how-to-verify>
    1. Compare the brand name/ID and visible subject in both brand screenshots with its evidence-table asset path/hash.
    2. Confirm that asset's license, attribution text, and source are complete and describe the same brand asset.
    3. Repeat for the named model in both model screenshots, checking cap/nib/body details are not cropped or visually mismatched.
    4. Confirm the evidence says fixture-only approval and defers the real 305 image review to Phase 23.
  </how-to-verify>
  <acceptance_criteria>
    - Both entity-to-asset-to-rights-to-attribution chains are visually and textually approved.
    - Desktop/mobile show the same correct asset without identity-obscuring crop.
    - No approval statement extends to production or the real 305 inventory.
  </acceptance_criteria>
  <verify><human-check>Approve both hero mapping rows in `20-VERIFICATION-EVIDENCE.md`, or reject with the exact entity/asset/rights/attribution mismatch.</human-check></verify>
  <done>The evidence artifact records explicit human approval for both fixture hero mappings and the Phase 23 production-image boundary.</done>
  <resume-signal>Type "approved" or describe the exact rejected mapping.</resume-signal>
</task>

</tasks>

<prohibitions>
- Do not run `pnpm test:e2e`, `check-phase19-regression`, the Phase 19 monolithic wrapper, remote/Turso commands, or deployment.
- Do not edit Phase 19 fixture/lifecycle/migration/readiness files, any migration/schema, or real catalog/inventory.
- Do not describe fixture screenshots, 15 models, or green tests as publication of the real 305 rows.
- Do not treat fixture hero approval as production-image approval; all real 305 asset-level visual review remains Phase 23 scope.
</prohibitions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| narrow runner -> DB/server/browser children | Test commands receive filesystem/database authority and must remain disposable/local. |
| fixture evidence -> project completion claim | Synthetic rows can prove rendering but not real-catalog publication. |

## STRIDE Threat Register (ASVS L1; block high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-20-11 | Tampering | renderer runner target selection | high | mitigate | Reject remote/external/real-catalog inputs, force one owned local DB and one worker, and cleanup only owned resources. |
| T-20-12 | Repudiation | verification evidence | medium | mitigate | Record exact commands, screenshots, counts, scope diff, and the synthetic-fixture boundary in a durable artifact. |
| T-20-13 | Spoofing | hero entity/asset identity | high | mitigate | Block completion on human review of each fixture entity-to-asset-to-rights-to-attribution mapping; defer real inventory review to Phase 23. |
</threat_model>

<source_audit>
| SOURCE | ID | Feature/Requirement | Plan | Status |
|---|---|---|---|---|
| GOAL | — | Current-hash published evidence-rich brand/model encyclopedia pages | 20-01..20-04 | COVERED |
| REQ | PAGE-01..PAGE-06 | Summary, exact story, evidence modules, brand/model content/media | 20-01, 20-02 | COVERED |
| REQ | PAGE-07 | Desktop/mobile reading, navigation, overflow, accessibility | 20-03, 20-04 | COVERED |
| REQ | PAGE-08 | All brand models/count and exactly one canonical model brand | 20-01, 20-02, 20-04 | COVERED |
| RESEARCH | — | One public snapshot, pure props, same-AST headings, exact media/relations | 20-01, 20-02 | COVERED |
| RESEARCH | — | Independent renderer fixture and focused feedback only | 20-01, 20-04 | COVERED |
| CONTEXT | — | No CONTEXT.md; locked RESEARCH/UI-SPEC constraints and no-go boundaries | all | COVERED |
</source_audit>

<verification>
- `pnpm exec tsx scripts/check-renderer.ts --spec=renderer --project=desktop,mobile --evidence-dir=.planning/phases/20-renderer/artifacts`
- `pnpm exec tsx scripts/check-renderer.ts --spec=boundary --project=desktop`
- `pnpm exec tsc --noEmit`
- Exact changed-file Biome command from Task 2.
</verification>

<success_criteria>
- PAGE-08 exact 15-model/count and exactly-one-brand assertions pass on desktop and mobile.
- All PAGE-01..PAGE-08 renderer checks, four screenshots, typecheck, formatting, and the Phase 20-owned boundary regression are recorded.
- Both fixture hero mappings receive explicit asset-level human approval; the evidence defers all real 305 image review to Phase 23.
- No Phase 19 protected code/data, real inventory, remote system, or deployment is changed or claimed.
</success_criteria>

<output>Create `.planning/phases/20-renderer/20-04-SUMMARY.md` when done.</output>
