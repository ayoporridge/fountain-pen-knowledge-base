---
phase: 20-renderer
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/lib/renderer-fixture.ts
  - tests/renderer/entity-page.test.ts
  - src/lib/entity-page.ts
autonomous: true
requirements:
  - PAGE-02
  - PAGE-03
  - PAGE-05
must_haves:
  truths:
    - "A non-public brand or pen has no page data, while a current public row is the only authorization anchor."
    - "A public page uses exactly one expected published story and never substitutes legacy body or deprecated/reviewed content."
    - "Visible specs, variants, sources, timeline events, and primary media satisfy the Phase 19 qualified evidence contract."
    - "Canonical relations preserve all public brand models and expose every model-brand candidate so invalid cardinality cannot be hidden."
  artifacts:
    - path: "src/lib/entity-page.ts"
      provides: "Discriminated current-public page view model and single-snapshot loader"
      exports: ["PublishedPageInvariantError", "PublishedPageData", "BrandPageData", "ModelPageData", "getPublishedEntityPage"]
    - path: "tests/renderer/entity-page.test.ts"
      provides: "Independent disposable renderer fixture and loader contract regression"
      contains: "page loader"
    - path: "scripts/lib/renderer-fixture.ts"
      provides: "Renderer-owned disposable migrated database, seed, and cleanup seam"
      exports: ["RendererFixture", "createRendererFixture", "seedRendererFixture", "cleanupRendererFixture"]
  key_links:
    - from: "src/lib/entity-page.ts"
      to: "public_entities"
      via: "root parameterized CTE keyed by type and slug"
      pattern: "FROM public_entities"
    - from: "src/lib/entity-page.ts"
      to: "publication_v2_field_evidence"
      via: "qualified field evidence and source joins"
      pattern: "publication_v2_field_evidence"
    - from: "tests/renderer/entity-page.test.ts"
      to: "src/lib/entity-page.ts"
      via: "direct loader contract assertions against an owned disposable database"
      pattern: "getPublishedEntityPage"
---

<objective>
Establish the one authorization/snapshot truth consumed by every Phase 20 brand and model page, preceded by the independent renderer fixture that proves it.

Purpose: PAGE-02, PAGE-03, and PAGE-05 cannot be made trustworthy in components that query different snapshots or silently fall back to weaker legacy data.
Output: An isolated renderer fixture plus `getPublishedEntityPage(type, slug)` returning a fail-closed `BrandPageData | ModelPageData`.
</objective>

<execution_context>
@/Users/xz/.codex/gsd-core/workflows/execute-plan.md
@/Users/xz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/20-renderer/20-RESEARCH.md
@.planning/phases/20-renderer/20-PATTERNS.md
@.planning/phases/20-renderer/20-UI-SPEC.md
@.planning/phases/20-renderer/20-VALIDATION.md
@.planning/phases/19-real-audit-evidence/19-05-SUMMARY.md
@src/lib/public-visibility.ts
@src/lib/public-media.ts
@src/lib/db.ts
@src/lib/library.ts
@migrations/031_evidence_readiness_v2.sql

<interfaces>
Existing authorization interface: `getPublicEntityBySlug(type: string, slug: string): Promise&lt;PublicEntity | undefined&gt;` in `src/lib/public-visibility.ts`; reuse its `public_entities` semantics, not its separate-query shape. Do not use the legacy in-memory `isPublicEntity` predicate for brand/pen.

Existing DB calls in `src/lib/db.ts`: non-generic `queryAll(sql, args)` and `queryOne(sql, args)`. Treat returned rows as unknown at the loader boundary, validate/cast every selected field explicitly, and keep type/slug in bound parameters.

Existing exact relation analog in `src/lib/library.ts`: `getBrandPublicModels(entityId)` joins public brand -> `entity_links(link_type='made_by')` -> public pen, groups by pen, and orders by name/slug without a business limit.

Migration 031 live view names: `publication_source_item_entities`, `publication_v2_qualified_source_items`, `publication_v2_field_evidence`, `publication_v2_qualified_core_claims`, `publication_v2_qualified_primary_media`, and `public_entities`.
</interfaces>
</context>

<execution_preflight>
Before any task, read `.planning/phases/19-real-audit-evidence/19-VERIFICATION.md`. Do not rerun the Phase 19 wrapper. If status is `gaps_found`, continue only under the accepted user-directed content-track exception, record that exception in `20-01-SUMMARY.md`, preserve every Phase 19/real-catalog boundary, and do not imply the Phase 19 verification debt disappeared. Any other blocking verdict stops execution for user direction.
</execution_preflight>

## Artifacts this phase produces

Plan 20-01 owns these new artifacts:

- `scripts/lib/renderer-fixture.ts` with `RendererFixture`, `assertRendererFixtureEnvironment`, `createRendererFixture`, `seedRendererFixture`, and `cleanupRendererFixture`.
- `tests/renderer/entity-page.test.ts` with named groups `fixture boot`, `page loader`, and `qualified content`.
- `src/lib/entity-page.ts` with exported `PublishedPageInvariantError`, `PublishedPageData`, `BrandPageData`, `ModelPageData`, and `getPublishedEntityPage`.
- New routes, schema files, migrations, package dependencies, environment flags, and deployment configuration: none.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the independent Wave 0 renderer fixture and RED contracts</name>
  <files>scripts/lib/renderer-fixture.ts, tests/renderer/entity-page.test.ts</files>
  <read_first>
    - .planning/phases/20-renderer/20-VALIDATION.md
    - .planning/phases/20-renderer/20-PATTERNS.md
    - migrations/031_evidence_readiness_v2.sql
    - scripts/lib/phase19-fixtures.ts
    - scripts/migrate.ts
    - .planning/phases/19-real-audit-evidence/19-VERIFICATION.md
  </read_first>
  <behavior>
    - Non-public slug -> loader returns null.
    - Missing or duplicate expected story -> explicit renderer invariant; no alternate body is selected.
    - Brand fixture -> two qualified timeline nodes and exactly fifteen distinct public model links in stable order.
    - Model fixture -> exactly one public canonical brand; zero or two candidates is an invariant failure.
    - Unqualified spec, variant, source, gallery/remote-only/missing-attribution media, legacy body, and deprecated story sentinels -> absent.
    - Unsafe environment -> remote credentials, external `E2E_BASE_URL`, protected catalog path, or missing fixture flag is rejected before database access.
    - Complete model story -> independent sentinels for identity/product line, history, design/dimensions/materials/ergonomics, nib plus attributed experience, filling/maintenance, variant boundaries, and purchase checks.
  </behavior>
  <action>
Create renderer-only fixture helper `scripts/lib/renderer-fixture.ts` before production behavior. Export `assertRendererFixtureEnvironment` and require the explicit renderer fixture flag. Reject remote credentials, an inherited external E2E URL, the protected catalog path/aliases, or a missing flag before creating one owned temp root/database; run the canonical migration entry point only against that file; expose its client/database URL for direct contract tests and the later narrow E2E runner; and cleanup only its owned rows/root after closing the client. Reuse Phase 19 files as read-only analogs, but do not import their fixture/lifecycle implementation or reproduce their process/signal/descriptor orchestration.

Create `tests/renderer/entity-page.test.ts` using Node's test runner through `tsx`. Seed renderer-owned IDs/slugs prefixed `renderer-`: one complete public brand, fifteen complete public pens related by canonical `made_by`, one directly exercised public pen, unique expected published stories, 60–160-character Chinese summaries, two qualified timeline events, qualified sources, field evidence, scoped variants, attributed stable primary media, and exact publication/review rows needed for `public_entities`. The complete model story must contain eight independent named sentinels covering identity/product line, history, design/dimensions/materials/ergonomics, nib, attributed writing experience, filling/maintenance, variant boundaries, and purchase checks so later server/desktop/mobile tests can detect truncation by topic. Seed separate conspicuous invalid sentinels for every negative case. Add passing `fixture boot` and `fixture safety` cases before RED `page loader` / `qualified content` cases; dynamically import the not-yet-created loader only inside the RED cases so fixture tests remain runnable.
  </action>
  <acceptance_criteria>
    - The helper owns only disposable DB seed/command/cleanup, rejects unsafe environment before resource creation, and never copies Phase 19 process lifecycle code.
    - The contract test owns distinct row IDs and runs without a browser or production server.
    - The dataset contains a brand with exactly 15 public models and a model with exactly one public canonical brand.
    - Every legacy/deprecated/unqualified/generic-media fallback has a unique sentinel asserted absent.
    - Guard tests adversarially reject remote credentials, external base URL, protected catalog, and missing fixture flag before resource creation.
    - Eight distinct PAGE-06 topic sentinels are seeded in the one published model story.
    - The test file is discoverable without editing `publication-gate.spec.ts` or any Phase 19 file.
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec tsx --test --test-name-pattern "fixture boot|fixture safety" tests/renderer/entity-page.test.ts &amp;&amp; renderer_red_log=$(mktemp) &amp;&amp; if pnpm exec tsx --test --test-name-pattern "page loader|qualified content" tests/renderer/entity-page.test.ts &gt;"$renderer_red_log" 2&gt;&amp;1; then cat "$renderer_red_log"; exit 1; fi; rg -q "ERR_MODULE_NOT_FOUND|Cannot find module.*entity-page" "$renderer_red_log"</automated>
  </verify>
  <done>The Wave 0 fixture and named RED contracts exist before the loader implementation they prove.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the current-public single-snapshot page loader</name>
  <files>src/lib/entity-page.ts, tests/renderer/entity-page.test.ts</files>
  <read_first>
    - tests/renderer/entity-page.test.ts
    - src/lib/public-visibility.ts
    - src/lib/public-media.ts
    - src/lib/db.ts
    - src/lib/library.ts
    - migrations/031_evidence_readiness_v2.sql
    - .planning/phases/20-renderer/20-RESEARCH.md
  </read_first>
  <behavior>
    - Current public payload -> one discriminated view model containing complete story, evidence modules, exact media, and canonical relations.
    - Public row with summary outside 60–160 Unicode characters, wrong story cardinality, missing required brand/model facts, or invalid canonical-brand cardinality -> `PublishedPageInvariantError`.
    - Optional qualified variants/tags/relations -> empty collection allowed; unqualified items never enter the collection.
    - Numeric spec value 0 -> retained; only null/empty/unqualified fields are omitted.
    - SQL-shaped slug/type input -> no authorization bypass, no extra row, and no SQL text interpolation.
  </behavior>
  <action>
Create server-only `src/lib/entity-page.ts`. Export the named discriminated types, `PublishedPageInvariantError`, and `getPublishedEntityPage(type: "brand" | "pen", slug: string)`. Execute one parameterized SQL statement whose root CTE selects `public_entities` by bound type/slug; return null only when that root is absent. Because `queryOne`/`queryAll` are non-generic, decode the unknown row through explicit scalar/JSON assertions before constructing the discriminated model; malformed or missing required data raises the invariant error. In that same statement, select exactly one `published` `brand_story` or `model_story`, complete `body_md`, summary, qualified publication dependencies, field-level values with human-readable source/locator data, approved scoped variants, qualified timeline events, attributed stable primary media owned by the entity, all public brand models, and all public model-brand candidates. Intersect primary media with the existing `publicMediaFilter` safety semantics and require primary usage, current owner, public license, non-empty attribution, and stable local/on-site path; never promote gallery, remote-only, library hero, illustration, or generic media. Deduplicate sources by source item/canonical URL with deterministic ordering. Assert required cardinalities after decoding: brand has at least two timeline events and one model; model has one canonical brand and a non-empty qualified spec set; expected story/source/primary-media cardinality is exact. Add SQL-shaped slug and runtime-cast type adversarial cases and prove they return no unauthorized data. Keep internal evidence IDs/status enums out of returned props. Extend the focused tests until GREEN. Do not add migration/schema work or a second publication eligibility predicate.
  </action>
  <acceptance_criteria>
    - `public_entities` is the only authorization root and slug/type are bound parameters.
    - All page facts are decoded from one SQL statement/snapshot.
    - Expected story, summary, required modules, exact media, and canonical relation invariants fail closed with a server-only diagnostic.
    - Fifteen brand models remain fifteen, and a duplicate model-brand relation is rejected rather than truncated.
    - Legacy body, deprecated/reviewed/wrong-type story, unqualified evidence, and weak media never enter `PublishedPageData`.
    - Unknown DB rows are explicitly decoded and SQL-shaped inputs cannot alter statement structure.
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec tsx --test --test-name-pattern "page loader|qualified content" tests/renderer/entity-page.test.ts</automated>
  </verify>
  <done>The fixture-backed loader contracts pass with one authorization/snapshot truth and no fallback path.</done>
</task>

</tasks>

<prohibitions>
- Do not modify Phase 19 fixtures, lifecycle, readiness, migrations, audit scripts, protected catalog, or real 305-row inventory.
- Do not call the Phase 19 monolithic wrapper, `pnpm test:e2e`, a remote database, or deployment tooling.
- Do not query `entities.body_md`, accept deprecated/reviewed/wrong-type story rows, treat approved as qualified, or concatenate SQL inputs.
- Do not hide duplicate story/media/brand cardinality with `LIMIT 1`, and do not introduce gallery/generic/remote media fallback.
</prohibitions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| request type/slug -> server loader | Untrusted route strings enter a server-only SQL read path. |
| SQLite publication/evidence rows -> public view model | Internal rows become user-visible facts only after current-public and qualification checks. |
| contract test -> disposable SQLite | Fixture writes and loader reads share one owned database while remaining isolated from the protected catalog and Phase 19 lifecycle. |

## STRIDE Threat Register (ASVS L1; block high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-20-01 | Elevation of Privilege | `getPublishedEntityPage` authorization root | high | mitigate | Root the only statement in `public_entities`; no alternate eligibility or legacy fallback path. |
| T-20-02 | Tampering | multi-fact page snapshot | high | mitigate | One parameterized statement obtains authorization and every visible fact from one consistent snapshot. |
| T-20-03 | Information Disclosure | story/evidence/media decoding | medium | mitigate | Exact expected-story and qualified-view joins plus an allowlisted public view model exclude internal IDs/status and weak rows. |
| T-20-04 | Tampering | renderer test database selection | high | mitigate | Fail before inserts unless the explicit disposable local database and fixture flag are present; renderer-prefixed ownership bounds cleanup. |
</threat_model>

<verification>
- `pnpm exec tsx --test --test-name-pattern "fixture boot|fixture safety" tests/renderer/entity-page.test.ts`
- `pnpm exec tsx --test --test-name-pattern "page loader" tests/renderer/entity-page.test.ts`
- `pnpm exec tsx --test --test-name-pattern "qualified content" tests/renderer/entity-page.test.ts`
- `pnpm exec tsc --noEmit`
- Confirm `git diff --name-only` contains no Phase 19, migration, data catalog, or real-inventory file.
</verification>

<success_criteria>
- PAGE-02 has an exact current-public story source with no legacy/deprecated fallback.
- PAGE-03 data includes only evidence-qualified specs/variants/sources; unsupported fields are absent.
- PAGE-05 media is current-entity primary media with stable path, attribution, license, and optional source link; weak alternatives are absent.
- The loader retains the full 15-model set and exposes exactly-one-brand violations for focused PAGE-08 acceptance later.
- The fixture provides eight independent PAGE-06 topic sentinels and fails safely for every hostile environment probe.
</success_criteria>

<output>
Create `.planning/phases/20-renderer/20-01-SUMMARY.md` when done.
</output>
