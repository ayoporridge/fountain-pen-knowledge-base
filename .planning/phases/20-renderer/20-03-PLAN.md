---
phase: 20-renderer
plan: "03"
type: execute
wave: 3
depends_on: [20-02]
files_modified:
  - src/app/globals.css
  - src/components/library/EncyclopediaShell.tsx
  - tests/renderer/components.test.tsx
autonomous: true
requirements: [PAGE-07]
must_haves:
  truths:
    - "At 1024px and wider the story uses an 8/4 main/rail layout with a sticky TOC; below 1024px the same DOM becomes one column with mobile TOC chips."
    - "Story, tables, pre, images, sources, attribution, variants, and relation links cannot create page-level horizontal overflow."
    - "All links and TOC targets remain keyboard/touch operable, and reduced motion never hides or animates required content."
  artifacts:
    - path: "src/app/globals.css"
      provides: "Token-based responsive encyclopedia layout, local overflow, focus, and heading offsets"
    - path: "src/components/library/EncyclopediaShell.tsx"
      provides: "Accessible desktop/mobile nav semantics and renderer class hooks"
    - path: "tests/renderer/components.test.tsx"
      provides: "Structural accessibility and responsive-class contract assertions"
  key_links:
    - from: "src/components/library/EncyclopediaShell.tsx"
      to: "src/app/globals.css"
      via: "encyclopedia layout/nav/story/facts class hooks"
      pattern: "encyclopedia-"
---

<objective>
Make the pure encyclopedia renderer completely readable and operable on desktop and mobile without changing its current-public data contract.

Purpose: Close PAGE-07 before browser evidence by making responsive behavior explicit and structurally testable.
Output: Warm Pen Atlas responsive CSS, accessible TOC markup, and focused component contracts.
</objective>

<execution_context>
@/Users/xz/.codex/gsd-core/workflows/execute-plan.md
@/Users/xz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-renderer/20-UI-SPEC.md
@.planning/phases/20-renderer/20-PATTERNS.md
@.planning/phases/20-renderer/20-02-SUMMARY.md
@src/app/globals.css
@src/components/library/EncyclopediaShell.tsx

<interfaces>
`EncyclopediaShell` already emits the mobile-first semantic order and same-story navigation links. Existing CSS contracts to reuse are `.library-panel`, `.reading-measure`, `.prose-body`, the global 3px focus ring, local table/pre overflow, responsive images, dark tokens, and reduced-motion rules.
</interfaces>
</context>

<execution_preflight>
Before any task, read `.planning/phases/19-real-audit-evidence/19-VERIFICATION.md`. Do not rerun the Phase 19 wrapper. If status is `gaps_found`, continue only under the accepted user-directed content-track exception, record it in `20-03-SUMMARY.md`, and do not imply the debt was resolved. Any other blocking verdict stops execution for user direction.
</execution_preflight>

## Artifacts this phase produces

- New CSS selectors: `.encyclopedia-page`, `.encyclopedia-grid`, `.encyclopedia-main`, `.encyclopedia-rail`, `.encyclopedia-toc`, `.encyclopedia-mobile-nav`, `.encyclopedia-source-link`, and `.encyclopedia-local-scroll`.
- No new file, route, flag, dependency, schema, fixture, or deployment setting.

<tasks>

<task type="auto">
  <name>Task 1: Implement the exact desktop/mobile layout and local overflow contract</name>
  <files>src/app/globals.css, src/components/library/EncyclopediaShell.tsx, tests/renderer/components.test.tsx</files>
  <read_first>
    - .planning/phases/20-renderer/20-UI-SPEC.md
    - src/app/globals.css
    - src/components/library/EncyclopediaShell.tsx
    - tests/renderer/components.test.tsx
    - .planning/phases/19-real-audit-evidence/19-VERIFICATION.md
  </read_first>
  <behavior>
    - Desktop/mobile nav -> same href set and exact heading targets.
    - Optional module absent -> no orphan nav link or empty panel.
    - Required links/media -> semantic labels, alt/caption, safe external attributes, and focusable targets.
    - Content -> no clamp, fixed-height story, client fetch, or reveal gate.
  </behavior>
  <action>First add focused tests that read the stylesheet and render the shell, asserting every declared `.encyclopedia-*` selector/class pairing, the exact 1024px breakpoint, 1152px container, 8/4 grid, 32px gap, 96px sticky/heading offsets, `min-width:0`, local overflow selectors, 44px nav target, focus, dark-token, and reduced-motion rules. The same tests must assert shared desktop/mobile TOC hrefs, exact emitted heading targets, present-module navigation, semantic lists/definitions/figure, safe external links, optional-module removal, and absence of truncation/hydration gates. Then add only those token-based renderer selectors. Use 16px mobile/24px desktop gutters; below 1024px use one column and a horizontally scrolling TOC. Set URLs/captions/model names to `overflow-wrap:anywhere`, media to contain without cropping, and table/pre/nav scrolling to their own wrappers. Update shell markup only where semantic wrappers, `aria-current="location"`, labels, or tested class hooks are required; required content remains visible without JS. Leave viewport geometry to the independent browser fixture in Plan 20-04.</action>
  <acceptance_criteria>
    - Desktop measurements and mobile DOM order exactly follow UI-SPEC.
    - Only table, pre, and mobile nav may scroll horizontally; page/container never does.
    - Link/touch targets are at least 44px and focus remains visible.
    - No literal light-only palette, reveal dependency, layout-moving hover, or hidden citation is added.
    - Every nav href resolves to an emitted ID, and absent optional modules leave no heading, panel, or nav target.
    - Brand/model markup exposes required content and canonical links before hydration.
  </acceptance_criteria>
  <verify><automated>pnpm exec tsx --test --test-name-pattern "responsive selectors|navigation|accessibility" tests/renderer/components.test.tsx</automated></verify>
  <done>The renderer has deterministic responsive, overflow, navigation, focus, dark-mode, reduced-motion, and progressive-markup behavior.</done>
</task>

</tasks>

<prohibitions>
- No page-level overflow, client fetch, accordion, clamp, fixed story height, reveal animation, new palette, or broad visual redesign.
- No data query, schema, Phase 19, real-catalog, deployment, or content change.
</prohibitions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| server HTML -> browser viewport | Unbounded stored text/media is laid out in desktop/mobile clients. |

## STRIDE Threat Register (ASVS L1; block high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-20-09 | Denial of Service | renderer layout | medium | mitigate | Bound containers, localize overflow, allow wrapping, and preserve operable focus/touch targets for long content. |
| T-20-10 | Information Disclosure | hover/client-only evidence | medium | mitigate | Keep facts, citations, attribution, and canonical links visible in server DOM for keyboard/touch clients. |
</threat_model>

<verification>
- `pnpm exec tsx --test tests/renderer/components.test.tsx`
- `pnpm exec tsc --noEmit`
- `pnpm biome check src/app/globals.css src/components/library/EncyclopediaShell.tsx tests/renderer/components.test.tsx`
</verification>

<success_criteria>
- PAGE-07 has an exact desktop/mobile layout contract, local overflow only, operable navigation, and progressive server rendering ready for browser proof.
</success_criteria>

<output>Create `.planning/phases/20-renderer/20-03-SUMMARY.md` when done.</output>
