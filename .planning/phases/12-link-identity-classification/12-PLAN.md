# Phase 12 Plan: 链接、身份与分类纠错

**Goal:** 公开内容只有正确、唯一且可到达的身份，页面不泄漏内部审核与占位字段。

## Tasks

1. 让未解析的 wiki-link 退化为普通文字，把旧站相对 `.htm/.html` 链接还原为原站链接。
2. 对无效两段式命名空间直接返回 404，消灭动态详情路由产生的 200 空壳。
3. 合并 Custom 823、Pelikan M800、Parker 51 三组重复公开身份，旧地址永久跳转到规范地址。
4. 将 Kimberly 从钢笔型号重分类为文章，修正 LAMY 2000 重复品牌名。
5. 公开页只读取 approved 规格、来源、事实和时间线；隐藏 pending、needs_source、identity pending、待映射和未知属性键。
6. 图片授权索引只展示已批准、实际使用的媒体，并把技术字段转换为自然中文。
7. 增加全量数据库与浏览器回归断言，验证无重复公开实体、无错误类型、无内部状态词。

## Verification

- 审计中 31 个旧错误链接不再由公开 HTML 产生。
- 无效命名空间不再返回 200 空壳。
- 三组重复对象只出现在一个 sitemap/browse 身份中，旧地址永久跳转。
- Kimberly 仅作为 article 公开。
- 573 实体渲染面不出现内部审核状态和未映射占位词。
