# Phase 146 Parker Ingenuity / Urban checkpoint

## 范围

- 重放仓库中既有 Parker Ingenuity Fountain Pen 与 Parker Urban Fountain Pen canonical identities。
- 不创建第二套同名实体；只更新来源化内容包、品牌导航证据和本站原创 factual SVG。
- 所有数据库试验只写 caller-owned checkpoint copy。

## 验收

1. 现有 ID／slug 预检拒绝混名与冲突，缺失型号才在 checkpoint 插入。
2. Parker 品牌、Ingenuity、Urban 均通过 `recordEntityContentReview` 三项审核与 `publishEntity`。
3. 两个型号各有一条 Parker `made_by` 与品牌反向导航关系。
4. 首次发布、重放 noop、远程环境拒绝、空 reviewer 拒绝、SVG 事实图标记、真实资料库快照／SHA 不变。

## 非目标

- 不迁移真实 `data/fpkg.db`。
- 不部署、不做全站完成声明、不扩建通用验收或 Playwright 框架。
