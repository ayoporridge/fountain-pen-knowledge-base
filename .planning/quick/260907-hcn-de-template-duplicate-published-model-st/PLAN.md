---
status: complete
created: 2026-09-07
---

# De-template duplicate published model stories

Repair the remaining exact paragraph reuse found in published pen model stories without inventing facts or changing entity identity. The scope is the four Pilot pages generated by Phase 111 and the five Wancher Cosmic 2022 Raden pages from Phase 528.

## Success criteria

- Each scoped page keeps its exact entity/slug, source-backed summary, model specs, source items, claims, variants, scopes, relations, tags and primary media.
- Each scoped page has model-specific body copy; no exact duplicate body paragraph of at least 100 Unicode characters remains within either family.
- Entity and published story bodies, titles, source markers, review/publication hashes and readiness remain consistent; replay is a no-op.
- The apply path rejects remote database environment variables, symlinks/hard-links and non-owned copies, and refuses partial/tampered source markers.
- A formal migration runs only on a disposable owned copy derived from the protected real catalog, then atomically installs the verified copy with an exact rollback backup.
- Local data-contract, public-boundary, library/evidence/publication gates, media/markdown checks, entity-quality/readiness checks, build/type/lint checks and route readback pass. Turso remains explicitly unclaimed if SQL reads are blocked.

## Work items

1. Rewrite only the repeated Pilot shared sections and Wancher Cosmic repeated paragraphs with exact-model facts and the existing source boundary.
2. Add a guarded apply script and focused replay/structural-fingerprint test.
3. Run the test and static checks on a disposable copy, commit owned code/content/plan files.
4. Perform formal local migration, capture evidence and install the verified catalog; update the real-catalog fingerprint fixture if required.
5. Run broad local gates and route/readback checks, then record a concise summary with known external blockers.

## Constraints

- Do not stage the unrelated research-SKB modification or existing untracked research/quick directories.
- Do not use remote credentials or claim Turso/deployment verification while SQL reads are blocked.
- Do not delete or overwrite the protected catalog without an exact backup and a verified owned replacement.
