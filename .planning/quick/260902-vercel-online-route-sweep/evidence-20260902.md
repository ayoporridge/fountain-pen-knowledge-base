# Vercel online public-route sweep — 2026-09-02

## Run disposition

Rejected as a pass. The read-only run enumerated 1,175 canonical routes from the
formal local catalog and fetched them with concurrency 6. Its HTTP counter was
`200: 1,175`, but the run overlapped a Turso read block. Vercel runtime logs
reported:

`BLOCKED: Operation was blocked: SQL read operations are forbidden (reads are blocked, do you need to upgrade your plan?)`

The middleware caught the database error and the dynamic page could return a
200 error/not-found shell. Therefore the counter is not evidence that the
published content was read successfully.

## What remains

- Restore Turso read access (login/plan quota or a permitted database target).
- Rerun the sweep with expected-name/content assertions and explicit detection
  of the blocked-error shell.
- Do not mark the global content goal complete from this run.

## Local gate refresh — 2026-09-02

After stopping the stale local Next server that was holding the real catalog
open, the read-only local checks completed without changing `data/fpkg.db`:

- `check:library`: 4,030 sources, 6,116 source items, 7,303 claims, 17,487
  citations, 958 stories, 1,228 media; contract OK.
- `check:data-contract`, `check:migrations`, `check:evidence-contract --all`,
  `check:public-boundary --all`, `check:publication-gate --all`, and
  `check:articles`: all passed.
- TypeScript and production build: passed; static generation 18/18.
- Entity quality: 906 active entities, zero duplicate groups, thin entities,
  suspicious pen articles, or `made_by` blockers. The 3 retired brands and 16
  retired models remain intentional lineage rows, not public gaps.
- Production YSTUDIO YAKIHAKU asset returned HTTP 200 and contains the current
  Chinese labels and maintenance boundary; the earlier opaque English diagram
  was stale UI, not the deployed asset.

The full-repository Biome command still reports 73 pre-existing formatting
diagnostics in legacy files; no unrelated files were modified to suppress it.
