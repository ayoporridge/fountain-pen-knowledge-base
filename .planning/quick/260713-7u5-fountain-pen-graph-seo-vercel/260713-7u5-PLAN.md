---
phase: quick
plan: 260713-7u5
slug: fountain-pen-graph-seo-vercel
type: execute
status: ready
autonomous: true
created_at: 2026-07-13
files_modified:
  - src/app/page.tsx
  - src/app/search/page.tsx
  - src/app/search/layout.tsx
  - src/app/search/loading.tsx
  - src/app/api/search/route.ts
  - src/lib/search.ts
  - src/components/SearchExplorer.tsx
  - src/app/browse/page.tsx
  - src/app/browse/loading.tsx
  - src/app/api/browse/route.ts
  - src/lib/browse-data.ts
  - src/components/BrowseExplorer.tsx
  - src/components/FacetPanel.tsx
  - src/components/EntityCardImage.tsx
  - src/app/api/image-proxy/route.ts
  - src/lib/media-url.ts
  - scripts/audit-public-media.ts
  - src/lib/public-visibility.ts
  - src/app/api/entities/route.ts
  - src/app/api/entities/[slug]/route.ts
  - src/app/api/entities/[slug]/preview/route.ts
  - src/app/api/entities/[slug]/tags/route.ts
  - src/app/api/links/route.ts
  - src/app/[type]/[slug]/page.tsx
  - src/components/LocalGraph.tsx
  - src/components/Recommendations.tsx
  - src/lib/recommend.ts
  - src/app/graph/page.tsx
  - src/app/layout.tsx
  - src/app/sitemap.ts
  - src/app/robots.ts
  - src/app/chat/layout.tsx
  - src/app/compare/layout.tsx
  - src/components/Header.tsx
  - src/components/MobileNav.tsx
  - src/app/globals.css
  - src/app/library/page.tsx
  - src/app/library/sources/page.tsx
  - src/app/library/media/page.tsx
  - src/app/library/diagrams/page.tsx
  - src/app/library/coverage/page.tsx
  - src/app/library/community/page.tsx
  - src/app/exhibits/page.tsx
  - src/app/exhibits/[slug]/page.tsx
  - src/app/timeline/page.tsx
  - src/app/by/[dimension]/page.tsx
  - tests/e2e/entity-basic.spec.ts
  - tests/e2e/library.spec.ts
  - tests/e2e/site-quality.spec.ts
  - playwright.config.ts
  - package.json
  - .planning/STATE.md
  - .planning/quick/260713-7u5-fountain-pen-graph-seo-vercel/BASELINE-MANIFEST.md
  - .planning/quick/260713-7u5-fountain-pen-graph-seo-vercel/RELEASE-ALLOWLIST.txt
  - .planning/quick/260713-7u5-fountain-pen-graph-seo-vercel/SUMMARY.md
must_haves:
  truths:
    - "开始实施前完整审计并保留当前工作树中的既有未提交优化；不得 reset、checkout、删除或用旧版本覆盖这些改动。"
    - "首页三个示例入口都有确定可用的落点；500 以内映射为入门级与进阶价位 OR，预算金尖入口只返回日本、金尖、价格不高于 500 的实际型号；823/743 入口使用数据库已核实的真实 slug 并直接进入两项对比。"
    - "中文自然语言搜索会先解析品牌、型号、产地、笔尖和预算意图，再按精确、别名/型号、宽松分词逐级降级；已知预算问句的结果实体属性全部满足 facet，而不只是 total 大于零。"
    - "首页、图书馆、浏览和搜索不再以长时间纯文字加载作为第一屏；只读数据有明确缓存策略，浏览与搜索首批结果随页面返回，后续分页仍可用。"
    - "批准媒体通过明确的 media id 代理或站内资源显示；Richard's Pens 仅保留固定域名 legacy url 契约，所有 DNS、重定向、协议、MIME 和响应大小均受限，失败时降级为统一资料卡。"
    - "公开 GET API 的实体列表、实体详情/preview/tags 和一跳二跳 links 全部应用同一 public visibility 规则；隐藏实体作为中心或邻居时返回 404 或被过滤，不经 JSON 泄露。"
    - "词条页先呈现结论、规格和证据状态，来源可追溯；面包屑正确，关系图谱使用可理解的关系名并全宽展示，推荐卡说明具体关联理由且不泄露隐藏词条。"
    - "桌面和移动导航有当前位置，图谱有直接入口；移动筛选与导航抽屉没有重复标题，支持 Escape、焦点进入/返回、背景不可聚焦和滚动锁定。"
    - "键盘用户在所有链接、按钮、输入框和筛选控件上都能看到高对比 focus-visible 状态；页面没有明显水平溢出、重复可访问名称或不可达控件。"
    - "root 只提供 metadataBase 和共享模板，不设置会被子页继承的全局 canonical，也不输出首页 JSON-LD；每个可索引静态页和详情页自 canonical，首页独占 WebSite JSON-LD，search/chat/compare noindex 且不在 sitemap。"
    - "发布前有精确 baseline manifest 与 release allowlist，所有上线文件来自单一、干净、可审计的 release commit；Vercel Git integration 与 CLI 路径二选一，不产生双部署。"
    - "Playwright 通过 E2E_BASE_URL 在本地 production server 或外部 production alias 间切换；外部 URL 不启动 webServer，desktop 运行完整套件、mobile 运行 site-quality 移动合约，两者均通过；skip 门禁比较唯一 test id/源码 skip 声明，不比较 project 展开后的原始数量。"
    - "lint、build、数据契约检查和完整 E2E 全部通过；桌面与移动浏览器 QA 无 console/page error，再部署到现有 Vercel production alias。"
    - "https://fountain-pen-graph.vercel.app 指向本次部署，生产页面、API、SEO 标签、缓存、图片降级和移动交互均经过 live truth 验证。"
  artifacts:
    - path: "src/lib/search.ts"
      provides: "可复用的中文查询规范化、别名/型号识别、分层召回和稳定排序"
      exports: ["searchPublicEntities"]
    - path: "src/lib/browse-data.ts"
      provides: "浏览页服务端首批数据与 API 分页共用的数据查询和策展排序"
      exports: ["getBrowseData"]
    - path: "src/components/EntityCardImage.tsx"
      provides: "批准媒体展示、加载失败降级和统一图片比例"
    - path: "src/app/api/image-proxy/route.ts"
      provides: "批准 media id 主契约与 Richard's Pens 固定白名单 legacy url 契约，带 DNS/redirect/size 防护"
      exports: ["GET"]
    - path: "scripts/audit-public-media.ts"
      provides: "默认 dry-run 的全量批准媒体健康审计、结构化报告和显式授权写入门禁"
    - path: "src/lib/public-visibility.ts"
      provides: "页面、实体 GET API、links 和推荐共用的 public visibility 规则"
    - path: "src/app/api/links/route.ts"
      provides: "中心实体 404 与各跳邻居公开过滤后的关系 API"
      exports: ["GET"]
    - path: "src/app/graph/page.tsx"
      provides: "可从主导航直接进入的局部图谱探索起点"
    - path: "src/app/[type]/[slug]/page.tsx"
      provides: "带面包屑、证据层级、全宽图谱和完整 SEO 元数据的词条详情页"
    - path: "src/components/LocalGraph.tsx"
      provides: "关系类型可理解、图与列表一致且触屏可用的局部关系图"
    - path: "src/app/layout.tsx"
      provides: "站点级 metadataBase、共享标题和社交默认值，不含全局 canonical 或首页 JSON-LD"
    - path: ".planning/quick/260713-7u5-fountain-pen-graph-seo-vercel/BASELINE-MANIFEST.md"
      provides: "实施前 HEAD、精确 dirty path/status、diff 摘要、未跟踪文件校验和与既有语义成果清单"
    - path: ".planning/quick/260713-7u5-fountain-pen-graph-seo-vercel/RELEASE-ALLOWLIST.txt"
      provides: "允许进入 release commit 的逐文件路径白名单，不使用目录通配"
    - path: "playwright.config.ts"
      provides: "E2E_BASE_URL、本地独立 production 端口、外部禁用 webServer 与 desktop/mobile projects"
    - path: "tests/e2e/site-quality.spec.ts"
      provides: "坏入口、搜索、首屏、图片、详情、图谱、移动抽屉、键盘和 SEO 回归覆盖"
    - path: ".planning/quick/260713-7u5-fountain-pen-graph-seo-vercel/SUMMARY.md"
      provides: "基线差异、自动检查、浏览器 QA、部署 URL 与生产验真证据"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/app/browse/page.tsx and src/app/compare/page.tsx"
      via: "HERO_QUESTIONS 使用已验证的筛选参数与 compare items 参数，不再把策展问题交给自由文本搜索碰碰运气"
      pattern: "origin-japan|pilot-custom-823"
    - from: "src/components/SearchExplorer.tsx"
      to: "src/app/api/search/route.ts, src/lib/search.ts and src/lib/browse-data.ts"
      via: "searchPublicEntities 解析 max_price/origin/nib_material 意图，搜索结果与按条件筛选 CTA 共用同一 facet group 契约"
      pattern: "searchPublicEntities|parseSearchIntent|max_price|/api/search"
    - from: "src/app/browse/page.tsx"
      to: "src/components/BrowseExplorer.tsx and src/lib/browse-data.ts"
      via: "服务端解析 searchParams 并注入首批实体、facets、typeCounts；客户端只负责交互和后续分页"
      pattern: "getBrowseData|initialData"
    - from: "src/lib/browse-data.ts"
      to: "src/app/api/image-proxy/route.ts and src/components/EntityCardImage.tsx"
      via: "浏览数据仅暴露批准 media id/站内 URL；代理主路径使用 id，legacy url 只接受 Richard's Pens 白名单，卡片处理 onError"
      pattern: "media_id|image-proxy|richardspens"
    - from: "src/app/api/entities/route.ts and src/app/api/entities/[slug]/route.ts"
      to: "src/lib/public-visibility.ts"
      via: "实体列表、详情、preview、tags 的所有 GET 在查询或返回前应用同一公开过滤，隐藏 slug 统一 404"
      pattern: "PUBLIC_ENTITY_FILTER_SQL|isPublicEntity|publicEntityFilter"
    - from: "src/app/api/links/route.ts"
      to: "src/lib/public-visibility.ts"
      via: "先验证中心实体公开，再对 forward/backlinks/second-hop 的每个邻居应用公开过滤"
      pattern: "PUBLIC_ENTITY_FILTER_SQL|publicEntityFilter"
    - from: "src/app/[type]/[slug]/page.tsx"
      to: "src/components/LocalGraph.tsx and src/components/Recommendations.tsx"
      via: "详情页把关系图放到主内容栅格之外全宽渲染，并把实体身份传给图谱和推荐理由查询"
      pattern: "LocalGraph|Recommendations"
    - from: "src/components/Header.tsx and src/components/MobileNav.tsx"
      to: "src/app/graph/page.tsx"
      via: "桌面和移动主导航都提供关系图谱入口并依据 pathname 设置 aria-current"
      pattern: "href=.?/graph|aria-current"
    - from: "src/app/page.tsx, indexable static pages and src/app/[type]/[slug]/page.tsx"
      to: "src/app/layout.tsx, src/app/sitemap.ts and src/app/robots.ts"
      via: "root 不声明 canonical/首页 JSON-LD；每个可索引页面自 canonical，首页独占 WebSite JSON-LD，search/chat/compare noindex 且从 sitemap 删除"
      pattern: "metadataBase|alternates|application/ld\\+json|robots"
    - from: ".planning/quick/260713-7u5-fountain-pen-graph-seo-vercel/BASELINE-MANIFEST.md"
      to: ".planning/quick/260713-7u5-fountain-pen-graph-seo-vercel/RELEASE-ALLOWLIST.txt"
      via: "release commit 前把 staged path 与逐文件 allowlist 做集合相等校验，并从该 commit 的干净 checkout 发布"
      pattern: "release_commit|allowed_paths"
    - from: "tests/e2e/site-quality.spec.ts"
      to: "playwright.config.ts and https://fountain-pen-graph.vercel.app"
      via: "同一套 desktop/mobile 关键断言通过 E2E_BASE_URL 先对独立端口本地 production server 执行，再对 production alias 执行；外部模式禁用 webServer"
      pattern: "E2E_BASE_URL|desktop|mobile"
---

# Fountain Pen Graph 全站体验、SEO 与 Vercel 生产质量优化

## Objective

在不推倒现有 Warm Pen Atlas 视觉体系、不引入全局毛线球图谱、也不覆盖当前未提交成果的前提下，把上一轮线上审计发现的坏入口、搜索、加载、图片、详情、图谱、移动端、可访问性和 SEO 问题修到可测试、可部署、可在线验真的状态，并发布到现有 Vercel production alias。

## Context and non-negotiable baseline

- 仓库真实根目录是 `/Users/xz/Documents/fountain-pen-graph`，`/Users/xz/CodeBuddy/fountain-pen-graph` 指向同一工作区。
- 当前 `master` 与 `origin/master` 同在 `63c4e4d`，但工作树已有 16 个 modified 文件和多个 untracked 规划/修复脚本。这些改动来自上一轮全站优化、读者文案清理与长文截断修复，属于本次交付基线，不是可丢弃垃圾。
- 实施前必须生成 `BASELINE-MANIFEST.md`：记录 `HEAD/branch/upstream`、`git status --porcelain=v1 -uall` 的每一条精确路径和 XY 状态、tracked diff 的 name-status/stat/check、每个 untracked 文件的 SHA-256，以及既有成果的语义 marker。不得只写“`.planning/quick/` untracked”这种目录级摘要。逐项确认并保留下列既有成果：concept rule 修复、chat 配置门禁与公开实体过滤、真实品牌索引、compare 空状态、library 路由、sitemap 扩充、移动维度导航、来源占位处理、产品图/证据徽章、D1 长文导出防截断及相关审计/清理脚本。
- 同时建立逐文件 `RELEASE-ALLOWLIST.txt`，明确哪些 baseline 文件与本次新增文件允许进入 release commit；禁止 `*`、目录通配和 `git add .`。实施中出现不在 allowlist 的修改必须先解释并更新 manifest，否则停止发布。最终部署只能来自一个已记录 SHA 的 release commit 及其干净 checkout。
- 禁止 `git reset --hard`、`git checkout --`、强推、删除未跟踪文件、从 `HEAD` 整文件覆盖当前版本。只在当前内容上做外科式增量修改。敏感凭据仅使用现有环境变量；不得写入仓库。
- 数据修复脚本先 dry-run/只读审计，确认目标和影响数量后才允许写入；本任务不得为改善界面而编造“适合谁”“不适合谁”、价格、规格或来源事实。
- `image2` 只在现有已授权素材无法满足真实站点社交卡时调用一次；必须检查文字和内容，无可用结果就使用现有 `library-hero.jpg` 或省略图片，不能上线通用占位图。
- 本地数据库核实结果：823 有 `pilot-custom-823` 与重复档案 `百乐-pilot-custom-823`，743 的真实 slug 是 `百乐-pilot-custom-743`。首页 compare 入口使用 `pilot-custom-823` 与 `百乐-pilot-custom-743`，测试必须先查询并断言这两个 slug 仍分别对应 823/743，再组装 URL；不得凭记忆换成不存在的英文 743 slug。
- 本地 baseline 的隐藏品牌中心 ID 为 `banju=S3JHYQtqJExx`、`saier=kBv3hmJfi366`、`shanghai=uppuHJzvuw5k`、`yongxu=yTWZrIZnGyMx`。API/production 回归使用前先从本地 source-of-truth 再核对 slug↔id；若 ID 漂移就以数据库查询结果为准，不把旧 ID 当永恒常量。

<tasks>

<task type="auto">
  <name>Task 1: 保全脏工作树并修通入口、中文搜索、首屏数据和图片链路</name>
  <files>.planning/quick/260713-7u5-fountain-pen-graph-seo-vercel/BASELINE-MANIFEST.md, .planning/quick/260713-7u5-fountain-pen-graph-seo-vercel/RELEASE-ALLOWLIST.txt, src/app/page.tsx, src/app/search/page.tsx, src/app/search/layout.tsx, src/app/search/loading.tsx, src/app/api/search/route.ts, src/lib/search.ts, src/components/SearchExplorer.tsx, src/app/browse/page.tsx, src/app/browse/loading.tsx, src/app/api/browse/route.ts, src/lib/browse-data.ts, src/components/BrowseExplorer.tsx, src/components/FacetPanel.tsx, src/components/EntityCardImage.tsx, src/app/api/image-proxy/route.ts, src/lib/media-url.ts, scripts/audit-public-media.ts, src/lib/public-visibility.ts, src/app/api/entities/route.ts, src/app/api/entities/[slug]/route.ts, src/app/api/entities/[slug]/preview/route.ts, src/app/api/entities/[slug]/tags/route.ts, src/app/api/links/route.ts, src/app/library/page.tsx, package.json</files>
  <action>
1. 在第一次产品编辑前生成精确 baseline manifest 和 release allowlist：逐文件展开 tracked/untracked 状态，给 untracked 文件做 SHA-256，并记录当前 16 个 modified 文件及所有旧 quick task/脚本的准确路径；release allowlist 只列经过确认的前序优化、本计划文件、将新增的产品/测试/报告文件。特别检查上一轮 PLAN/SUMMARY 与当前 diff；若新方案与未提交实现重叠，合并逻辑而不是整文件重写。每次准备提交时做 `staged_paths == RELEASE-ALLOWLIST 中本次应提交路径` 的集合比较；不得使用 `git add .`。

2. 把首页策展问题改成确定落点：
   - “500 以内，日系金尖有哪些选择？”链接到 `/browse?type=pen&origin=origin-japan&nib_material=gold&max_price=500`。`max_price=500` 必须映射为 `price-entry OR price-mid`，而不是仅 `price-mid`；虚拟组 `nib_material=gold` 映射 `nibmat-gold/nibmat-14k/nibmat-18k/nibmat-21k/nibmat-bicolor` 中任一。FacetPanel 要能显示并清除这些语义化 active filters；
   - 活塞问题继续链接 `/concept/piston-filler`；
   - “百乐 823 和 743 怎么选？”在数据库断言 `pilot-custom-823` 对应“百乐 Custom 823”、`百乐-pilot-custom-743` 对应“百乐 Pilot Custom 743”后，链接到 `/compare?items=pilot-custom-823,%E7%99%BE%E4%B9%90-pilot-custom-743`，进入页面即加载两支笔；若 slug 漂移，按查询结果更新，不硬编码猜测；
   - 合并首页重复的“主要任务/馆区入口”，保留一组清晰四入口和精选内容，移动端首屏之后不再重复同义卡片。

3. 抽出 `parseSearchIntent()` 与 `searchPublicEntities()`，使 page 首批结果和 API 后续查询共用同一实现。先把自然语言解析成受控 facet：`日系→origin-japan`，`金尖→gold 组`，`500以内/以下/预算500→max_price=500`，品牌/型号数字保留；再按名称/slug/`entity_aliases.alias` 精确或型号包含、清理停用词后的 FTS OR、有效 token LIKE 逐级召回。带明确 facet 的问句必须把 facet 作为 SQL 参数化约束，而不只是返回 CTA；API 同时返回 `{ intent: { filters, browseHref }, results }`，SearchExplorer 展示“按这些条件筛选”。排序优先名称/别名命中、facet 完整满足、token 覆盖率、来源/标签完整度和稳定名称。空状态删除“→ /browse”内部路径残留。任何输入不得直接拼 SQL。

4. 把 browse 查询抽到 `getBrowseData()`；`src/app/browse/page.tsx` 恢复为 server page，解析 `searchParams` 后把首批实体、facets、typeCounts、total 交给 `BrowseExplorer`，客户端只处理切换筛选、history、加载更多和 popstate。facet builder 支持“跨维度 AND、同一语义组 OR”：`max_price=500` 展开 entry/mid，`nib_material=gold` 展开五个 gold slugs，不能为每个 OR slug 建多个 JOIN 造成实际上 AND。默认排序从纯 `e.name` 改为可解释的编辑完整度；保留普通单 tag facet。首页、图书馆及其他只读馆藏页移除不必要的 `force-dynamic`，使用 5–15 分钟 `revalidate` 或等价 `s-maxage`。loading 文件使用与真实卡片同尺寸的骨架。

5. 图片代理定义两个不含糊的契约：主契约 `/api/image-proxy?id=<media_assets.id>` 只查询 `review_status='approved' AND usage_status IN ('primary','gallery')` 的记录；兼容契约 `/api/image-proxy?url=<encoded-url>` 只接受 HTTPS、无 userinfo/自定义端口、hostname 精确为 `richardspens.com` 或 `www.richardspens.com` 的既有 markdown URL。两条路径都在首次请求及每次 redirect 前解析 DNS A/AAAA 并拒绝 loopback、link-local、RFC1918、ULA、multicast/reserved 地址；使用 manual redirect，最多 3 跳；8 秒超时；只接收 `image/*`；`Content-Length` 预检和实际流读取都硬限制 8 MiB；只缓存 200 成功响应。`EntityCardImage` 对 4xx/5xx/超时降级且不重试循环。

6. 新增 `scripts/audit-public-media.ts`，默认且无参数时必须是全量 read-only dry-run：扫描所有 approved primary/gallery 媒体，有限并发检查最终 URL/状态/MIME/大小，输出带 `media_id/entity_id/url/final_url/status/error/checked_at` 的 JSON 与 Markdown 报告。任何 `--apply` 都要显式 `ALLOW_MEDIA_STATUS_WRITE=1`、保存 apply 前报告、逐条记录 old/new 状态和目标数据库；检测到远程 Turso 时还必须有单独 `--remote` 与授权环境变量，否则拒绝写。没有生产数据写权限不是阻塞：保留数据记录并依靠 UI fallback，SUMMARY 明确列出未应用项；不得静默修改 production。

7. 把 public visibility 扩展到所有公开 GET：`/api/entities` 列表 SQL 过滤；`/api/entities/[slug]`、`preview`、`tags` 对隐藏 slug 返回 404；`/api/links` 先查中心实体且隐藏/不存在都 404，再过滤 forward、backlinks、secondHopForward、secondHopBacklinks 中的隐藏邻居。把硬编码 `e` alias 的过滤常量改成可安全用于不同 SQL alias 的 helper，页面、API、search、recommendation 共用，避免各自复制隐藏名单。写 API 仍按原 auth 契约，不因公开 GET 修复而放宽。
  </action>
  <verify>
1. `git diff --check`；用保存的基线逐个确认既有 dirty 文件/脚本没有丢失。
2. 先查询数据库核实 compare 的两个 slug 对应名称；再检查“百乐 823 743”结果集合同时含 823 与 743。对“500以内，日系金尖有哪些选择”不能只断言 `total > 0`：遍历返回结果并通过实体详情/标签断言每项都是 `type=pen`、含 `origin-japan`、含 gold slug 组之一、含 `price-entry OR price-mid`，且 API intent 为 `max_price=500`；恶意 FTS 字符不产生 500。
3. 首页三个问题点击后分别到筛选页、概念页、两项对比页；浏览页首次 HTML/水合完成时已有 30 张以内真实卡片或骨架，无长时间空白；筛选、后退/前进、加载更多正确。
4. 执行媒体 audit dry-run 并核对扫描数量等于 SQL 全量目标数量。批准 id 成功、未知/未批准 id 4xx、Richard's Pens legacy URL、非白名单 URL、直接私网 URL 和 403/502 fallback 做集成测试；DNS 重绑定、redirect 到私网、超过 3 跳、伪造 Content-Length、实际流超 8 MiB 与非图片 MIME 用可注入 resolver/fetch 的本地单元测试验证，禁止向 production 写入恶意 fixture 或测试后门。未提供 apply 授权时数据库零变更。

5. 从当前数据库动态选取至少一条因“品牌资料索引/品牌泛称” marker 被隐藏的 article，断言 detail/preview/tags 404，且 entities、browse、search、links、recommendation 与 sitemap 都不泄露；与四个隐藏品牌共同证明使用统一 public visibility helper，不是硬编码 slug 名单。
5. `GET /api/entities?type=brand` 不含隐藏 slug；`GET /api/entities/banju`、`/preview`、`/tags` 均 404；以隐藏实体 id 请求 `/api/links` 为 404，以公开实体请求时四个 link 数组都不含隐藏 slug/id。为 `saier/shanghai/yongxu` 重复同类断言。
6. 重复请求首页、library、browse/search GET，确认缓存头存在且第二次响应不劣于首次；记录 warm TTFB，不把单次网络抖动当成功证据。
  </verify>
  <done>baseline/allowlist 精确落盘且现有成果保留；500 以内是 entry+mid OR，预算搜索结果的产地/金尖/价位属性逐项成立，真实 823/743 slug 可加载；浏览/搜索首屏有服务端数据；图片代理与 audit 满足安全/审计契约；所有实体与 links GET API 不泄露隐藏内容。</done>
</task>

<task type="auto">
  <name>Task 2: 强化详情与图谱主线，完成移动端、可访问性和 SEO</name>
  <files>src/app/[type]/[slug]/page.tsx, src/components/LocalGraph.tsx, src/components/Recommendations.tsx, src/lib/recommend.ts, src/app/graph/page.tsx, src/components/Header.tsx, src/components/MobileNav.tsx, src/components/FacetPanel.tsx, src/app/globals.css, src/app/layout.tsx, src/app/page.tsx, src/app/sitemap.ts, src/app/robots.ts, src/app/chat/layout.tsx, src/app/compare/layout.tsx, src/app/search/layout.tsx, src/app/browse/layout.tsx, src/app/library/page.tsx, src/app/library/sources/page.tsx, src/app/library/media/page.tsx, src/app/library/diagrams/page.tsx, src/app/library/coverage/page.tsx, src/app/library/community/page.tsx, src/app/exhibits/page.tsx, src/app/exhibits/[slug]/page.tsx, src/app/timeline/page.tsx, src/app/by/[dimension]/page.tsx, public/og.png</files>
  <action>
1. 在当前未提交的产品图、证据徽章、长文阅读提示基础上增量整理详情页：把“首页”单返回链接改成 `首页 → 类型/品牌 → 当前词条` 面包屑；摘要作为一句话结论，pen 的批准规格、来源数量和证据状态进入首屏，故事/长文随后出现。已有数据不足时省略“适合/不适合”，不得生成结论。来源按官方/经销商/媒体/社区/待补证分级，Bing 搜索页继续只作为不可点击线索；把英文 “Claims” 全部改成面向用户的“事实”或“事实与证据”。

2. 把详情页关系图从 `lg:grid-cols-3` 主栏中移到正文与侧栏之后的全宽 section，保持关系列表作为触屏/读屏兜底。建立明确的 link type 中文映射（如同品牌、品牌型号、同系列、使用/实现、相关、前后代；未知值显示“相关”而不是 raw key）；画布的线型/透明度与关系列表保持一致，列表显示“为什么关联”。支持一跳/二跳切换或最小关系类型筛选，但继续限制节点数，不建设项目明确排除的全局毛线球。

3. 新增 `/graph` 作为局部图谱的直接入口：选择连接度高且公开的实体作为默认图谱，列出可切换/继续探索的图谱 hub，所有点击进入真实词条或更新起点。桌面 Header 与 MobileNav 都增加“关系图谱”，并用 `usePathname` 对所有主导航设置可见 active 状态与 `aria-current='page'`。`getRecommendations()` 必须加入 `PUBLIC_ENTITY_FILTER_SQL`，优先同品牌/同系列/同价位/同笔尖或直接关系，并显示具体理由，禁止只写“共享 6 个标签”。

4. 修复两个抽屉：FacetPanel 增加可隐藏标题的方式，移动筛选只保留一个标题和一个可访问关闭按钮；使用 `role='dialog' aria-modal='true'`、进入时聚焦第一个可操作项、Tab 循环、Escape 关闭、关闭后恢复触发按钮焦点、背景 `inert/aria-hidden` 与 body scroll lock。筛选抽屉底部固定“查看 N 个结果”和“清空”动作。MobileNav 使用同样的焦点与背景策略。检查 390×844 下首页、browse、详情和 graph 不横向溢出，触控目标至少约 44px。

5. 在 globals 统一 `:focus-visible` 高对比轮廓，并删除搜索框等控件只取消 outline 却没有替代的状态；尊重 `prefers-reduced-motion`，让 reveal/pulse/slide 动画可关闭。键盘逐项走 Header、search、facet、抽屉、graph 关系列表和 compare。

6. SEO 使用 `https://fountain-pen-graph.vercel.app` 作为 `metadataBase`，但 root layout **不得**设置 `alternates.canonical`，否则所有子页会继承首页 canonical；root 也不得渲染 WebSite/SearchAction JSON-LD。root 只放 title template、description、siteName、OG/X 默认值。首页 `src/app/page.tsx` 自己声明 canonical `/` 并仅在首页渲染 WebSite + SearchAction。每个可索引静态页都声明自己的 canonical：`/browse`（所有参数组合 canonical 回 `/browse`）、`/library` 及五个 library 子页、`/exhibits`、每个 published/reviewed `/exhibits/[slug]`、`/timeline`、`/graph`、每个 `/by/[dimension]`；详情页生成自身 canonical、BreadcrumbList 和基于真实数据的类型 JSON-LD。search/chat/compare 明确 `robots: { index: false, follow: true }` 且 sitemap 中完全删除这三项；sitemap 添加 `/graph` 和 published/reviewed exhibit 详情 URL，只列可索引静态页和公开详情。robots 保持 API/admin/new 禁止抓取。绝不补不存在的价格、评分或库存。

7. 社交图优先复用现有 Warm Pen Atlas hero 做 1200×630 合理裁切；如确需 `image2`，只生成一张与最终站点同文案、同暖棕色资料馆视觉的完整卡片，检查中文标题无错字后保存 `public/og.png`。生成结果文字错误或虚构产品时不要上线，退回无字 hero；详情页有批准实体图时用实体图，没有时用站点卡。
  </action>
  <verify>
1. 桌面 1440×900：Custom 74/823、Pilot 品牌和一篇长文章的面包屑、首屏规格/证据、来源分级正确；graph 横跨内容宽度，关系名无 raw key，推荐理由具体且链接均 200。
2. 移动 390×844：homepage/browse/detail/graph 无横向溢出；筛选和导航抽屉只出现一个标题/关闭按钮，Tab 不逃出 dialog，Escape 与点击遮罩关闭后焦点回到触发器，背景不可聚焦。
3. 仅用键盘遍历主要页面，所有交互元素都有 visible focus；启用 reduced motion 后动画停止或显著减少。
4. `curl` 或 DOM 逐页检查 canonical：root HTML 只有首页自身 canonical，每个 library 子页、exhibits、timeline、graph、by dimension 和详情页 canonical 都等于自身无参数公开 URL，不能全部指向 `/`；WebSite/SearchAction JSON-LD 只在首页出现，layout 及其他页面不重复。search/chat/compare 为 noindex 且三者都不在 sitemap；sitemap 含 `/graph` 和公开详情，不含 API/new/隐藏实体。
5. 查询推荐和 graph API/页面，确认 `banju`、`saier`、`shanghai`、`yongxu` 等隐藏 slug 不出现在 HTML、JSON 或链接中。
  </verify>
  <done>详情页信息层级与来源可信度清楚，局部图谱有直接入口且关系理由可读，推荐不再只按标签数；桌面/移动导航、筛选和键盘路径符合基本无障碍要求；公开页面 SEO 完整且工具页不污染索引。</done>
</task>

<task type="auto">
  <name>Task 3: 收敛自动化测试，完成桌面/移动浏览器 QA、Vercel 发布与 production live truth</name>
  <files>tests/e2e/entity-basic.spec.ts, tests/e2e/library.spec.ts, tests/e2e/site-quality.spec.ts, playwright.config.ts, package.json, .planning/STATE.md, .planning/quick/260713-7u5-fountain-pen-graph-seo-vercel/BASELINE-MANIFEST.md, .planning/quick/260713-7u5-fountain-pen-graph-seo-vercel/RELEASE-ALLOWLIST.txt, .planning/quick/260713-7u5-fountain-pen-graph-seo-vercel/SUMMARY.md</files>
  <action>
1. baseline 先强制启动新的单 project server（禁止复用残留 `:3000` dev server），把 passed/failed 总数与每个唯一 skipped test id（标题/文件/行号）写入 BASELINE-MANIFEST；前序报告只能作为参考。然后让 `playwright.config.ts` 支持 `E2E_BASE_URL`：未设置时 baseURL 为独立 production 端口（例如 `http://127.0.0.1:3107`），webServer 仅执行已构建产物的 `next start -p 3107`、`reuseExistingServer: false`；设置外部 URL 时 `webServer: undefined`。定义 `desktop`（1440×900）运行完整套件，`mobile`（390×844、hasTouch/mobile UA）只运行 `site-quality.spec.ts` 的移动合约；skip 门禁比较唯一 test id/源码 skip 声明集合，不比较 project 展开后数量。

2. 新增 `site-quality.spec.ts` 覆盖：首页三个入口和数据库核实的 823/743 slug；搜索 intent/facet 及预算结果逐项属性；FTS 特殊字符；browse 首批数据/OR facet/popstate/分页；media id 与 Richard's Pens legacy 代理安全边界/fallback；实体列表/详情/preview/tags/links 隐藏 404/过滤；详情面包屑/证据/全宽图谱/关系理由；推荐理由；移动 dialog 焦点；focus-visible；逐页 canonical、首页独占 JSON-LD、noindex/sitemap/robots。更新旧测试中已过期的 seed、公开写 API和文案断言，但不得大量 skip、删行为断言或只检查 200。新增 skip 数必须为 0；最终 skipped 总数不得高于 baseline，且每个新增/移动 skip 都要在 SUMMARY 解释，否则门禁失败。

3. 门禁顺序为：`pnpm lint`、`pnpm verify:markdown`、`pnpm check:data-contract`、`pnpm check:library`、`pnpm audit:entity-quality`、媒体 audit dry-run、`pnpm build`，然后才允许 production webServer 启动并运行 desktop 完整套件与 mobile `site-quality.spec.ts` 合约。保存机器可读 report；最终 failed=0，唯一 skip id 集合不比 baseline 增加。lint 现有 CSS warning 可记录，但新增 warning/error 必须清零。任何写数据检查先备份并 dry-run。

4. 使用上述独立 3107 端口的 `next start` 做人工浏览器验收，不复用可能残留的 `next dev :3000`：桌面和手机分别走首页→预算筛选→型号详情→关系图谱→推荐，以及首页→823/743 对比；检查图片、空状态、滚动、抽屉、键盘、console/page error。把 viewport、路径、截图和 console 结果写入 SUMMARY；修复后重跑两 project。

5. 发布前再次执行完整门禁。按 `RELEASE-ALLOWLIST.txt` 逐文件 stage，比较 `git diff --cached --name-only` 与本次 allowlist 期望集合完全相等；禁止 `git add .`。创建单一 release commit，记录 SHA，并确认 commit 包含前序 baseline 成果及本轮增量。部署源必须是该 SHA 的干净 checkout：若主工作区 `git status --porcelain=v1 -uall` 仍有任何非忽略文件，禁止从该目录 CLI deploy，改在临时 clean worktree checkout release SHA 后验证 `git status --porcelain` 为空。排除 `.env*`、数据库、缓存、报告临时文件和凭据，禁止 force push。

6. 在触发发布前检查 `.vercel/project.json`、Vercel project settings/CLI inspect 和最近部署的 Git metadata，确认 `master` push 是否由 Vercel Git integration 自动生产部署，并在 SUMMARY 记录证据。然后严格二选一：
   - **Git integration 已启用**：只 push release commit 一次，等待并 inspect 与该 commit SHA 匹配的 production deployment；禁止再运行 `vercel --prod`。
   - **Git integration 未启用或明确不自动 production**：正常 push release commit用于审计，但只从 release SHA 的 clean worktree 执行一次 CLI 部署。clean worktree 中必须先从主工作区已核验的 `.vercel/project.json` 取得 `projectId/orgId/projectName`，再显式执行 `vercel link --project <projectName> --scope <org>` 或安全复制并复核 project file；部署前后 `vercel inspect` 的 project ID 必须精确等于现有 fountain-pen-graph 项目，否则停止。然后才执行一次 `pnpm dlx vercel@latest --prod --yes`，禁止等待/触发第二个 Git deployment。
   两种路径都记录 immutable URL、release SHA、deployment id、alias；发现同一 SHA 两个候选 production deployment 时停止并判定真正 alias 指向，不继续盲目部署。

7. 对 production alias 做 live truth：
   - HTTP/内容：`/`、`/library`、`/browse?type=pen`、两条 `/search`、两项 `/compare`、`/graph`、`/pen/pilot-custom-823`、`/brand/pilot`、长文章、`/sitemap.xml`、`/robots.txt` 均返回预期状态与关键文本；
   - API：预算 search 的 intent 正确且每个结果标签满足日本+gold+entry/mid；823/743 搜索包含核实型号；browse OR facet 正确；media approved id、典型 legacy URL、未知/未批准 id、非白名单 URL、直接私网 URL 和正常 fallback 正确；redirect/DNS/实际流超限等极端边界只在本地可注入测试验证，production 不写入恶意 fixture；
   - 公开 API：entities 列表不含隐藏项，四个隐藏 slug 的 detail/preview/tags 均 404，hidden center links 404，public center 的四组邻居不含隐藏项；
   - SEO：每个可索引静态页/详情自 canonical，首页独占 JSON-LD，search/chat/compare noindex 且不在 sitemap；
   - 可见性：隐藏 slug 页面 404，browse/search/entities/links/sitemap 都不暴露；
   - 性能：对首页/library/browse/search 连续请求至少 3 次，记录 cache header 和 warm TTFB；期望首页/library warm median < 1s、browse/search API warm median < 1.5s，若外部网络使阈值不稳定，至少必须证明缓存命中/可复用首批数据并如实记录测量；
   - 浏览器：执行 `E2E_BASE_URL=https://fountain-pen-graph.vercel.app pnpm exec playwright test tests/e2e/site-quality.spec.ts --project=desktop --project=mobile`，确认配置没有启动 webServer，并记录 passed/failed/skipped；再做人工 smoke，确认无 console/page errors、破图、移动溢出或抽屉焦点逃逸。

8. 只有全部门禁通过后，写 SUMMARY：baseline manifest 与 allowlist 比对、release SHA/clean checkout、选择 Git 或 CLI 的证据、所有命令、desktop/mobile 本地与 production 的 passed/failed/skipped、skip 差异、截图、deployment/alias、live URL 和性能数据；再把 quick task 追加到 STATE。若 skip 增加、release checkout 不干净、双部署来源不明、production alias 或关键 smoke 未通过，不得标记 complete。
  </action>
  <verify>
1. 所有自动化命令退出码 0，`pnpm build` 成功；本地 desktop/mobile 与完整 E2E failed=0，skipped 不高于 baseline且没有新增 skip；`git diff --check` 无问题且仓库不含凭据/生成缓存。
2. 用 `E2E_BASE_URL` 跑 production 时 Playwright 配置解析结果显示 `webServer` 未启用；SUMMARY 列出两 project 的 passed/failed/skipped、QA viewport、路径、截图、console/page error。
3. baseline manifest、release allowlist、cached path 集合、release commit SHA 与 clean checkout 可互相核对；Vercel 证据显示只选择 Git integration 或 CLI 之一，production alias 指向该 SHA 的单一 immutable deployment。
4. `.planning/STATE.md` 仅在上述条件满足后新增 `260713-7u5` complete 记录，SUMMARY frontmatter 为 `status: complete`。
  </verify>
  <done>代码与数据契约通过；Playwright 可在本地独立 production server和外部 alias 运行，desktop/mobile 均 0 failed 且 skip 不增加；发布来自 allowlist 对应的干净 release commit并且 Git/CLI 只走一条；现有 Vercel alias 已切到该部署且 live truth 全通过。</done>
</task>

</tasks>

## Final acceptance gate

本任务不是“本地 build 通过”即完成。只有同时满足以下条件才允许结束：精确 baseline manifest 与 release allowlist 可核对且既有成果未被覆盖；预算搜索结果属性逐项正确；公开 entity/links GET 无隐藏泄露；媒体代理与 dry-run audit 契约通过；逐页 canonical/首页独占 JSON-LD/sitemap 排除工具页正确；desktop/mobile 本地与 production E2E 均 0 failed 且 skip 不增加；发布来自干净 release commit、Git/CLI 只走一条；production alias 指向该 SHA 的部署，并完成全部 live truth。
