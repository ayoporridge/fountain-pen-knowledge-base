---
quick_id: 260811-fm7
status: complete
scope: public-content-residue-cleanup
---

# 规范化公开实体主表中的 raw model_specs 副本

## 边界

- 只处理 `publication.status='published'` 且类型为 brand／pen、`entities.body_md` 含精确 `## model_specs` JSON fence 的 195 个实体；4 个 retired lineage 不改。
- 复用 Phase 583 已验证的严格 parser，只删除可解析 JSON 的完整 section；不重写其它正文，不修改结构化 `model_specs`、sources、claims、media、identity、links 或 published story。按候选库清理后正文的实际分布设置次级下限：pen 1,400 字符、brand 2,000 字符。
- 所有写入只在 caller-owned checkpoint copy；在首个写事务前拒绝 Turso、真实 `data/fpkg.db`、symlink、hard-link、越界 root 与错误 client。
- 正文更新后必须重新记录 current-hash fact、language、media review，再经 `publishEntity` 恢复发布；绝不直接更新 publication 状态。

## Task 1：实体正文精确清理与发布门

- **files:** `scripts/apply-phase585-public-entity-body-residue-cleanup.ts`
- **action:** 预加载并验证全部目标，确保 pen 有且仅有一条结构化规格、brand 没有误建规格；在单一事务中 optimistic-update entity body，随后逐实体审核发布并验证公开状态、正文残留、结构化规格与 published story 未变。
- **verify:** 首跑恰好清理 180 个 pen、15 个 brand、195 个 JSON blocks；当前公开实体数量不变，published blocker 为 0；4 个 retired residue 保持不变。
- **done:** 当前公开 brand／pen 的 `entities.body_md` 与 published story 均无 raw `## model_specs`，而结构化规格仍完整。

## Task 2：定向回归与 fail-closed 边界

- **files:** `tests/content/phase585-public-entity-body-residue-cleanup.test.ts`
- **action:** 在 Phase 584 候选的新 checkpoint copy 验证 parser、remote refusal、实体／story before-state、首次清理、current-hash reviews、public/readiness、内容保留、结构化规格保留、retired 不变和 replay zero-change。
- **verify:** 定向测试通过；真实数据库 main/WAL/SHM 快照与 SHA-256 不变；TypeScript、Biome 与 diff check 通过。
- **done:** Phase 585 可安全加入离线迁移栈，不依赖 Turso，也不把本 quick 视为全量 goal 完成。

## Task 3：离线候选与证据

- **files:** 本 quick `260811-fm7-SUMMARY.md`；checkpoint／evidence 仅本地保留且不提交。
- **action:** 从 Phase 584 checkpoint 建立独占 owned copy，运行定向测试生成 Phase 585 候选，执行 replay、SQLite integrity、全库 readiness、媒体与本地 production 页面抽查。
- **verify:** 新候选可重放且所有当前公开条目 ready；真实库哈希未变；只暂存本 quick 的脚本、测试、PLAN 与 SUMMARY。
- **done:** 形成清除双路径 raw JSON 后的最新离线候选，供下一批内容继续叠加。
