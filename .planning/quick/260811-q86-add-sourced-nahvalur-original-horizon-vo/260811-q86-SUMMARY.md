---
status: complete
quick_id: 260811-q86
scope: direct-content-repair
source_candidate_sha256: 8969d5b008cb33b16bb12ab735889a198c62fb37a9aa470d0d53142a8236ff1b
output_checkpoint_sha256: 81bf7975b41e9820b86546ac9406bf82b04eaadcf6761e6041241c7ece4dc5b3
implementation_commit: 91aa0c636d7cc720df96dbe251bd4a876933dc26
completed_at: 2026-08-11T19:58:23+08:00
duration: 68m
---

# Phase 596：新增 Nahvalur Original、Horizon、Voyage 与 Eclipse

本 quick 在 Phase 595 owned candidate 的新 caller-owned checkpoint 上，一次发布 Nahvalur 四个 canonical 型号，并把品牌公开导航从 Original Plus、Schuylkill、Nautilus 三个型号扩为七个。Turso 未被查询，真实 `data/fpkg.db` 与 Phase 595 source family 均未被 SQLite-open 或写入；本次只完成这一内容批次，full-corpus goal 继续 active。

## 已交付

- `Nahvalur Original Fountain Pen` 固定为 2019 Narwhal 首发阶段的 No.6 钢尖活塞产品线。现行 Black 的六个 available SKU 被放在独立 edition 下；vacuum-filling Original Plus 保持 sibling，不并入。
- `Nahvalur Horizon Fountain Pen` 固定为手工珠光树脂、传统墨窗与活塞产品线。Gaia 保留为 archive edition，Soleil 五个 available SKU 为 current；Pride 2026、Pearl、Nebula 与 Soleil EF 共十九个 unavailable 代码只作为拒绝证据。
- `Nahvalur Voyage Fountain Pen` 固定为旅行主题树脂活塞产品线。Hawaii 五个 available SKU 为 current，unavailable EF 只作拒绝证据；Pride 2024 的 628 支和 Nashville 合作材料不外推到全系列，也不与三舷窗 Nautilus 混名。
- `Nahvalur Eclipse Fountain Pen` 固定为十二切面铝杆、可拆夹、整杆按压伸缩与 converter 产品线。Black Silver 与 Cobalt Black 共五个 current SKU；官网 F／M 文案与后续 Broad selector、首发重装报告和新版 feed／back-cap 演进均保留作用域与冲突。
- 四个型号合计建立 6 个 edition group、21 个 current market-SKU child。每个 market SKU 的父级均为 edition group，没有把颜色、合作故事或售罄代码冒充第二基础型号。
- 品牌和四个型号均有自然中文正文，覆盖身份、规格、时态、版本、使用维护、选购核验、图片边界与具名来源；正文均超过 2,500 Unicode 字符。
- 新增四张互不相同的 1600×900 site-original SVG，均带 `<title>`、`<desc>`、`role="img"` 与“本站原创示意图／非产品照片”边界。原生渲染目视未见裁切、重叠或错位。

## 审核与发布链路

- Nahvalur brand refresh 与四个型号 pack 均走现有 `recordEntityContentReview`／`publishEntity` 路径；fact、language、media、publication 四类 current-hash review 全部 approved，没有直接修改 publication 状态。
- wrapper 在写前拒绝任何非空 `TURSO_DATABASE_URL`、`TURSO_AUTH_TOKEN`、`FPKG_DATABASE_URL`，并校验 reviewer、owned realpath、regular file、symlink／hard-link／inode、protected snapshot、client binding 与 migration 032。
- 四目标的 entity、slug、canonical name、alias collision 均 fail closed。三个既有 Nahvalur public model 的完整 digest 前后不变；首次五包发布成功，完整 replay 五包全部 `noop`。

## 离线验收

- 输入 Phase 595 candidate SHA-256：`8969d5b008cb33b16bb12ab735889a198c62fb37a9aa470d0d53142a8236ff1b`；结束时 main／WAL／SHM family 与开始快照逐字段一致。
- 输出 Phase 596 checkpoint SHA-256：`81bf7975b41e9820b86546ac9406bf82b04eaadcf6761e6041241c7ece4dc5b3`。真实库 SHA-256 仍为 `acbb5710050b5c9d6e435666dda909b66055c290a4640f04faa38860ea24d09a`，且没有 WAL／SHM。
- 最终定向回归 1／1 通过（214268 ms）：四类 collision、三个 remote selector、source／real family、baseline +4、Nahvalur 3→7、6／21 topology、current／unavailable SKU、冲突证据、四类 review、首次发布与 replay 均有断言。
- TypeScript、目标测试精确 Biome、四张 SVG 的 `xmllint` 与十二路径 staged `git diff --check` 全部通过。SQLite `integrity_check=ok`，`foreign_key_check` 无行，current-public approved primary local-path duplicate group 为 0。
- Readiness：834 个 brand／pen inventory、810 个 published、810 个 content-ready，published/public blocker 均为 0；24 个 backlog 均为既有 retired lineage，因此 `content_complete=false` 仍是正确结果。
- Entity quality：810 个 active entity 中 duplicate-name group、thin entity、suspicious pen article 与 made_by blocker 全为 0。Library contract 为 `OK`：3825 sources、5775 source items、7050 claims、16314 citations。Public media 为 826／826 healthy。
- Production build 通过并生成 18／18 static pages，standalone libsql runtime 已准备。`/brand/nahvalur`、四个 `/pen/{slug}` 与四张 SVG 共九个 URL 全部 HTTP 200；品牌新增四链接、型号代码与冲突标记均从 production server 回读。
- SVG SHA-256：Original `d255d652809c8bf581f28b482004576cc83134ebf125791806dc6c4d64cbe828`；Horizon `04422ff4a874e56bd1fff989491b60cc8915cc0bb582a5cfb31d60038cead26d`；Voyage `b28bf3cbaaa0ca8f43dfb9350d2d2670037eb4a0c35b0bca7b8e8ac5e99e8cd9`；Eclipse `4f7e9d5ca7980aa9872c961ab13688d26e5440da39f7a4193652a804a4501583`。HTTP 回读哈希逐一相同。

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Content] 三篇正文未达到正文层 2,500 字符门槛**

- **Found during:** 第一次完整定向回归
- **Issue:** 文件总长度超过门槛，但去掉 frontmatter 和 `model_specs` 后，Original、Horizon、Voyage 的实际 `body_md` 分别只有 2206、2374、2337 字符。
- **Fix:** 补写商品代码时态、edition 证据边界与二手核验逻辑；最终三篇 `body_md` 均超过 2,700 字符，第二轮及最终格式化后回归均通过。

**2. [Rule 3 - Isolation] 并行审计造成源 checkpoint sidecar 快照互扰**

- **Found during:** 全量离线审计
- **Issue:** readiness、entity quality、library contract 与 SQLite integrity 同时运行时，直接 integrity read 改变了 owned checkpoint 的 SHM 时间，三个复制型审计在 cleanup snapshot 阶段按设计 fail closed；审计内容本身没有报告数据问题。
- **Fix:** 不修改通用 audit infrastructure，停止并行打开同一 source，按 readiness → entity quality → library contract → media 的顺序逐项串行重跑；最终全部通过，输出 checkpoint、Phase 595 source 与真实库哈希均核对不变。

**3. [Rule 1 - Format] 用 Biome 机械整理目标测试**

- **Found during:** 精确静态检查
- **Issue:** 测试逻辑通过，但 import 排序和长测试块未符合 formatter 输出。
- **Fix:** 只对目标测试运行 Biome `--write`，随后重新执行 Biome、TypeScript 与完整定向回归；最终均通过。

**4. [Rule 3 - Runtime selection] Production readback 显式使用 disposable database**

- **Found during:** standalone runtime 准备
- **Issue:** standalone 内置默认数据库仍是旧真实库文件的只读构建副本，不能证明 Phase 596 页面。
- **Fix:** 从 Phase 596 checkpoint 通过 `copyCheckpointedCatalogToDisposableCopy` 建立新的临时 production copy，并用显式 `FPKG_DATABASE_URL=file:...` 启动 server。九个 URL 与四个 SVG 哈希均从该 disposable copy 回读；没有使用内置旧库进行验收。

## Known Stubs

None。十二个实现文件没有 TODO、FIXME、placeholder、coming-soon 或阻断目标的数据空壳。

## Threat Review

没有新增网络 endpoint、auth path、schema trust boundary 或远程数据库路径。全部数据库操作受 caller-owned realpath／inode／link／snapshot 与空 remote selectors 约束；Turso 未访问。

## Evidence

本地 checkpoint 与验收材料保存在本 quick 的 `checkpoint/`、`evidence/` 下，未进入实现提交。实现 commit 为 `91aa0c636d7cc720df96dbe251bd4a876933dc26`，精确包含计划允许的十二个文件，4138 insertions、0 deletions。

## 下一步

从 Phase 596 candidate 出发，将同品牌、同证据结构的剩余重要型号合并为更大的内容批次，减少逐型号重复 checkpoint／build 开销。内容封板后再统一迁移真实本地库并真人全页面遍历；Turso 可用且用户确认后才进行云端同步、生产部署和线上逐条复查。full-corpus goal 保持 active。

## Self-Check: PASSED

- 十二个实现文件存在，commit `91aa0c63` 可从 Git 历史读回，且没有 tracked deletion。
- commit 路径集合精确等于十二文件 allowlist；实现提交后 index 为空。
- SUMMARY、PLAN、STATE、checkpoint 与 evidence 均未进入实现 commit。
- targeted regression、offline audits、production build 与九 URL readback 均通过。
